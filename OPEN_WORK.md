# OPEN WORK — the register that outlives a session

> **This file exists because work kept getting lost.** A payments audit produced ~72
> findings that lived only inside a Claude session transcript. A preflight audit produced
> 36 more that lived in a file under `/tmp`. Both were invisible to the next session, so
> "we fixed x and y" quietly became "z never got done".
>
> **If you are a Claude session working in this repo: this is your backlog.** Work the
> highest severity open item, verify it against live `pg_proc`/production BEFORE believing
> the description, fix it with a migration that asserts its own effect, prove the fix
> discriminates (broken vs fixed on the same staged row, rolled back), run the gate, and
> move the row to Closed with the commit that did it. Do not wait to be asked.
>
> `__tests__/openWork.test.js` fails if this file drifts out of the format below, so it
> cannot rot into prose.

_Last reconciled: 2026-09-08 (a working payments audit: 11 fixes, none deployed yet — see the banner above the Closed block)._

> **`gated` is not a severity.** It marks a row blocked on a business decision or a
> milestone rather than on work — go-live, a device, a counterparty. It exists because the
> live-mode Stripe webhook sat as `high` for two days while being entirely correct: the
> platform is on Stripe SANDBOX and go-live is not scheduled, so there was nothing to do. A
> high row nobody can action reads as neglect and teaches people to skim the severity
> column. A gated row must say what it is waiting on.


> **2026-09-06, after the deploy.** All three waves are live: 51 migrations applied, 32
> edge functions deployed, the console and website shipped, and two OTAs published at
> the runtime build 34 carries. What remains open below is the 13 rows needing a product
> decision plus low-severity polish.
>
> **All four live-board items are now closed.** The four unanswered support tickets and
> the failed payout turned out to be TEST data and went with the pre-beta purge (see
> PRE_LAUNCH_DATA_RESET.md), as did the 20c fee under-collection from 2026-07-02 that
> `ctl_captured_fee_off_pin` had found. And the Stripe subscription is done: the
> test-mode account endpoint `we_1To5Y2E0UZFlVCOpeQsXtqFA` now carries
> `payment_intent.amount_capturable_updated` alongside its original nine, still
> `livemode: false`.
>
> That last one was NOT the minor thing I first called it. `accept-booking` only promotes
> a booking still `pending`, so on a RECOVERY re-hold against an already-confirmed
> booking nothing wrote the promotion back: the row sat at `pending` naming real money
> while both capture and earner-claim refused it with HOLD_EXPIRED — the earner unpaid on
> funds Stripe was genuinely holding. This webhook is the only thing that closes it.
>
> **The control board is clean: 0 open findings, 0 controls errored, 73 registered.**

## Open (101)

| Sev | Area | Finding | Fix |
|---|---|---|---|
| low | moderation | Four QA reports (2026-09-06) sit OPEN in `/moderation`, all against the throwaway gig "Qa temp listing - delete me45" (now `cancelled`, invisible in Browse). They were filed deliberately while verifying the new browse-card report control end to end — reporter and reported user are both the same test account. Each one also emailed the on-call address, so those four safety emails are expected, not an incident. | Resolve the four in `/moderation` (they are the only rows in the table). The `cancelled` job row is deliberately left so the queue's new gig link still resolves; hard-delete it too if a spotless `jobs` table matters more. |
| low | moderation | THREE hand-rolled copies of the report-reason sheet remain — `JobDetailScreen`, `PublicProfileScreen`, `MessageSheet` — each with its own Modal, styles and reason loop. `src/components/ReportSheet.js` (added 2026-09-06 for the browse card) is the shared one and is the fourth caller's only implementation. Nothing enforces that the four agree, so a reason added to `REPORT_REASONS` reaches all four but a layout or a11y fix reaches one. | Move the three onto `ReportSheet` (they are ~12 lines of markup each, and the sheet already takes `title`/`subtitle`). Do it with the screen open in the simulator, not by reading the diff: the browse sheet's two real defects — Cancel inside the home-indicator strip, and an 18pt touch target — were both invisible in review and obvious on device. |
| medium | moderation | Reporting is now one tap from a scrolling feed, and **every non-auto report pages the on-call human** (`trg_notify_safety_report` → `safety-alert` email, filtered only on `source <> 'auto'`). The rate limit caps a single reporter at 10/hour, so the platform-wide ceiling scales with the number of users, not with how serious the reports are. Nothing today distinguishes "this listing looks like a scam" from "I am at a stranger's house and afraid" — both arrive as the same email. | Decide before beta opens: either triage in `notify_safety_report`'s `WHEN` clause (page immediately for `source='emergency'`, for a report carrying a `booking_id`, and for the harassment/abuse reason; batch the rest into the daily `controls_digest`), or accept the volume and say so here. This is a product decision about who gets woken up, so it is Chris's call, not a fix to make quietly. |
| high | launch | **The waitlist collects addresses that cannot yet be legally mailed at launch.** `WAITLIST_POSTAL_ADDRESS` defaults to `Monroe, Louisiana, USA` — a city, not the valid physical postal address CAN-SPAM §316.5 requires in every commercial message. The confirmation email is arguably transactional (the person just asked for it); the LAUNCH email is unambiguously commercial, and it is sent by hand from a mail tool using the CSV export, where nothing enforces a footer at all. | Before the first launch send: register the entity, set the secret (`npx supabase secrets set WAITLIST_POSTAL_ADDRESS="…"`), and put the address plus an unsubscribe link into whatever tool does the send. The `/waitlist` Export card says this on screen; nothing enforces it, because the send happens outside this codebase. |
| medium | product | **Signups are OPEN (`beta_allowlist` holds `*`), so an invite grants nothing.** The waitlist still collects, and `inviteCohort` routes through the one existing write into the gate, so closing the beta is a one-row delete in `/access`. But until that row is deleted: waves are labels, `ctl_waitlist_invite_broken` is deliberately silent, and anyone who finds `/login` can create an account into an empty marketplace. | Chris's call, not an engineering one. Closing it makes the waitlist real and makes every existing tester's signup path invite-only. Leaving it open is defensible while the list is being built — but the two states should be chosen, not drifted into. |
| medium | marketing | **The landing page's remaining unverifiable claim is the device mock.** `PhoneMock` renders a fabricated feed — "6 gigs available", "Dana Kim ★4.9", a U-Haul job in Plano TX. That is normal practice for a UI screenshot and is not a testimonial or a statistic, so it was left. The invented STATS, category counts and three named testimonials were removed 2026-09-08 (see the comments above `STATS`, `CATEGORIES` and `FAQ`). | Decide whether the mock's sample data should be Monroe-flavoured rather than Plano/Ohio. Cheap, and it is the last place on the page that implies a market we are not in. |
| low | waitlist | **Nothing stamps `last_email_sent_at` for the LAUNCH send**, because that send happens in an external mail tool. `ctl_waitlist_emailed_after_optout` therefore watches only the confirmation email — the one send it least needs to watch. The suppression itself is sound (opt-outs are excluded server-side from both the invite and every export scope). | If the launch send ever moves in-house, stamp the column from it. Until then the control's coverage is honestly narrower than its name; consider renaming the finding text rather than leaving the gap implicit. |
| low | marketing | **The founder's FAQ draft says workers can be 16+; the platform is 18+.** The published Terms say "at least 18 years old", the Privacy Policy §14 says the same, `shared/age.js` sets `MIN_AGE = 18`, and `guard_min_age` (20260710040000) refuses a younger signup server-side. The landing FAQ was written to 18 with a comment recording why. | If 16+ is the actual intent it is a Terms + Privacy + trigger change plus a parental-consent question — not a copy edit. If 18+ is the intent, correct the FAQ document the copy came from so the next person does not reintroduce it. |
| gated | stripe-config | **NOT A DEFECT — a LIVE-CUTOVER GATE. Confirmed 2026-08-14 with Chris: the platform is still on Stripe sandbox and go-live is not scheduled.** Live mode has zero webhook endpoints (verified via `stripe webhook_endpoints list --live`: empty, 0 connected accounts, 0 payment intents). Nothing is broken today — the app runs on test keys and the test-mode endpoints are correct and complete. | **Do this as part of go-live, not before.** Create the live account webhook AND the live Connect webhook at `https://nfioebqsgmmzhbksxozc.supabase.co/functions/v1/stripe-webhook`, subscribed to the same events as the test endpoints (the account set plus the five `payout.*`), and put the live signing secret in the function env. In the SAME change, flip `app_flags.stripe_mode` to live — `ctl_stripe_id_mode_mismatch` is a deliberate no-op until then, and flipping it is what arms the check for stale sandbox ids. Full sequence in PRE_LAUNCH_DATA_RESET.md. |

| medium | docs | CLAUDE.md's remaining wrong assertions, from CLAUDE_MD_DRIFT.md — **which is known to contain at least one FALSE finding of its own** (the adminSurface claim, refuted by direct test), so every entry is a lead to VERIFY, not a fact. Worked 2026-08-14: the drift file's most actionable finding — four escape hatches in `adminSurface.test.js` — is CLOSED, each proved by probe (a commented-out nav entry now reads ORPHANED; a nav link to a nonexistent route fails naming it; pages outside the route group are enumerated; every route.ts handler must be listed with what it serves). Also corrected this session: the delete-account bucket count, the external-controls singular/plural contradiction, the "ENTIRE live schema" claim, and the rate-limit insert-then-count wording. | Remaining entries are per-surface detail in a 728-line file. Work them the same way — reproduce the claim against code BEFORE believing it, fix the doc, and add an assertion where one is possible. The inventory guard now covers edge functions, screens, routes, tables and controls, so future drift in those classes fails the gate rather than accumulating here. |
| low | docs | CLAUDE.md inventory, extended 2026-08-14. `__tests__/claudeMdInventory.test.js` now guards EDGE FUNCTIONS, SCREENS, ROUTES, **TABLES** and **CONTROLS** — 66 tables enumerated across 188 migrations plus 32 legacy files (20 were unnamed; now 0), and the registry proved bijective at 53 rows = 51 in-database + 2 external, correcting an earlier "51/49" that was itself wrong. All allowlists EMPTY; proved to discriminate by injecting a fake table, a fake control and an orphan ctl_ function. Remaining unguarded by DELIBERATE argument stated in the test header: 60 test suites and 34 components, leaf surfaces whose names would add doc rows and no checks. | Nothing outstanding unless that argument stops holding. |
| low | stripe-config | The `stripe_webhook_config` control is proven to EXECUTE (ran live on stripe@22, reported 0) but its DETECTION branch is unproven — removing an event to test it was blocked by the permission classifier, correctly. | Prove it once by hand: remove one payout event from the Connect endpoint, run `select public.controls_sweep_and_page();`, confirm a finding appears, then restore all six. Or accept that live cutover proves it: the control checks the mode the CURRENT KEY belongs to, so it will assert live-mode config the moment live keys are in use. |
| medium | release | SDK 55 needs a NEW BUILD for every existing tester — an OTA cannot carry it (runtimeVersion policy is `fingerprint`). **Current: build 1.4.2 (34)**, EAS `dd582110-1d19-48ad-bb1d-f55e31a7be30`, FINISHED and submitted to App Store Connect. Runtime **`7c71f381d18a5f39706e1f36ca82c5aeb06d13a7`**, read from the build's own phase log and reproduced locally by `npx expo-updates fingerprint:generate --platform ios`, zero mismatch warnings. It carries build 33 plus the 15 SDK-55 patch bumps (doctor 18/20 → 19/20). Build 32 ERRORED on the fingerprint drift; 33 FINISHED and is superseded. | Testers install 34. Before any `eas update`, run `node scripts/preflight-eas-build.mjs --expect-runtime 7c71f381d18a5f39706e1f36ca82c5aeb06d13a7` — it fails closed rather than publishing to nobody. Close this row when TestFlight shows 34 processed and a tester has updated. |
| medium | verification | SDK 55 native surfaces, updated 2026-08-14. **VERIFIED on the simulator:** app launch, Browse, Profile, maps (tiles + location dot), navigation, the Stripe payment sheet (Link + a saved card fetched live from Stripe), and **Transactions end-to-end after the refund-attribution rewrite** — summary reconciles ($45.00 gig totals − $4.50 fees = $40.50 earned), rows render, and the Bank deposits sheet resolves to its confirmed-empty state rather than spinning. Device log clean: only OS-level network/UIKit noise, zero JS errors. | Still unexercised and needing a REAL DEVICE or a second account: remote push (push.js returns null on simulators via Device.isDevice), Google sign-in (needs credentials), and a full `confirmPayment` through to capture (needs a counterparty — the poster side reads "No active applicants"). Walk those on TestFlight build 31. |
| medium | payments-audit | **REPRODUCED against current code 2026-08-14.** An earner's vested referral fee credit is consumed in full but delivered only in part (or not at all), and is never restored after a partial capture, a refund, or a chargeback (payments-core F10) — PARTIAL-CAPTURE HALF — FIXED. /Users/chrismalson/Documents/gohustle/supabase/migrations/20260813130000_return_unused_fee_credit.sql:44-100 adds return_unused_fee_credit(p_booking, p_delivered_cents), which walks the booking's state='applied' delivery='credit' rows newest-first and hands back applied_total − delivered, splitting the stradd | n/a — carried on the incentives F11 row. |
| medium | payments-audit | **REPRODUCED against current code 2026-08-14 and again 2026-08-17.** A poster's single discount use is consumed by whichever stranger applies first, not by the hire they make. `pin_booking_amount` gates the whole benefit chain on `if tg_op = 'INSERT'` (20260806320000:152) and calls `consume_poster_discount` at :200-202 — and INSERT is when an earner **applies**. Three people apply, the first burns the poster's one use, the poster declines that one and hires the third, and the hire they actually made carries no discount. **DETECTION now exists** (20260817030000, `ctl_benefit_held_by_pending`) — the loss is no longer silent. **The fix itself is still open.** | The fix is to consume at ACCEPT, and it is deliberately NOT a quick change: `poster_discount_cents` is one of the four immutable values pinned at INSERT, and CLAUDE.md rests capture's idempotency on the fee deriving only from values that cannot change. Moving it means quoting at INSERT, consuming at PI creation, and teaching `guard_bookings_write` to permit exactly one transition of a column whose entire purpose is never transitioning — including the case where two slots on one job are both accepted and the second finds the grant gone. That needs its own design pass against the idempotency proof, not a late addition to an unrelated day. Until then the control surfaces it at 48h and `expire_stale_pending_bookings(14)` remains the backstop. |
| medium | payments-audit | **Stolen-card cash-out — now MEASURED, not yet capped.** Destination charges put funds in the earner's Connect account at capture, Stripe pays out daily, and the platform absorbs chargebacks by the deliberate 20260813160000 decision. There was no velocity limit anywhere on that rail and nothing measuring it either. `ctl_earner_cashout_velocity` (20260817050000) now fires on the bust-out shape — a lot of money to one earner in seven days from almost nobody — with Connect-account age carried as evidence. | **The remaining work is the hard gate, and it is deliberately waiting for data.** The lever is CAPTURE, the only point the platform controls before funds move. A threshold picked with no traffic baseline either never fires or blocks the only real earners on the platform, and the second failure is what gets a control switched off for good. Write the cap once the control has ~a month of real distribution behind it — then gate `stripe-capture-payment` above the measured p99, holding rather than refusing, with a console release. |

> **The `payments-audit` rows have all been REPRODUCED against current code (2026-08-14).**
> They came from the 2026-08-12 audit that lived only on the unmerged branch
> `docs/payments-audit-2026-08`; the full reports are now on master at
> `docs/audits/2026-08-12-payments/`.
>
> All 42 transcribed findings were re-checked, not assumed. **17 were already fixed and 1
> was never true** — the code moved a long way on 2026-08-13/14, and five of the
> `settle_booking_benefits` findings turned out to be one defect stated five times. The 20
> rows that survive below carry evidence cited to current `path:line`, so they are leads
> that reproduce rather than a three-day-old report.
>
> The 30 `low` findings from that audit are still only in the reports on disk. Two of them
> were promoted here by the triage because a reader judged them worse than filed.
| high | audit-2026-09-05 | **The profile tombstone leaves school, major, grad year, class standing, DOB, availability, skill rates and the assistant's memory on the row — most of it granted to anon/authenticated and rendered on the public profile — and the leak control certifies it clean** — `supabase/migrations/20260813150000_profile_tombstone.sql:57`. tombstone_profile() scrubs exactly name, username, bio, avatar_url, city, skills and referral_code. Everything else on the profile row survives an erasure request: school, major, degree_type, class_standing, grad_year, student_status, student_verified(+_at, _method), work_status, skill_rates, avatar_initial (all in the cross-user SELECT grant, readable with the anon key), availability (granted to authenticated), plus owner-only date_of_birth, work_status_note, school_domain and assistant_memory (the AI's free-text notes about the person, replayed into prompts). PublicProfileScreen selects and renders school/major/grad_year for any userId, so a 'Deleted user' still displays their college, maj _(needs a product decision)_ | NEW migration that (a) extends tombstone_profile to null/blank school, school_domain, major, degree_type, class_standing, grad_year, student_status→'none', student_verified=false (+_at/_method), work_status_note, availability='[]', skill_rates='{}', assistant_memory='[]', date_of_birth, avatar_initial='D', lat/lng if present; (b) deletes the owner-only rows for p_user from assistant_messages/assistant_threads, class_schedule, saved_searches, notification_preferences, favorites, certifications, s Files: supabase/migrations/NEW_tombstone_scrub_completeness.sql, supabase/functions/delete-account/index.ts, __tests__/NEW_tombstone_columns.test.js. Verified by 3 independent refuters (edge-support-safety-ai#3). |
| medium | audit-2026-09-05 | **ctl_safety_checkin_overdue pages CRITICAL immediately at due_at because the 'nudge first' stage its registry entry and detail describe was never built — nothing anywhere writes safety_checkins.nudged_at** — `supabase/migrations/20260806230000_checkin_false_page.sql:13`. The check-in design is explicitly two-stage: 'First a gentle nudge to the earner, and only if that goes unanswered does it escalate — because the overwhelming majority of overdue gigs are somebody who forgot to tap a button, and a system that cries wolf on those is a system nobody reads.' The registry `why` repeats it ('the app nudges first and this only fires on the ones that stay silent') and the detail carries `'nudged', c.nudged_at is not null`. But no code path in migrations, edge functions, mobile, web or admin ever sets nudged_at or escalated_at — the columns exist and are read by the control only. So the control fires a CRITICAL security finding (paged by controls-alert as fresh) the _(needs a product decision)_ | Build the first stage: a sweep step `nudge_overdue_checkins()` (wrapped like the other housekeeping calls) that, for open check-ins past due_at with nudged_at null, stamps nudged_at and writes the in-app alert row the Alerts inbox reads (and/or dispatches a push through send-push via pg_net with a service secret, the same shape as the existing dispatchers). Then narrow the control to `nudged_at is not null and nudged_at < now() - interval '30 minutes'` so it fires only on the ones that stay sile Files: supabase/migrations/NEW_safety_checkin_nudge_stage.sql, __tests__/NEW_safetyCheckinNudge.test.js. Verified by 2 independent refuters (controls-registry#4). |
| medium | audit-2026-09-05 | **A booking can be inserted on a job that is not 'open': verifyAndRate closes a job while untaken future slots remain, both JobDetail pages only block 'cancelled', and no DB guard reads jobs.status at booking INSERT** — `supabase/migrations/20260730140000_pin_booking_insert_columns.sql:59`. The INSERT branch of guard_bookings_write validates self-booking, slot∈job and derives starts_at, but never checks the job's status; guard_booking_slot_not_past checks only the slot time; the RLS insert policy checks only earner_id. On the client, verifyAndRate sets jobs.status='completed' whenever no OTHER pending/confirmed/completed booking exists — ignoring free future slots on a gig the code itself says 'can have several earners'. Once status is 'completed' the gig leaves Browse (isJobBookable requires status==='open'), but SavedGigs still links into JobDetail, and both JobDetail pages hide the book CTA only when status==='cancelled', so the free future slot is still bookable. accept-boo _(needs a product decision)_ | (a) DB: in guard_bookings_write's INSERT branch (non-service-role), `if (select status from public.jobs where id = new.job_id) <> 'open' then raise exception 'This gig is no longer open.' using errcode = 'check_violation'; end if;` — service-role writers already check openness (assistant) or are admin paths. Probe: stage a job with status 'completed' and a free future slot, insert a booking as the earner → check_violation post-fix, success pre-fix, rolled back. (b) Both JobDetail pages: replace  Files: supabase/migrations/NEW_booking_requires_open_job.sql, src/context/JobsContext.js, web/lib/jobs.tsx, src/screens/JobDetailScreen.js, web/app/(app)/jobs/[id]/page.tsx, __tests__/NEW_bookingRequiresOpen. Verified by 2 independent refuters (db-guards-lifecycle#4). |
| medium | audit-2026-09-05 | **Tombstoning retains every per-user row the old cascade removed — push tokens keep delivering counterparty-authored pushes to a deleted person's other devices, their saved-search watchers keep firing forever, and their AI chat history, class schedule, expenses and income records persist with no owner able to delete them** — `supabase/functions/send-push/index.ts:390`. Thirty-one tables reference profiles(id) ON DELETE CASCADE; the tombstone rewrite keeps the profile row so none of them are cleared, and there is no purge job keyed on deleted_at. Concretely: (a) push_tokens survive — the client removes only the token of the device that pressed Delete (best-effort), so a second phone/tablet keeps receiving pushes; because bookings survive, a former counterparty still passes send-push's shared-booking gate with hasLiveRelationship = true and messages_insert still lets them write into the conversation, so caller-authored message text lands on the deleted person's lock screen and a notifications row is written for a banned uid. (b) saved_searches with notify =  _(needs a product decision)_ | New migration extending tombstone_profile: `delete from push_tokens, saved_searches, notification_preferences, conversation_state, favorites, saved_jobs, assistant_threads (cascades messages), assistant_pending_actions, class_schedule, expenses, income_entries, certifications, user_challenges, student_email_verifications where user_id = p_user` (keep legal_acceptances, reports, blocks, bookings, payments, messages, reviews as records of record); also `update bookings … ` is covered by the listin Files: supabase/migrations/NEW_tombstone_purges_owner_rows.sql, supabase/functions/send-push/index.ts, __tests__/NEW_tombstoneScope.test.js. Verified by 2 independent refuters (notifications-realtime-privacy#5). |
| medium | audit-2026-09-05 | **Account deletion now bans the auth user but leaves Google/Apple identities linked, so a deleted user can never sign up again with the same Google or Apple account** — `supabase/functions/delete-account/index.ts:218`. To keep the counterparty's records, delete-account no longer calls auth.admin.deleteUser; it rewrites the email to deleted-<uuid>@removed.invalid and bans the row for 100 years. That frees the email address for re-registration (as the comment says) but does nothing to auth.identities: the Google `sub` and Apple `sub` stay bound to the banned user. AuthContext's native flows use signInWithIdToken, which resolves an existing identity to its user; for a banned user GoTrue refuses the sign-in. Before this change deleteUser removed the identities and a fresh signup created a new account. Nothing in the diff, the register or KNOWN_RISKS records this consequence, and the ledger.test.js guard assert _(needs a product decision)_ | Decide whether a deleted user may return with the same OAuth identity (the Apple/GDPR deletion framing implies yes). If so, use Supabase's soft delete — `admin.auth.admin.deleteUser(user.id, true)` — which scrubs email/phone, removes identities and sessions but keeps the auth.users row so profiles_id_fkey does not cascade (verify in the sandbox that the profile row survives before relying on it), or explicitly remove each identity from auth.identities in the same step as the ban. Mirror the choi Files: supabase/functions/delete-account/index.ts, admin/lib/deleteUser.ts, __tests__/ledger.test.js. Verified by 2 independent refuters (recent-changes-review#5). |
| medium | audit-2026-09-05 | **Staff identities and internal resolution notes are readable by end users through `disputes`, `support_ticket_messages` and `payments` — no column lockdown, contrary to the `reports` precedent — and the uuids resolve to the staffer's real name via `profiles_select_all`** — `supabase/migrations/20260804010000_phase1_admin_ops.sql:84`. 20260705060000 deliberately hid `reports.resolved_by` and `reports.resolution` from clients ("internal to the console"). The same class of data was later added elsewhere with no lockdown: `disputes.assigned_to`, `disputes.resolved_by`, `disputes.resolution_note` (20260804010000), `support_ticket_messages.admin_id` (20260705040000, exposed to users by the 20260806360000 select policy) and `payments.refunded_by` (record_refund). All three tables have table-wide SELECT for `authenticated` (only UPDATE/INSERT/DELETE were revoked), and their SELECT policies are party/owner-scoped by row, not by column — so both parties to a dispute, the ticket owner, and both parties to a refunded payment can rea _(needs a product decision)_ | **The `disputes` third of this is CLOSED as of 2026-09-08 (`20260909030000`) — `resolution_note` was kept in the grant, because both dispute screens render it and the console now tells the operator it is user-visible. What remains open here is `support_ticket_messages.admin_id` and `payments.refunded_by`.** Mirror 20260705060000: `revoke select on public.disputes from anon, authenticated; grant select (id, booking_id, raised_by, reason, pct_paid, photos, status, resolved_at, created_at) on public.disputes to authenticated;` (decide whether `resolution_note` is user-facing — if it is, keep it in the grant and tell operators it is visible to BOTH parties in the console dialog); `revoke select on public.support_ticket_messages from authenticated; grant select (id, ticket_id, author, body, images, crea Files: supabase/migrations/NEW_staff_attribution_column_lockdown.sql, __tests__/NEW_staffAttributionColumnsUngranted.test.js. Verified by 2 independent refuters (rls-policies#2). |
| low | audit-2026-09-05 | **ctl_dead_listing_open fires on every normal in-progress or awaiting-verification gig, because the control lacks the live-booking exclusion its sibling sweep function has** — `supabase/migrations/20260813060000_expire_dead_listings.sql:170`. expire_dead_listings() closes an open job only when every slot is in the past AND no booking on it is pending/confirmed/completed. The control that is meant to notice the sweep failing copies the first two predicates and omits the third. A single-slot gig scheduled for 10:00 whose booking is confirmed is reported from the 10:05 sweep onward — while the work is literally in progress — and stays open through mark-done and until the poster verifies (which is when verifyAndRate sets jobs.status='completed'). That is the ordinary lifecycle of every gig, not a slipped listing. Severity is medium so it does not page, but every gig on the platform will pass through the open-findings list and the dig _(needs a product decision)_ | Either mirror the sweep's exclusion in the control so it reports only what the sweep should have closed and did not, or keep the live-booking case but only when the last slot is more than N days past (e.g. 7) and the booking is still confirmed/completed — i.e. the poster has not verified for a week — which is a state that genuinely needs a human. Product decision on which; either way add a rolled-back probe with a confirmed booking on a just-passed slot asserting 0 rows. Files: supabase/migrations/NEW_dead_listing_control_excludes_live_bookings.sql, __tests__/NEW_deadListingControl.test.js. Verified by 1 independent refuters (controls-registry#7). |
| low | audit-2026-09-05 | **A referral can be attached to any existing account at any time — referrals_insert_self has no signup-window or first-booking condition, so bonus campaigns pay for accounts that were never acquired** — `supabase/migration_referrals.sql:12`. The client records a referral once, at onboarding, from the signup metadata (OnboardingScreen → recordReferral). Nothing server-side enforces that timing: `referrals_insert_self` is `with check (auth.uid() = referred_id)` with no condition on profile age, onboarding state or prior bookings, and `accrue_referral_bonus` mints a bonus for whoever is in `referrals` at the moment the referred earner's booking flips to verified. Any long-standing account with no referrer can therefore POST `referrals {referred_id: me, referrer_id: X}` straight at PostgREST today and have its next verified gig charge the active bonus campaign for X. This is a materially different angle from the closed loyalty-ladde _(needs a product decision)_ | Decide the rule (e.g. a referral counts only if inserted within N days of `profiles.created_at` and before the referred user's first verified booking), then enforce it in a BEFORE INSERT guard on `referrals` (or in `accrue_referral_bonus`, which already has the booking in hand) with a migration that asserts its own effect; keep the client unchanged since it only inserts at onboarding. Files: supabase/migrations/NEW_referral_window_guard.sql, src/lib/referrals.js, __tests__/NEW_referralWindow.test.js. Verified by 1 independent refuters (mobile-screens-account#8). |
| low | audit-2026-09-05 | **A vested referral credit is not frozen by an open chargeback on its source booking and is never clawed back once spent — the payable-void pass keys on refunded_cents, which a chargeback never sets until an operator records it by hand** — `supabase/migrations/20260814150000_bonus_lifecycle_completes.sql:80`. The pending→payable pass refuses to vest while a disputes row is open, but the payable void pass added in 20260814150000 (the one the header says exists because 'a chargeback can arrive months later') tests only `payments.refunded_cents > 0 or p.status = 'refunded'`. A real chargeback (`charge.dispute.created`) reaches recordReversal in stripe-webhook, which inserts a `disputes` row and never touches refunded_cents; that column moves only when an operator runs the console's record_reversal op (→ record_refund). consume_fee_credit consults neither disputes nor the source booking. So between the chargeback landing and the manual 'Record chargeback', the payable credit remains spendable; and on _(needs a product decision)_ | Decide the policy, then in a new migration: (a) hold a payable bonus while an open disputes row exists on its source (mirror the pending gate) so consume_fee_credit cannot spend it — simplest is to add the same `not exists (select 1 from disputes …)` condition to consume_fee_credit's row selection or to move such rows to a 'held' state; (b) for source reversals after application, either record a negative/debt bonus_ledger row that consume_fee_credit nets against future credits, or explicitly doc Files: supabase/migrations/NEW_payable_bonus_held_by_open_dispute.sql. Verified by 2 independent refuters (money-db-incentives#5). |
| low | audit-2026-09-05 | **Referral attribution is client-asserted and time-unbounded: an established user can insert a referrals row naming any beneficiary and the next verified gig pays the full bonus** — `supabase/migration_security_hardening_4.sql:9`. The only insert policy on referrals requires `auth.uid() = referred_id and referrer_id <> referred_id`; UPDATE/DELETE are revoked. The app writes the row only during onboarding from signup metadata, but nothing in the database ties the row to signup time or to the referred user having no prior activity. accrue_referral_bonus pays whoever the row names on the referred user's NEXT verified booking. So any existing earner — including one with dozens of verified gigs — can POST a single row via PostgREST naming a friend or their own second account (profile ids are readable) and convert the next gig they were doing anyway into a full bonus_cents credit for that beneficiary, at zero marginal cost. _(needs a product decision)_ | New migration: a BEFORE INSERT trigger on referrals that refuses when the referred profile already has any bookings row (or is older than N days), and/or have accrue_referral_bonus require `referrals.created_at < (select min(created_at) from bookings where earner_id = new.earner_id)`; add a control listing referral rows created after the referred user's first booking. The window (days) is a product choice. Files: supabase/migrations/NEW_referral_must_predate_activity.sql. Verified by 1 independent refuters (money-db-incentives#9). |
| low | audit-2026-09-05 | **Three layers disagree on which admin tier may move money: the console server actions admit `finance`, the edge function's shared gate only knows `admin`/`support` and denies finance, and the UI tells finance it needs admin** — `supabase/functions/_shared/adminAuth.ts:154`. admin/lib/guard.ts documents `finance` as 'payments, refunds, payout intervention, escrow' and every money server action in bookings/actions.ts runs under requireFreshAdmin('finance'), forwarding the operator's own JWT to admin-payment-action. That function calls requireAdminCaller(req, 'admin', 300), and the shared gate's role type is `'admin' \| 'support'` with `if (minRole === 'admin' && role !== 'admin') forbidden` — so a finance member is refused. The InterventionPanel, meanwhile, hides the whole panel unless ctx.role === 'admin'. Net effect: a finance-tier operator is told by the UI they need admin, could invoke the server action directly and have an audit 'intent' row written for a re _(needs a product decision)_ | Decide the tier once and pin all three layers to it. If finance owns money: extend _shared/adminAuth.ts's AdminRole and satisfaction sets to mirror admin/lib/guard.ts (admin ⊃ finance/trust ⊃ support), call requireAdminCaller(req, 'finance', 300) in admin-payment-action, and render the panel for finance. If only admin may move money: change the server actions to requireFreshAdmin('admin') and correct the guard.ts/CLAUDE.md role description. Either way add a test that parses the three gates and f Files: supabase/functions/_shared/adminAuth.ts, supabase/functions/admin-payment-action/index.ts, admin/app/(console)/bookings/actions.ts, admin/app/(console)/bookings/[id]/InterventionPanel.tsx, admin/lib/g. Verified by 1 independent refuters (money-edge-escrow#6). |
| low | audit-2026-09-05 | **admin-payment-action `settle` has no booking-lifecycle gate — the console offers 'Settle & pay earner' on any authorized payment, including a still-pending application whose earner was never confirmed** — `supabase/functions/admin-payment-action/index.ts:172`. Both other capture paths enforce the lifecycle server-side: stripe-capture-payment requires the booking to be completed/verified and earner-claim-payment requires earner_done and a non-final status. The settle op checks only `pay.status === 'authorized'`, captures in full, credits the earner, and writes bookings.status='verified' unconditionally. The InterventionPanel enables the button on `paymentStatus === 'authorized'` alone, ignoring booking status. A pending booking can carry a genuine requires_capture hold (accept-booking lost a BOOKING_CHANGED/HOLD_RELEASED race, or the tab closed after the card sheet), so an operator working from the queue can charge the poster the full amount and cr _(needs a product decision)_ | In the settle op read the booking and refuse unless status is confirmed/completed (and, per product decision, earner_done = true), returning a distinct code the panel can show; gate the button on the same booking statuses; when settling a confirmed booking set earner_done/poster_done/completed_at alongside status so the row is self-consistent. Files: supabase/functions/admin-payment-action/index.ts, admin/app/(console)/bookings/[id]/InterventionPanel.tsx, __tests__/NEW_settleLifecycleGate.test.js. Verified by 1 independent refuters (money-edge-escrow#7). |
| low | audit-2026-09-05 | **Web tells a poster whose tip card was declined to 'try the tip again', but neither client has any way to send a tip after verification — a failed tip is silently lost** — `web/lib/jobs.tsx:950`. The only tip entry point on both clients is the Verify & Rate sheet, and the tip runs AFTER the booking is written 'verified'. If stripe-tip fails (decline, authentication_required, TIPS_PAUSED, 500), verification stands and the sheet is gone: EarnScreen/GigsScreen/PaymentsScreen have no 'add a tip' control, the assistant has no tip tool, and CompletionModal only opens for completed bookings. The web copy nevertheless instructs 'Update your card in Payments and try the tip again', a step that does not exist; mobile says only 'the tip could not be charged'. The count cap of three tips per booking (trg_guard_tip_caps) and the reservation-key release on confirm show repeat tipping is intended,  _(needs a product decision)_ | Product decision: either add a post-verification tip entry point (a 'Tip <earner>' action on a verified booking in Hire → Past and on the web equivalent, calling the same stripe-tip — which also makes the count cap meaningful) or correct the copy on both clients to say the tip was not sent and will not be retried. Either way, write the failed tip attempt server-side (stripe-tip already knows it) so support can see it, rather than only in the client ring buffer. Files: web/lib/jobs.tsx, src/context/JobsContext.js, src/screens/GigsScreen.js, web/components/CompletionModal.tsx. Verified by 1 independent refuters (money-edge-webhook#8). |

> **2026-09-05 — a full-platform audit added 79 rows here and closed 54.**
> 20 read-only finders produced 157 findings; 136 survived dedup and each was attacked by
> independent refuters (3 lenses for high, 2 for medium, 1 for low). 133 survived, 3 were
> refuted. Wave 1 fixed 54 of them in isolated worktrees, merged as a99ac38 with the gate
> green (106 suites / 1781 tests, both Next builds, deno check on 14 edge functions).
> **Every row below carries the finder id it came from, so its full evidence is
> recoverable.** Nothing from this audit is deployed: 14 of the fixes need
> `supabase db push`, 15 distinct edge functions need deploying by hand, the admin console
> needs `cd admin && npx vercel --prod --scope go-hustlr`, and the mobile half is an OTA.

| high | disclosure | **The landing page and the site's meta description still ship the "48 hours to your bank" figure that the same file's own comment says was removed as invented** — `web/app/page.tsx:73`. A student signs up off the landing page for their first gig, worth $60, finished Friday. The poster verifies Friday evening; capture succeeds. Because this is the account's first payout, Stripe holds it 7–14 days. The earner sees nothing in their bank the following Sunday, contacts support, and is told what the app's own careful copy says — that deposits follow Stripe's schedule. The 48-hour figur _(audit-2026-09-08/display-truth, 2/2 confirm.)_ | Delete the number from both places, as STATS already did. STEPS[2] becomes something true by construction ("Money is held before you start. The poster verifies, and it is released to your bank account through Stripe.") and layout.tsx's DESCRIPTION drops the trailing clause. Change PayoutSetupScreen.js:287 to name Strip |
| high | escrow | **The hold-expiry escape hatch added to earner-claim-payment is unreachable from either client, so a gig booked 4+ days ahead still has no self-service payout path — the exact gap the code comment says it closed** — `supabase/functions/earner-claim-payment/index.ts:147`. Poster accepts on day 0, so `payments.authorized_at` = day 0 (stripe-create-payment-intent:584) and Stripe voids the uncaptured manual-capture authorization at about day 7. The gig's slot is day 5 — booking a week ahead is ordinary. The earner works it, marks done, the poster ghosts. Server: `scheduledTimePassed` from day 5, `holdNearlyExpired` from day 6, so the claim is genuinely settleable duri _(audit-2026-09-08/earner-claim, 3/3 confirm.)_ | Either (a) expose the hold timestamp to the clients — `transformBooking` already embeds the job; add `payments.authorized_at`/`created_at` to the bookings select and give `canClaimEarnerPayment` an optional `holdPlacedAt` so it can evaluate the same second anchor the server does; or (b) relax the client gate to offer t |
| high | reconcile | **Pass 2 asks Stripe what money came back out, then silently discards every reversal on a PaymentIntent that has no `payments` row — which is every tip on the platform** — `supabase/functions/reconcile-stripe/index.ts:150`. A poster charges back (or an operator refunds from the Stripe Dashboard) a $40 tip. The `charge.refunded` / `charge.dispute.created` webhook is not delivered — endpoint disabled, signing secret rotated, or a plain `supabase functions deploy` re-armed gateway JWT verification so stripe-webhook 401s before its own auth runs (the failure supabase/config.toml is written to prevent). On the next hourly _(audit-2026-09-08/reconcile, 3/3 confirm.)_ | After the `pullIn` at index.ts:172-179, diff the requested PI set against `byId`: for each `reversalPi` that matched no `payments` row, look it up in `tip_ledger.payment_intent_id` and either (a) compare Stripe's reversed total against `tip_ledger.reversed_cents` exactly as check #4 does for gigs, filing a finding when |
| medium | Database guards that pin money columns (guard_bookings_write, pin_booking_amount, pin_payment_fee_bps, guard_jobs_write, guard_profiles_write, guard_disputes_write, and every trigger/policy/grant on bookings/payments/jobs/profiles/disputes) | **amount_cents_quoted is pinned when the earner APPLIES, but jobs.pay/estimated_hours stay editable until a booking is CONFIRMED — and every earner-facing pay display reads the unpinned jobs.pay** — `src/screens/EditJobScreen.js:128`. Poster lists "Move a couch — $60 flat". Earner applies; trg_z_pin_booking_amount stamps amount_cents_quoted = 6000 on that booking row. Nobody else applies, so the poster opens Edit gig (unlocked — the only booking is 'pending') and raises Pay to $600; guard_job_pay_floor permits anything up to $10,000 and guard_jobs_write's has_active early return lets it through. The poster then accepts that sam _(audit-2026-09-08/Database guards that pin money columns (guard_bookings_write, pin_booking_amount, pin_payment_fee_bps, guard_jobs_write, guard_profiles_write, guard_disputes_write, and every trigger/policy/grant on bookings/payments/jobs/profiles/disputes), re-verified 2/2 confirm.)_ | Same pin-first rule already applied to MoneyGoalCard, shared/taxFormat.js and shared/badges.js (OPEN_WORK rows 140/204/238): make every per-booking pay display go through `bookingGrossDollars(booking, job)` — pinned `amountCentsQuoted` first, list pay only as the pre-pin fallback — at src/screens/EarnScreen.js:304/568/ |
| medium | Database guards that pin money columns (guard_bookings_write, pin_booking_amount, pin_payment_fee_bps, guard_jobs_write, guard_profiles_write, guard_disputes_write, and every trigger/policy/grant on bookings/payments/jobs/profiles/disputes) | **bookings.completed_at is written from the handset clock, pinned by nothing, and it is the year bucket for the Tax Center's income figure and year-end CSV** — `src/context/JobsContext.js:592`. (a) Honest: an earner finishes a gig late on 31 December and taps 'I finished this job'; their phone's clock is a few hours slow (or fast) across the boundary, so completed_at is stamped in the wrong calendar year while the money is captured in the other one. platformIncomeForYear buckets purely on that string, so the gig's net earnings appear in the wrong year in the Tax Center headline, in the s _(audit-2026-09-08/Database guards that pin money columns (guard_bookings_write, pin_booking_amount, pin_payment_fee_bps, guard_jobs_write, guard_profiles_write, guard_disputes_write, and every trigger/policy/grant on bookings/payments/jobs/profiles/disputes), re-verified 2/2 confirm.)_ | Mirror trg_guard_conversation_state_read_at: in guard_bookings_write, on UPDATE force `new.completed_at := coalesce(old.completed_at, case when new.status = 'completed' and old.status <> 'completed' then now() else old.completed_at end)` for non-service-role writers (write-once, server clock, never rewritable), and nul |
| medium | Do the money controls actually discriminate? | **ctl_dispute_notice_missing fires on every refund and chargeback record the webhook files, and its remedy re-arms the settler on exactly the rows 20260909040000 made inert** — `supabase/migrations/20260909010000_a_dispute_that_both_parties_can_answer.sql:494`. A poster charges back a $200 gig that was already captured and paid out. `charge.dispute.created` → `recordReversal` files a `disputes` row with no `proposed_pct`; `dispute_set_defaults` stamps `respondent_id` = the earner; `dispute_notify_respondent` correctly writes no notice. 30 minutes later `ctl_dispute_notice_missing` opens a finding against a perfectly healthy row and tells the on-call: "Th _(audit-2026-09-08/Do the money controls actually discriminate?, re-verified 2/2 confirm.)_ | Give the control the same predicate its trigger already has: add `and d.proposed_pct is not null and d.pct_paid is null` to `ctl_dispute_notice_missing()`. Rewrite the remedy so it never names `settle_after` — the correct action on a genuinely-notice-less adjustment is to re-fire `dispute_notify_respondent` (or insert  |
| medium | Do the money controls actually discriminate? | **ctl_escrow_hold_expiring_work_done auto-resolves at the exact moment the loss becomes permanent, and no registered control reports a live booking whose only payment row is 'cancelled' or 'failed'** — `supabase/functions/stripe-webhook/index.ts:566`. Poster accepts a gig on day 0; the slot is day 6. The earner works it and taps "mark done" (earner_done = true); the poster never verifies. Days 5-7: `ctl_escrow_hold_expiring_work_done` opens a HIGH finding — correctly. Day ~7: Stripe auto-cancels the uncaptured authorization, `payment_intent.canceled` arrives, the payments row flips to 'cancelled', and because `earner_done` is true the booking i _(audit-2026-09-08/Do the money controls actually discriminate?, re-verified 2/2 confirm.)_ | Add an arm (or a new control) that matches the terminal shape rather than the transient one: `payments.status in ('cancelled','failed') and bookings.status in ('confirmed','completed') and coalesce(bookings.earner_done,false) and not exists (a later captured payment on that booking)`. That row is durable, so the findin |
| medium | Money disclosure — what each party is TOLD about amounts, fees, percentages, timing and guarantees, on mobile, web, the public site and the assistant prompt | **The dispute screen's "what is at stake, in dollars" line is missing entirely for the poster and overstated for the earner, on both clients** — `src/screens/DisputeScreen.js:42`. A poster reports a problem on a $400 gig and proposes 50%. They open Hire → the booking → "You asked to pay 50% · waiting for their reply" → the dispute screen, which shows the words "You asked to pay" and the number "50%" and no dollar amount anywhere, because the booking is not in their earner-scoped list. Separately the earner on that same gig is shown "$200.00 of your pay is on hold" when $186 _(audit-2026-09-08/Money disclosure — what each party is TOLD about amounts, fees, percentages, timing and guarantees, on mobile, web, the public site and the assistant prompt, re-verified 2/2 confirm.)_ | Resolve the booking from `[...bookings, ...posterBookings]` (the pattern already used at JobsContext.js:589/622/696/822), or better, return amount_cents_quoted / fee_bps_quoted / fee_credit_cents from public.my_dispute so the screen does not depend on which client list the reader happens to be in. For the earner's line |
| medium | RLS and column exposure on money tables | **The subject-access export re-exposes every staff-attribution and internal column the migrations deliberately withheld from clients — including the two disputes columns locked down yesterday** — `admin/app/(console)/users/[id]/export/route.ts:144`. A poster files a payment adjustment, the console adjudicates it, decideDispute stamps disputes.resolution_pct and disputes.resolved_by = <staffer's auth uid>. The poster then makes a data-access request. An admin opens /users/<poster>/export; the disputes block matches on raised_by = the poster and emits select("*"), so the JSON handed to the poster contains assigned_to and resolved_by. Same shape _(audit-2026-09-08/RLS and column exposure on money tables, re-verified 2/2 confirm.)_ | Do not hand-maintain a second redaction list. Drive it off the live grants: before emitting a table, drop any column for which has_column_privilege('authenticated', <table>, <col>, 'SELECT') is false — that makes the export structurally incapable of leaking a column a migration withheld, and covers the next one automat |
| medium | admin-console | **The dispute-closing escrow guard fails OPEN: both of its reads discard the query error** — `admin/app/(console)/disputes/actions.ts:50`. During a transient PostgREST/pool failure a trust operator presses Resolve on a dispute carrying proposed_pct=60 whose payment is still `authorized`. The guard silently skips, the patch at :73-84 stamps resolved_at, and earner-claim-payment's gate — `.is('resolved_at', null)`, supabase/functions/earner-claim-payment/index.ts:174-179 — stops refusing. The earner's Claim button appears and captures  _(audit-2026-09-08/stepup-and-console, 2/2 confirm.)_ | Destructure `error` on both reads and throw/return a refusal on either, matching bookings/actions.ts:160-164 verbatim in shape: "Couldn't check whether this booking still has an escrow hold (…). Nothing was changed — retry in a moment." Also treat a null `d` as a refusal rather than a pass-through, since the row must e |
| medium | admin-console | **The dispute console presents a chargeback/refund record as a poster proposal at 100% with a reply window, and offers a Settle button the database refuses to arm** — `stripe-webhook/index.ts:227`. A cardholder charges back a $200 gig. stripe-webhook files a bare disputes row. A trust operator opens /disputes: the row reads "asked 100% · reply due —", and the case page tells them the earner was notified, has a reply window, and that the case self-settles at 100% if they stay silent. All four statements are false — dispute_settlement_pct returns null for that row forever (settle_after null, r _(audit-2026-09-08/stepup-and-console, 1/1 confirm.)_ | Make `proposed_pct is not null` the predicate on both screens, exactly as the two triggers do. When it is null, render the row as "reversal recorded — no adjustment proposed", hide the reply-window and silence-stands copy, and replace DecisionPanel with the status controls only (setDisputeStatus is the correct exit for |
| medium | admin-console | **The user detail page prints a fully-refunded charge as money collected — refunded_cents is not even selected** — `admin/app/(console)/users/[id]/page.tsx:150`. A $200 booking is captured and later refunded in full by admin-payment-action or charged back and recorded. payments.status stays 'captured' (CHECK-constrained to authorized/captured/cancelled/failed) and refunded_cents becomes 20000. Support opens /users/<id> for the poster who is asking why they were charged, or for the earner asking where their money went, and the Payment column reads "captured _(audit-2026-09-08/stepup-and-console, 2/2 confirm.)_ | Add `refunded_cents` to the select and render through the shared helpers already written for this — `netCollectedCents(p)` from admin/lib/money.ts, with the gross and the reversal shown beside it exactly as /payments/page.tsx:276-303 does. The three console surfaces that print a collected figure should all route throug |
| medium | admin-money | **"Release hold" has no booking-lifecycle gate and manufactures the one money state no control watches — a live booking with a dead hold** — `supabase/functions/admin-payment-action/index.ts:126`. A poster emails support asking to back out of a $100 confirmed gig. The operator opens /bookings/<id>, sees the hint "Hold is open — release it rather than refunding", and clicks Release hold WITHOUT force-cancelling. payments.status becomes 'cancelled'; bookings.status stays 'confirmed'. The console reports "Hold released. The poster was never charged." and says nothing about the earner. The earn _(audit-2026-09-08/admin-payment-action, 2/2 confirm.)_ | Have release_hold read the booking and refuse when bookings.status is 'confirmed' or 'completed' unless the caller passes an explicit acknowledgement, with a message naming what happens to the earner — or make the console action cancel the booking in the same call (the pairing forceCancel already performs). At minimum  |
| medium | admin-money | **"Record chargeback" silently discards the amount typed in the box beside it and always writes the FULL remaining capture as reversed — irreversibly, from the console's point of view** — `admin/app/(console)/bookings/[id]/InterventionPanel.tsx:180`. A $100 gig has captured (earner_amount_cents 9300 + fee_cents 700 = 10000). The cardholder disputes $30 and the platform loses that partial dispute; Stripe refuses refunds.create on a disputed charge, which is exactly why this op exists (index.ts:177-182). The operator types 30 into the amount box, confirms "Record a LOST CHARGEBACK against this booking?", and the browser sends no amount. record_r _(audit-2026-09-08/admin-payment-action, 2/2 confirm.)_ | Either wire the field through — pass `{ amount }` in the Record chargeback fire() call, read it in recordReversal exactly as refundPayment does at actions.ts:353-359, and forward amountCents to the edge (the op already accepts it) — or give Record chargeback its own confirm text and disable/blank the shared input when  |
| medium | disclosure | **The landing page tells posters their card is "captured at booking" and tells earners money is released when both parties mark it done — neither is what the code does, and the second one erases the whole poster-ghosting problem** — `web/app/page.tsx:132`. An earner finishes a $150 gig on Saturday and marks it done. The poster also marks it done, so the booking reads `completed`, and then goes quiet. Per the landing page both sides have marked it done, so the money is released — but no capture has occurred and no money has moved. The earner waits, and can only self-release three days after the scheduled slot time via Claim your payment, which the la _(audit-2026-09-08/display-truth, 1/1 confirm.)_ | Rewrite :132 to "Funds are authorised and held on the poster's card when they accept you — never at application — and captured when the work is verified." Rewrite :164 and :489-490 to name verification as the release condition, and add one line about Claim your payment after the grace window, which is already a MUST_KN |
| medium | disputes | **The settler's 50-row batch is permanently occupied by rows that can never settle, and at 50 it pays nobody while returning ok:true** — `supabase/functions/settle-disputes/index.ts:36`. Reversal rows are permanent residents of the settler's queue. An operator who reads a chargeback record and closes it with setDisputeStatus sets `resolved_at` and `status='resolved'` — but `pct_paid` stays NULL forever, so the row is still returned by the settler's list query on every sweep, for the life of the platform. Once 50 lifetime chargeback/Dashboard-refund records exist (plus any dispute  _(audit-2026-09-08/settle-disputes-fn, 3/3 confirm.)_ | Give the list query a predicate that matches what can actually settle, not what has not been paid: add `.not('proposed_pct', 'is', null)` (a row with no proposed_pct is a reversal record and can never settle by construction — that is exactly what 20260909040000 established) and, for the HOLD_EXPIRED class, exclude rows |
| medium | disputes | **respond_to_dispute accepts a stance on a refund/chargeback row, and branch 2 then auto-closes the record that exists to keep the reversal visible** — `supabase/migrations/20260909010000_a_dispute_that_both_parties_can_answer.sql:350`. A verified $200 booking is charged back (charge.dispute.created) or refunded at Stripe. recordReversal files a bare disputes row; dispute_set_defaults correctly gives it no settle_after and dispute_notify_respondent correctly sends no notice — but it DOES stamp respondent_id = the earner. The earner calls respond_to_dispute(id,'accept') — either from the screen (my_dispute has no ORDER BY, so on a _(audit-2026-09-08/dispute-sql, 3/3 confirm.)_ | Refuse in respond_to_dispute when `d.proposed_pct is null` (same predicate dispute_set_defaults and dispute_notify_respondent already use), and belt-and-braces the read side by making branch 2 `if d.response_stance = 'accept' and d.proposed_pct is not null`. Gate the accept/contest UI on `dispute.proposed_pct != null`  |
| medium | disputes | **respond_to_dispute never checks settle_after, so a reply after the window has closed cancels "silence stands" and moves the poster's outcome from the proposed percentage up toward 100** — `src/screens/DisputeScreen.js:194`. $200 gig, poster proposes 75% at 09:00 Monday; settle_after = 09:00 Wednesday. The earner ignores it. At 09:02 Wednesday — before the 09:05 sweep, or any time later if the settler is backlogged (MAX_PER_RUN=50), erroring, or the sweep is late — the earner opens the screen, reads "The reply window has closed", taps "That's not right", and responded_at is stamped. Branch 3 is now dead forever. The o _(audit-2026-09-08/dispute-sql, 2/2 confirm.)_ | Refuse in respond_to_dispute when `d.settle_after is not null and now() >= d.settle_after` (errcode check_violation, message "the reply window has closed"), and hide the accept/contest controls on both screens once hoursLeft is 0, replacing them with the closed-window state. If a small grace is wanted, make it explicit |
| medium | disputes | **The settlement notification describes a branch-4 timeout capture to both parties as an adjudicated verdict, and gives the poster no route to the refund it says is still possible** — `supabase/functions/settle-disputes/index.ts:214`. Poster reports a problem on a $200 gig and proposes 75%, attaching photos. The earner contests within 48 hours. Nobody at GoHustlr adjudicates. At authorized_at + 5 days dispute_settlement_pct branch 4 returns 100 and settle-disputes captures the full $200. The earner's inbox says "Paid in full — The reported problem did not stand", asserting a verdict nobody reached. The poster's inbox says "Your _(audit-2026-09-08/dispute-sql, 2/2 confirm.)_ | Pass the dispute row (or at least resolution_pct and response_stance) into notifyOutcome and branch the copy: adjudicated → "a member of the team reviewed both sides"; accepted/silence → the current wording; branch 4 → say plainly that no one decided in time, that the full amount was captured because a lapsed hold pays |
| medium | escrow | **`payments.authorized_at` records when the PaymentIntent was MINTED, not when the hold was placed — and `accept-booking` blocks the one writer that would correct it** — `supabase/functions/stripe-create-payment-intent/index.ts:579`. Poster opens "Accept & pay" on a $200 flat gig on day 0. The sheet is dismissed (card in the other room). `payments` = {status:'pending', authorized_at: day 0, created_at: day 0}; nothing is held. On day 6 the poster reopens it — the replay branch returns the same client secret, `authorized_at` is untouched — completes the sheet, and Stripe places a real 7-day authorization running day 6 → day 13. _(audit-2026-09-08/hold-creation, 3/3 confirm.)_ | Stamp the timestamp at the observation that makes the hold real, in both places that make it. Add `authorized_at: new Date().toISOString()` to the update in accept-booking/index.ts:104 — it has just verified `pi.status === 'requires_capture'` at :67, which is exactly the fact the column is defined to record, and it alr |
| medium | escrow | **accept-booking never re-checks suspension, so a banned earner's application can still be confirmed — placing a real card hold for a worker who cannot log in** — `supabase/functions/accept-booking/index.ts:44`. Earner E applies to poster P's $200 gig on Monday (booking inserted 'pending' — the suspension guard passes because E is clean). Tuesday, a DIFFERENT poster reports E and trust suspends the account. Wednesday, P — who was never told anything and sees an ordinary application card — taps "Accept & pay". stripe-create-payment-intent mints a $200 manual-capture hold and Stripe authorizes P's card; acc _(audit-2026-09-08/accept-booking, 2/2 confirm.)_ | Re-assert both parties' eligibility at confirm time, where the money and the address access are actually granted. In accept-booking, between the poster-ownership check and the Stripe retrieve, call a service-role RPC over private.is_suspended for booking.earner_id and for the job's poster_id and return a distinct 409 ( |
| medium | escrow | **NO_ROOM_TO_HOLD writes a pre-settled "captured in full" dispute record BEFORE the capture and never retracts it when the capture fails — and re-writes it on every retry** — `supabase/functions/stripe-capture-payment/index.ts:112`. A `completed` booking whose escrow Stripe voided at 7 days (payments.status='cancelled'). The poster opens Verify & rate (GigsScreen.js:873 still shows the button, because its `dispute` gate filters `.is('pct_paid', null)`), taps "report a problem" at 75%. `runwayHours = 168 - age < 36` → the NO_ROOM branch inserts the pre-settled row → `settleEscrow` returns `HOLD_EXPIRED` → 409. Nothing was capt _(audit-2026-09-08/capture, 3/3 confirm.)_ | Move the insert after `settleEscrow` returns `ok: true`. On `!settled.ok`, still record the poster's reason and photos, but WITHOUT `pct_paid`/`settled_at`/`resolved_at`/`status:'rejected'` — leave it an ordinary unsettled row so `ctl_dispute_settlement_overdue` and the /disputes queue can see it — and log fatal. Also  |
| medium | escrow | **ctl_dispute_notice_missing fires permanently on the two dispute shapes the design deliberately does not notify (chargeback rows and NO_ROOM_TO_HOLD rows)** — `20260909050000_a_settled_row_has_no_clock_to_announce.sql:29`. A cardholder charges back a completed gig. `recordReversal` inserts a bare `disputes` row; `dispute_set_defaults` gives it a `respondent_id`; `dispute_notify_respondent` deliberately skips it. Thirty minutes later `ctl_dispute_notice_missing` opens a MEDIUM finding whose remedy reads "They are being timed out of a window they were never told about — extend settle_after and tell them before it sett _(audit-2026-09-08/capture, 2/2 confirm.)_ | Add `and d.proposed_pct is not null and d.pct_paid is null` to `ctl_dispute_notice_missing`, mirroring exactly the predicate of the trigger it audits — the control must be re-created in a new migration whenever `dispute_notify_respondent`'s guard changes, since the two are one rule stated twice. A test in `disputeTwoPa |
| medium | escrow | **stripe-cancel-payment answers "success, nothing to release" when the payments lookup ERRORS — the one release path in the repo that fails open** — `supabase/functions/stripe-cancel-payment/index.ts:92`. Poster cancels a confirmed $150 gig. `cancelBooking` (src/context/JobsContext.js:863-866) writes bookings.status='cancelled' successfully, then calls stripe-cancel-payment. The bookings read at :44-51 succeeds; the payments read at :92-96 hits a transient PostgREST/pooler failure (the same class of failure forceCancel and deleteUser were hardened against). The function returns 200 `{success:true,  _(audit-2026-09-08/cancel-release, 2/2 confirm.)_ | Split the two cases: `if (pErr) { await logServerError('stripe-cancel-payment', `could not read the payments row for booking ${bookingId} — refusing to report the hold released: ${pErr.message}`, { booking_id: bookingId, booking_status: booking.status }, { fatal: true, userId: user.id }); return json({ error: 'lookup_f |
| medium | escrow | **The website tells the poster the booking was cancelled and a cancellation fee recorded even when the cancel was refused — including when the earner has already started work** — `web/app/(app)/hiring/page.tsx:112`. A $80 confirmed gig. The earner taps "Start job · I'm on site", stamping started_at. The poster, on gohustlr.com/hiring, opens the cancel modal — which reads "Cancelling now applies a cancellation fee of $12 to the worker. This releases the payment hold and notifies them." (page.tsx:474-476) — and confirms. cancelBooking either returns early on the cached startedAt (jobs.tsx:886-888) or the DB rai _(audit-2026-09-08/cancel-release, 2/2 confirm.)_ | Change web/lib/jobs.tsx's `cancelBooking` to `Promise<boolean>` returning false on all three refusal paths (mirroring src/context/JobsContext.js exactly — it already returns true/false), update the JobsValue signature at :188, and have confirmCancel bail on a false result the way src/screens/GigsScreen.js:277-281 does. |
| medium | escrow | **The mobile earner is offered "Claim your payment" and told "Waiting for the poster to verify & pay" while a dispute proposal's 48-hour forfeiture clock is running — the web client suppresses both** — `src/screens/EarnScreen.js:528`. Gig scheduled 2026-09-01, $100, earner marks done. On 2026-09-06 the poster verifies with a reduction (`pct = 0.6`); stripe-capture-payment captures nothing, writes a `disputes` row with `proposed_pct = 60` and `settle_after = now + 48h`, and the booking stays `completed` with the payment still `authorized`. The earner opens My Jobs on iOS. `canClaimEarnerPayment` is satisfied (earnerDone, status  _(audit-2026-09-08/earner-claim, 3/3 confirm.)_ | In src/screens/EarnScreen.js, mirror the web: compute `const disputed = !!openDisputes[booking.id]` in the `completed` branch, render `{!disputed && renderClaimCta(booking)}`, and swap the waiting text for "Nothing is paid while this is open — open the report above." when disputed. Then add an assertion to `__tests__/d |
| medium | ledger | **Web Transactions turns a failed bank-deposits read into "No deposits recorded yet." — the exact lie mobile's three-state exists to prevent** — `web/app/(app)/profile/transactions/page.tsx:79`. An earner who has been paid opens gohustlr.com → Transactions → Bank deposits. The `stripe_payouts` select fails for any reason (offline, PostgREST 5xx, expired access token, RLS hiccup) — `stripe_payouts_select_own` is a normal authenticated policy, so this is an ordinary network failure, not an exotic one. The catch sets `deposits = []`. The button subtitle reads "none yet" and the modal reads " _(audit-2026-09-08/ledger-clients, 3/3 confirm.)_ | Mirror mobile exactly: `deposits: Deposit[] / null / 'error'`, initialise to null, `.catch(() => setDeposits('error'))`, render a spinner for null, the existing empty copy for [], and an error row with a Retry for 'error'. web/lib/payments.ts already throws correctly; the swallow is only in the page. __tests__/webLedge |
| medium | ledger | **Web's summary deducts the refund a second time — "Total charged $30.00" sitting directly above "Refunded −$30.00" — because it dropped mobile's gross anchor and kept a minus sign** — `web/app/(app)/profile/transactions/page.tsx:198`. $60 gig, captured, then $30 refunded (staged through the shared module: earner stats → netCents 2790, refundedCents 2790, grossCents 6000; poster → netCents 3000, refundedCents 3000, grossCents 6000). Web poster sees "Total charged $30.00" with "Refunded −$30.00" beneath it, which reads as $0.00 net and shows the refund — money that came BACK to them — carrying a minus on the payer's own statement _(audit-2026-09-08/ledger-clients, 3/3 confirm.)_ | Port mobile's two corrections into the web summary: add the gross (`sum.grossCents`) row as the anchor, and make the refund row side-aware — poster: "Refunded to you" +$X in success colour; earner: "Your share of refunds" −$X. Both figures already come from the shared stats(); only the presentation is wrong. |
| medium | ledger | **The CSV export and the on-screen summary bypass every correction receiptLines makes, so one booking is stated three different ways — and the poster's CSV row does not add up** — `shared/ledger.js:372`. An earner completes a $200 gig carrying a $7.65 poster-discount grant. They tap a row and read "Gig total $200.00, Platform fee (7%) − $14.00". They scroll up and the summary for the same period says "Gig totals $192.35, Platform fees − $6.35". They tap Export CSV and hand the file to an accountant: gross income $192.35, platform fee $6.35, fee rate 7%. A poster exporting the same booking gets a r _(audit-2026-09-08/ledger-clients, 2/2 confirm.)_ | Make ledgerCsv and stats consume receiptLines rather than the raw columns — the corrected gigTotal and earnerFee are already computed there and the invariant (lines sum to total) is already asserted by __tests__/ledger.test.js. For the rate column use `effectiveFeeLabel(gigTotal, feeBps)` and leave the cell blank when  |
| medium | payouts | **A Connect account Stripe has DISABLED for a reason outside a 9-value allowlist is reported to the earner as "Stripe is verifying your details — nothing more is needed from you", forever** — `supabase/functions/_shared/connectStatus.ts:177`. Stripe disables an earner's Express account with `requirements.disabled_reason = 'other'` (or 'action_required.requested_capabilities') and no outstanding user-actionable fields. payouts_enabled goes false, so `onboarded` goes false and stripe-connect-status writes that to the cache. deriveConnectStatus returns state='pending', needsAction=false, pendingVerification=true. PayoutSetupScreen.js:320- _(audit-2026-09-08/payouts-connect, 2/2 confirm.)_ | Invert the default for a non-null `disabled_reason`: treat any disabled_reason that is not one of the two known pending values as `restricted` rather than letting it reach the `else state = 'pending'` fall-through. Concretely, add `// (!!disabledReason && !['requirements.pending_verification','under_review','requiremen |
| medium | promotions | **settle_booking_benefits still prices a fee_override against the standing rate card while consume_promo_grant reserves against the booking's pinned baseline — the half of 20260814120000 that was never done** — `supabase/migrations/20260906074000_a_partial_capture_charged_the_campaign_the_whole_discount.sql:130`. An operator enables the seeded "Veteran" tier (700 bps) or edits a tier below the standing rate and runs a 0% fee_override campaign with a $500 budget. For every booking by a tiered earner, consume_promo_grant reserves the true benefit (baseline -> 0) but stripe-capture-payment -> settle_booking_benefits (supabase/functions/_shared/settleEscrow.ts:529) re-prices it standing -> 0 and pushes spent_c _(audit-2026-09-08/promotions, 2/2 confirm.)_ | Give settle the same baseline the reserve used. The cleanest source is the booking itself: record the pre-promo baseline (e.g. store it on promo_redemptions alongside `fee_bps`, the delivered rate) at consume time, and have settle price `platform_fee_cents(p_amount_cents, baseline) - platform_fee_cents(p_amount_cents,  |
| medium | reconcile | **Both scan caps truncate silently, so an under-scanned run reports as a clean one — contradicting the code's own comment** — `index.ts:47`. The platform reaches ~15 bookings a day, so more than 200 payments have a created/authorized/captured/refunded/cancelled timestamp inside the trailing 14 days. Every hourly sweep now examines the newest 200 and the same tail is dropped on every run, permanently. A capture that succeeded at Stripe and failed to write here on payment #250-by-recency is never retrieved, so check #3 (`stripe_captured_ _(audit-2026-09-08/reconcile, 2/2 confirm.)_ | Push a warning whenever a cap binds: `if (payments.length >= limit) warnings.push(...)` after index.ts:137, and a `warnings.push` beside each `break` at :156 and :162. That alone turns a false-clean into an ERRORING control, which is the signal this file already relies on everywhere else. Longer term the window pass sh |
| medium | tax | **A failed bookings read reports $0 platform income as fact, and Export writes it into the accountant's CSV — the exact class the load-state fix covered for expenses, on the largest of the three inputs** — `src/context/JobsContext.js:333`. The earner opens Tax Center on a flaky connection, or after a migration adds a column the shipped build's job embed does not select (CLAUDE.md documents this exact PostgREST failure for jobs.category_slug), or on a fresh install where the AsyncStorage bookings cache is empty. `loadBookings` returns silently, `bookings` stays `[]`, `platformIncome` = 0. The expenses/income read succeeds, so `loaded _(audit-2026-09-08/tax-center, 3/3 confirm.)_ | Expose a per-collection status from JobsContext / web jobs.tsx (`bookingsStatus: 'loading' / 'ready' / 'error'`, mirroring UserContext's `profileStatus`) instead of `if (error // !data) return;`, and fold it into `unknownTotals` on both Tax Centers so a bookings failure prints the dash, shows the retry banner and refus |
| medium | tax | **The Tax Center is hardcoded to the current calendar year with no year selector, so from 1 January the year you actually file for is unreachable and un-exportable** — `src/screens/ExpensesScreen.js:118`. An earner works all of 2026 and opens Tax Center on 5 February 2027 to produce the summary for the return they are filing. `new Date().getFullYear()` is 2027, so the headline reads '2027 net profit $0.00', Income $0.00, Set aside $0.00, and the By-job card disappears. Tapping Export hits ExpensesScreen.js:227 and alerts 'Nothing to export — No income or expenses recorded for 2027 yet' (web shows t _(audit-2026-09-08/tax-center, 3/3 confirm.)_ | Add a year selector defaulting to the current year and offering at least the previous one — derive the option list from `min(expense.date, income.date, booking.completedAt)` so it only ever offers years with data. Thread the selected year through `inYear`, `platformIncomeForYear`, `yearSummary`, the By-job card and `bu |
| medium | tips | **A tip charged but never confirmed is invisible to every reversal path, and the control's printed remedy then credits the earner money Stripe already gave back** — `supabase/functions/stripe-tip/index.ts:176`. Poster tips $20. stripe-tip reserves the slot, Stripe charges the card successfully, and the function then dies before/at `confirm_tip_charge` (the state stripe-tip:216-240 logs as fatal and ctl_earner_credit_missing reports as `tip_reservation_unconfirmed`). The tip_ledger row now holds amount_cents=2000, payment_intent_id=NULL, credited=false. The poster complains 'I was charged $20 for a tip th _(audit-2026-09-08/tips, 3/3 confirm.)_ | Two independent changes. (a) Give the reversal a handle that exists before the charge: write the PaymentIntent id (or the Stripe idempotency key `resv_<row id>`, which stripe-tip already knows at :151) onto the reservation row immediately BEFORE `paymentIntents.create`, or have recordReversal fall back to matching `tip |
| medium | webhook | **Every tip fires a FATAL "unknown payment_intent" alert, and three tips in 90 minutes page the on-call with a HIGH finding** — `supabase/functions/stripe-webhook/index.ts:345`. A poster tips $5 on a verified gig. stripe-tip charges pi_X; Stripe delivers `payment_intent.succeeded` for pi_X; the handler finds no `payments` row and writes a FATAL `client_errors` row reading "succeeded for unknown payment_intent pi_X". Three tips anywhere on the platform inside 90 minutes -> `ctl_edge_errors_burst` opens a HIGH finding whose text is "A money edge function is failing repeated _(audit-2026-09-08/webhook, 2/2 confirm.)_ | In the `!row` branch, before logging, do what `recordReversal` already does: treat `pi.metadata?.type === 'tip'` (stripe-tip sets it at index.ts:180) — or a `tip_ledger` lookup on `payment_intent_id` — as a known, accounted-for PaymentIntent and `break` silently (or log non-fatal at most). Keep `fatal: true` only for a |
| low | Database guards that pin money columns (guard_bookings_write, pin_booking_amount, pin_payment_fee_bps, guard_jobs_write, guard_profiles_write, guard_disputes_write, and every trigger/policy/grant on bookings/payments/jobs/profiles/disputes) | **settle-disputes' held-review dedupe omits reviewed_user_id, so on a multi-earner gig the second earner's review is silently dropped (and every dispute-settled review renders a raw ISO timestamp)** — `supabase/functions/settle-disputes/index.ts:190`. A multi-slot gig with two earners — the codebase says so itself in verifyAndRate ('a multi-slot gig can have several earners'). The poster verifies earner A normally, writing a review row (job, poster, A, 'earner'). The poster then proposes a reduction on earner B's booking; it settles, and publishHeldReview queries by (job_id, reviewer_id, role) only, finds A's row, returns early — so earner B's  _(audit-2026-09-08/Database guards that pin money columns (guard_bookings_write, pin_booking_amount, pin_payment_fee_bps, guard_jobs_write, guard_profiles_write, guard_disputes_write, and every trigger/policy/grant on bookings/payments/jobs/profiles/disputes), re-verified 2/2 confirm.)_ | Add `.eq('reviewed_user_id', b.earner_id)` to the existence check at :192 so it matches the unique index and the client's own dedupe, and format `date` the same way both clients do (or better: stop trusting a text column and render from reviews.created_at). Extend __tests__/disputeTwoParty.test.js to pin the settler's  |
| low | Idempotency and races on money paths (capture, cancel, claim, tip, refund, promo redeem, bonus vest, the new dispute settler) | **settle-disputes moves the money before it claims the dispute row, so an operator's adjudicated percentage can be recorded, reported as applied, and silently discarded — and no control can see it** — `admin/app/(console)/disputes/actions.ts:191`. A contested dispute proposed at 55% has run past settle_after with no adjudication, so branch 3 makes it due. At 14:05:01 the sweep calls dispute_due_pct for it and gets 55. At 14:05:02 the trust operator, who has read both sides, submits 90% — the CAS passes because pct_paid is still null, and the console tells them the sweep will capture 90%. At 14:05:03 the sweep's capture lands at 55% and stam _(audit-2026-09-08/Idempotency and races on money paths (capture, cancel, claim, tip, refund, promo redeem, bonus vest, the new dispute settler), re-verified 2/2 confirm.)_ | Claim before capturing, not after: stamp a settling marker on the dispute with a CAS (e.g. `update disputes set settling_at = now() where id = $1 and pct_paid is null and settling_at is null returning proposed_pct, resolution_pct, response_stance`) and derive the pct from THAT returned row, so decideDispute and respond |
| low | Money disclosure — what each party is TOLD about amounts, fees, percentages, timing and guarantees, on mobile, web, the public site and the assistant prompt | **On every gig priced $10.00–$13.39 — the bottom of the allowed range at the live rate — the job's Payment panel says "GoHustlr is taking 7% in platform fee" beside an 8.4% deduction, and then says "we keep nothing on top"** — `src/screens/JobDetailScreen.js:560`. An earner opens a $10 gig. The Payment panel reads: Gig pay $10.00 / Payment processing −$0.84 / You receive $9.16, then "GoHustlr is taking 7% in platform fee on this gig. That $0.84 is card processing ($0.59) plus $0.25 toward payment costs — we keep nothing on top." Three of those statements cannot all be true: the deduction is 8.4% not 7%, GoHustlr's own share is $0.25 (2.5%), and "we keep not _(audit-2026-09-08/Money disclosure — what each party is TOLD about amounts, fees, percentages, timing and guarantees, on mobile, web, the public site and the assistant prompt, re-verified 2/2 confirm.)_ | In the floored branch, do not print feeLabel(feeBps) at all — state the platform's actual share instead, e.g. "On a gig this small, card processing costs more than our fee. Of the ${fee}, ${processing} is the card networks' and ${platform} is ours — less than our usual {feeLabel(feeBps)}." Add a pricing.test.js case at |
| low | Money disclosure — what each party is TOLD about amounts, fees, percentages, timing and guarantees, on mobile, web, the public site and the assistant prompt | **Five user-facing surfaces hardcode "48 hours" for a reply window the database computes as 24–48 hours, and the parity test pins the wrong number** — `supabase/functions/stripe-capture-payment/index.ts:31`. A gig is booked on a Monday and the hold is minted then; the work happens Saturday and the poster verifies Sunday morning, 6 days into the 7-day authorization (runway 40h, above the 36h floor so the proposal path is taken). dispute_set_defaults gives the earner until hold_dies − 12h, i.e. 28 hours. The poster's toast says "They have 48 hours to reply", the verify sheet said "48 hours to accept or  _(audit-2026-09-08/Money disclosure — what each party is TOLD about amounts, fees, percentages, timing and guarantees, on mobile, web, the public site and the assistant prompt, re-verified 2/2 confirm.)_ | Render the returned `settleAfter` instead of a constant: both toasts and both verify sheets already receive it. In the assistant prompt say "up to 48 hours, and the exact deadline is on the banner" and change parity.test.js:279 to assert that rather than the bare number. |
| low | Money disclosure — what each party is TOLD about amounts, fees, percentages, timing and guarantees, on mobile, web, the public site and the assistant prompt | **Mobile's verify sheet tells the poster "Confirming releases it to <earner>" while they are actively choosing to pay less — the same sheet says four lines down that nothing is released** — `src/components/CompletionModal.js:178`. A poster ticks "There was a problem — pay a reduced amount", picks 50%, types a reason, and taps a button labelled "Confirm job complete" while the panel at the top of the sheet says confirming releases the payment to the earner. Nothing is released; the money stays on hold for up to 48 hours and settles later. A poster reading the top panel believes they have just paid the earner 50% and closed t _(audit-2026-09-08/Money disclosure — what each party is TOLD about amounts, fees, percentages, timing and guarantees, on mobile, web, the public site and the assistant prompt, re-verified 2/2 confirm.)_ | Wrap the escrow box's second sentence in `{!disputed && …}` exactly as web/components/CompletionModal.tsx:185 does, or swap it for the proposal wording when `disputed` is true. |
| low | Money disclosure — what each party is TOLD about amounts, fees, percentages, timing and guarantees, on mobile, web, the public site and the assistant prompt | **Both clients tell a cancelling poster that a cancellation fee "applies… to the worker" and was "recorded" — no money moves and the worker is never told a fee exists** — `src/screens/GigsScreen.js:263`. A poster cancels a confirmed $60 booking. The dialog tells them a $9 cancellation fee applies to the worker; after confirming, a toast says a $9 cancellation fee was recorded. Their card is never charged $9, the earner never receives $9, and the earner's only message is "The poster cancelled 'Move a couch'". If the earner later asks support about the cancellation fee the poster mentioned, no such  _(audit-2026-09-08/Money disclosure — what each party is TOLD about amounts, fees, percentages, timing and guarantees, on mobile, web, the public site and the assistant prompt, re-verified 2/2 confirm.)_ | Until the fee is actually charged, say what happens: "Cancelling releases the payment hold and notifies the worker. A $9 cancellation is recorded against this booking as part of your cancellation history." Drop "to the worker" and drop "fee" from the success toast, or wire the charge. Whichever is chosen, the earner sh |
| low | Money disclosure — what each party is TOLD about amounts, fees, percentages, timing and guarantees, on mobile, web, the public site and the assistant prompt | **The landing FAQ tells earners they can report a payment problem and that a person reviews it — an earner cannot open a dispute at all, and the default outcome has no human in it** — `web/app/page.tsx:172`. An earner does a $200 job, the poster refuses to verify and then reports a problem proposing 50%. The earner, having read the FAQ, looks for a way to report their own problem with the poster. There is none — no button, no RPC, no RLS path. Their only lever is the one-shot reply on the dispute screen. If instead the poster simply never verifies, the earner has no "report" at all, and the FAQ's "not _(audit-2026-09-08/Money disclosure — what each party is TOLD about amounts, fees, percentages, timing and guarantees, on mobile, web, the public site and the assistant prompt, re-verified 1/2 confirm — SPLIT, read both verdicts.)_ | Rewrite to match the shipped model: the poster can propose paying less with a reason and photos; the earner is told, sees the money still held, and can accept or answer once with their own evidence; GoHustlr can step in; and if we do not decide before the hold runs out the earner is paid in full. Drop "either side can  |
| low | RLS and column exposure on money tables | **promo_grants.revoked_reason is operator free text that the revoked user can read, and nothing tells the operator that** — `admin/app/(console)/promotions/actions.ts:266`. An operator revokes a promo grant and, prompted to "say why", types a candid internal reason — e.g. "suspected code farming across three alt accounts, see report 12". The holder issues GET /rest/v1/promo_grants?select=revoked_at,revoked_reason with their own app JWT; the policy matches on user_id = auth.uid() and the column grant exists, so PostgREST returns the text verbatim. Nothing in the app o _(audit-2026-09-08/RLS and column exposure on money tables, re-verified 2/2 confirm.)_ | Pick one and make it explicit, the way the disputes note was: either drop revoked_reason from the column grant (`revoke select on public.promo_grants from authenticated;` + grant back the other 11 by name), or keep it and put a line under the reason field in PromoControls.tsx saying the person whose grant this is can r |
| low | escrow | **A recovery re-hold leaves the previous hold's `cancelled_at` on the row, so a live authorization is dated in both parties' Transactions to the day the dead hold was released** — `supabase/functions/stripe-create-payment-intent/index.ts:544`. A gig's first hold lapses and is cancelled on 20 December (cancelled_at = 20 Dec). On 5 January the poster re-accepts; a fresh authorization is placed and the row reads status='authorized', authorized_at=5 Jan, cancelled_at=20 Dec. Both the poster's and the earner's Transactions screen show this outstanding hold dated 20 December, filed under the previous year. `summarize()` counts its value into  _(audit-2026-09-08/hold-creation, 1/1 confirm.)_ | Add `cancelled_at: null` to the upsert payload at index.ts:544-585 — a row that is taking a new authorization is not a cancelled row, and `created_at` (untouched) already preserves the original-hold history the column was being relied on for. `ctl_escrow_hold_lapsed_uncancelled` and `ctl_escrow_hold_expiring_work_done` |
| low | escrow | **A failed read of the payments row is reported to the poster as "no hold exists", and both clients classify that answer as permanent** — `supabase/functions/accept-booking/index.ts:58`. The poster completes the Stripe sheet on mobile (GigsScreen.js:210-217): presentPaymentSheet resolves, so the PaymentIntent is now requires_capture and the card is genuinely authorized. acceptBooking fires, and the payments SELECT inside accept-booking hits a transient error (connection reset, PostgREST 5xx, a schema-cache miss right after a migration adds a column to payments). The function answe _(audit-2026-09-08/accept-booking, 1/1 confirm.)_ | Capture the error from the payments read and answer it distinctly and retryably — e.g. `const { data: payment, error: payReadErr } = …; if (payReadErr) { await logServerError('accept-booking', …); return json({ error: 'HOLD_UNVERIFIABLE', message: "We couldn't check the card hold on this booking. Try again in a moment. |
| low | escrow | **Both clients tell the poster the earner has "48 hours" while the server-derived reply window can be 24, and both ignore the settleAfter the server returns for exactly this purpose** — `stripe-capture-payment/index.ts:186`. A gig accepted Monday (hold minted then) and finished Saturday: hold age ~130h, runway ~38h, so the proposal is accepted. `dispute_set_defaults` sets `settle_after = authorized_at + 156h`, i.e. ~26 hours from now. The poster's Verify sheet and the success toast both say 48 hours. The earner is told correctly (the server-written notice interpolates `new.settle_after`, and DisputeScreen.js:80-82 com _(audit-2026-09-08/capture, 1/1 confirm.)_ | Render the returned `settleAfter` in both toasts and derive the CompletionModal sentence from the hold's remaining runway rather than a literal — e.g. "until <date/time>" from `capture.settleAfter`. Falling back to "48 hours" only when the field is absent keeps older builds working. |
| low | escrow | **declineBooking voids the hold BEFORE the guarded status write and rolls nothing back — on a failed write the hold is gone, the application is still live, and the poster has been told it was declined** — `src/context/JobsContext.js:790`. Poster's first accept attempt on a $90 gig fails after the payment sheet succeeds (accept-booking 500s, or the app is backgrounded). The booking is 'pending' with an authorized $90 hold. The poster gives up and taps Decline instead. stripe-cancel-payment voids the hold (status 'pending' is in CANCELLABLE_STATUSES) — then the `update({status:'declined'})` fails (offline, token refresh, RLS hiccup). _(audit-2026-09-08/cancel-release, 2/2 confirm.)_ | Invert the order in both clients to match cancelBooking: write `status:'declined'` first (the poster branch of guard_bookings_write already permits pending→declined and nothing else), roll the optimistic dispatch back and surface a toast if it fails, and only then call stripeEdge.cancelPayment with the same one-shot re |
| low | ledger | **The exported statement prints a fee RATE the same row's own numbers refute, on any gig under $13.41 — and contradicts the on-screen receipt for that booking** — `shared/ledger.js:191`. An earner completes a $10 dog-walk at the standing 700 bps. platform_fee_cents floors the fee at ceil(1000*0.029)+30+25 = 84c, so $0.84 comes off — 8.4%. They open Transactions: the receipt correctly reads 'Platform fee −$0.84' with no percentage. They tap Export CSV and hand the file to an accountant: the row reads Platform fee 0.84 under Fee rate 7%. 0.84 is not 7% of 10.00. The two artifacts th _(audit-2026-09-08/ledger-shared, 2/2 confirm.)_ | In ledgerCsv (shared/ledger.js:382), print `effectiveFeeLabel(e.grossCents + e.discountCents, e.feeBps) ?? ''` rather than `e.feeLabel`, so the column is blank exactly where the receipt drops the parenthetical. `entry.feeLabel` (set at :191) has no other consumer — grep shows ledgerCsv is its only reader — so it can be |
| low | ledger | **On a promoted booking the summary tiles report the platform's residual as the earner's fee and the discounted charge as the gig total, disagreeing by the discount with the receipt one tap away** — `shared/ledger.js:153`. A poster_discount campaign gives $5 off a $100 gig. EARNER opens Transactions → the tile says 'Platform fees − $2.00' and the CSV row reads "100.00"… no: "95.00","2.00","7%",…,"93.00". They tap the same booking and the receipt says 'Gig total $100.00 / Platform fee (7%) − $7.00 / Your payout $93.00'. Two numbers for the same fee, $5 apart, one tap from each other, and the CSV's own columns are sel _(audit-2026-09-08/ledger-shared, 2/2 confirm.)_ | stats() (shared/ledger.js:297-314) should compute the same quantities receiptLines does rather than summing the persisted columns raw: gross as `e.grossCents + e.discountCents` (the gig's value) and fees as the derived `earnerFee`, so a booking's summary line and its receipt cannot disagree. Same substitution in ledger |
| low | ledger | **The '$X in escrow' line on the money hub is filtered to the current calendar year, so on 1 January every live hold disappears from it** — `src/screens/PayoutSetupScreen.js:226`. A poster accepts a $100 booking on 28 December; the escrow hold is minted then. The gig is done 2 January and the earner opens Profile → Payments & payouts on 3 January to check the money is there. summarize defaults to year 2027, the December-dated entry is filtered out, held = 0, and the subtitle falls through to the else branch: '1 transaction · receipts, fees and refunds'. The line the screen' _(audit-2026-09-08/ledger-shared, 1/1 confirm.)_ | heldCents is a point-in-time balance, not a year-to-date total, and should not go through the year filter at all. Either give summarize a separate unfiltered `heldCents` (computed over all entries for that side) or have PayoutSetupScreen compute the escrow line from `stats(filterEntries(ledger, { side, range: 'all' })) |
| low | ledger | **An earner's fully-paid gig is thrown out of the 'Completed' filter by the poster's chargeback, which cost the earner nothing** — `shared/ledger.js:268`. An earner completes a $100 gig and keeps $93. Weeks later the poster's bank claws the $100 back; the platform eats it and the earner's payout is untouched. The earner filters Transactions → Earnings → Completed to reconcile their year: the gig is gone. They switch to Refunded and it is there, showing '+$93.00' with no refund sub-line, no refund line on its receipt, and the 'Your share of refunds'  _(audit-2026-09-08/ledger-shared, 1/1 confirm.)_ | Both STATUS_FILTERS predicates should use the side-scoped figure the module already computes: `settled: (e) => e.settled && !e.refundShareCents` and `refunded: (e) => e.refundShareCents > 0`. That leaves the poster's behaviour identical (their share IS the whole refund, toEntry:166) and stops the earner's classificatio |
| low | ledger | **Both ledgers are blind to disputes: an earner on a 48-hour reduction clock is told the full amount is "In escrow … Released to you when they verify the work"** — `supabase/functions/stripe-capture-payment/index.ts:198`. $60 gig. Poster taps "report a problem" at 50%. payments.status stays 'authorized'. The earner opens Transactions and sees "In escrow +$55.80", the summary "In escrow $55.80 · released on verify", and the note "Held by the poster. Released to you when they verify the work." All three are false: the poster HAS verified, the money is on a 48-hour clock, and if the earner does nothing the proposal st _(audit-2026-09-08/ledger-clients, 2/2 confirm.)_ | Add a dispute-aware state to shared/ledger.js: fetch the open dispute for the entry's booking, and for `status='authorized'` with a live proposal render a distinct chip ("Adjustment proposed") whose note names the proposed dollars and the deadline and links to the Dispute screen, instead of the generic escrow copy. For |
| low | ledger | **The six-month trend is computed from the range-filtered set, so months with real earnings render as $0 — and on web an all-zero chart sits under a non-zero headline** — `src/screens/PaymentsScreen.js:106`. Verified against the shared module with staged entries: with range='ytd' viewed in February, the trailing-six window Sep–Feb renders Sep=0.00 Oct=0.00 Nov=0.00 Dec=0.00 Jan=80.00 Feb=90.00 for an earner who actually earned $400/$500/$600/$700 in those four months. Tapping the "30 days" chip zeroes five of the six bars. Tapping "Last year" zeroes all six: mobile hides the strip, web renders six fla _(audit-2026-09-08/ledger-clients, 2/2 confirm.)_ | Feed monthlyTotals the side-filtered but NOT range-filtered set (`filterEntries(entries, { side, status, query })`), so the trend is always the real trailing six months, and label it "Last 6 months" so it is visibly independent of the range chips. Alternatively derive the window from rangeBounds so the bars only cover  |
| low | payouts | **The "View payout details" button shown to an earner whose ID is under Stripe review is refused by the server with "Finish setting up payouts first" — contradicting the sentence directly above it** — `supabase/functions/stripe-payout-login-link/index.ts:36`. An earner finishes Express onboarding; Stripe puts the account in review (requirements.pending_verification, or details submitted with capabilities not yet live). stripe-connect-status returns state='pending', onboarded=false. PayoutSetupScreen shows the "In review" pill and the text "Nothing more is needed from you… Payouts switch on automatically", then a "View payout details" button. Tapping it _(audit-2026-09-08/payouts-connect, 2/2 confirm.)_ | Loosen the gate to what Stripe actually requires for a login link: refuse only when there is no account at all, and otherwise gate on `details_submitted` rather than the composite `onboarded` (Stripe rejects a login link only for an account that has not completed onboarding). Alternatively, if the gate must stay, do no |
| low | payouts | **stripe-connect-onboard ignores the error from its stripe_accounts insert, so a duplicated request hands the user an onboarding link for an orphan Stripe account and their KYC lands on an account the platform never uses** — `supabase/functions/stripe-connect-onboard/index.ts:221`. Two stripe-connect-onboard requests for the same user overlap (a retried POST on a flaky mobile connection, or a duplicate submit before either response lands — the button-disable in PayoutSetupScreen.js:363 and payouts/page.tsx:232 only guards the local click, not a network-level retry). Both read no existing row, both call stripe.accounts.create, so TWO Express accounts exist. One insert wins; t _(audit-2026-09-08/payouts-connect, 1/1 confirm.)_ | Destructure the insert error and treat a unique-violation as "someone else won the race": re-select the stored row and mint the account link for THAT account_id, not the local one. Where the insert fails for any other reason, refuse rather than issue a link for an account the database does not know about. Consider `ups |
| low | promotions | **redeem_promo_code records the refused over-limit attempt, so a user who keeps retrying extends their own lockout indefinitely — the exact anti-pattern 20260814040000 removed from the MFA limiter** — `supabase/migrations/20260906031000_a_code_for_a_campaign_nothing_can_spend.sql:64`. A student is handed a campus code and mistypes it (the codes are 8 chars from a 32-symbol alphabet). After 8 tries the RPC starts refusing — but every refusal inserts another `promo_redeem_attempts` row dated now(). Because the window is `created_at > now() - 1 hour` over a table that keeps counting refusals, a user who taps "Apply code" 20 times over 40 minutes still has 12 rows in the window an  _(audit-2026-09-08/promotions, 2/2 confirm.)_ | Return false from the over-limit branch WITHOUT inserting, matching redeem_mfa_recovery_code (20260814040000:177-181). Same for the promotions_enabled branch — a refusal caused by the platform's own kill switch must not consume the user's budget. Add a rolled-back probe of the shape already in 20260814040000:258-278 (s |
| low | promotions | **The /promotions console tells the operator a poster discount is capped at $6.55 on a $100 gig; the live cap is $3.55 — the founding-rate figure survived in the UI prose after the SQL was corrected off it** — `admin/app/(console)/promotions/PromoControls.tsx:176`. An operator opens /promotions, clicks the "$10 off your first hire" preset, reads "capped at the fee headroom, which on a $100 gig is $6.55", and sizes and markets the campaign on that basis — "up to $6.55 off your first hire", or a $500 budget planned as ~76 uses. Every delivery is `least(poster_discount_cents, poster_discount_headroom(amount, fee_bps_quoted) - fee_credit)` (live consume_poster_d _(audit-2026-09-08/promotions, 2/2 confirm.)_ | Delete both hardcoded figures and render the number from the same RPC the preview already calls: `estimate_campaign_cost('poster_discount', ...)` already returns a rate-correct `per_use_cents`, so the paragraph can say "on a $100 gig at the current 7% rate that is $3.55" using the same server value. Then widen __tests_ |
| low | promotions | **estimate_campaign_cost's "if fully redeemed" total multiplies uses_allowed by max_redemptions, but max_redemptions bounds USES, not people — the shipped preset previews $600 against a real ceiling of $300** — `supabase/migrations/20260906075000_the_cost_preview_quoted_the_founding_rate.sql:83`. An operator sizing the "First 2 gigs free" campaign is told it costs up to $600 if fully redeemed and reads max_redemptions=200 as 200 students x 2 free gigs. The database caps the campaign at 200 total uses. Once 200 bookings have consumed it, consume_promo_grant's `redemptions_used < max_redemptions` fails and returns null for everyone still holding a grant with an unused second use. _(audit-2026-09-08/promotions, 1/1 confirm.)_ | max_total_cents for fee_override should be `per_use x greatest(1, p_redemptions)` — uses_allowed already lives inside the redemption count. If the intent is instead "max_redemptions is people", that is a change to consume_promo_grant's gate, not to the preview. Fix the `FM990.99` mask (`FM990.00` or `FM999.99`) so the  |
| low | promotions | **A grant an admin has revoked still answers TRUE from redeem_promo_code, so the console's revoke is immediately followed by "Code applied!" on the revoked person's phone** — `admin/app/(console)/promotions/actions.ts:266`. An operator revokes a grant (the action demands a written reason because "this takes something away from a named person") and the console reports "Revoked. Bookings already made with it are untouched." The person retypes the same code in Settings -> Redeem a code. The already-claimed branch finds the revoked row, returns true, and the app says "Code applied! It will apply to your next eligible gig _(audit-2026-09-08/promotions, 1/1 confirm.)_ | Add `and revoked_at is null` to the already-claimed EXISTS, so a revoked grant falls through to the same generic false as every other refusal — the existing bare-boolean shape is preserved and nothing new is disclosed. Optionally have revokeGrant write an Alerts-inbox row, since the person was told the code applied. |
| low | promotions | **The console's "Two-sided launch pair" preset instructs the operator to run a 0% fee override and a poster discount together, a pairing in which the poster-side campaign can never deliver a cent** — `admin/app/(console)/promotions/presets.ts:172`. An operator opens a campus with the recommended pair. Every booking where the earner holds the 0% grant pins `poster_discount_cents = 0`. The posters who redeemed "$10 off your first hire" are charged in full on exactly the launch bookings the campaign was created for, and the demand-side budget shows $0 spent — which reads as "nobody used it" rather than "it could not apply". _(audit-2026-09-08/promotions, 1/1 confirm.)_ | Say it in the preset and in the create form: a fee override and a poster discount are funded from the same per-gig margin and only one redemption can attach to a booking, so a 0% override leaves nothing for the demand side. Either pair the earner half at a non-zero rate that leaves headroom, or make the pairing explici |
| low | promotions | **consume_promo_grant and consume_poster_discount evaluate only the single best-sorted grant, so one exhausted campaign suppresses a second live, funded grant the user is also holding** An earner redeems a campus-launch code (0%, exhausted after 300 uses) and later a category-boost code (5%, live with budget remaining). They book a $100 gig. consume_promo_grant sorts the 0% grant first, finds its campaign over budget, and returns null — the 5% grant is never looked at. The booking pins at the standing 700 bps: `platform_fee_cents(10000,700)` = $7.00 instead of `platform_fee_cents _(audit-2026-09-08/promotions, 1/1 confirm.)_ | Iterate the eligible grants in preference order and take the first one whose budget increment succeeds, rather than committing to `limit 1`. The increment-is-the-check discipline is unchanged — it just gets applied per candidate — and the loop should stop at the first success so one booking can still only consume one g |
| low | reconcile | **Any Stripe API failure on the per-payment retrieve is filed as a CRITICAL `payment_intent_missing_at_stripe`, so a rotated key reads as hundreds of money discrepancies** — `index.ts:218`. STRIPE_SECRET_KEY is rotated in the Stripe Dashboard but not in Supabase secrets (or Stripe has a partial API outage). `checkWebhookConfig`'s `webhookEndpoints.list` throws and is swallowed at index.ts:110-113, then every `paymentIntents.retrieve` in the loop throws. The run writes one CRITICAL finding per payment in scope — up to 200 — each entity a real payment id, each detail claiming Stripe ha _(audit-2026-09-08/reconcile, 2/2 confirm.)_ | File `payment_intent_missing_at_stripe` only when the error is a genuine 404 (`(e as Stripe.errors.StripeError).code === 'resource_missing'` or `statusCode === 404`). For anything else, `warnings.push` the message and `continue` — that makes the run ERRORING on the registry row, which is both truthful and the signal co |
| low | reconcile | **The `no_stripe_key` finding can never auto-resolve, because "config" is excluded from the only list the resolver will close against** — `index.ts:92`. STRIPE_SECRET_KEY goes missing for one sweep (a secret not carried over on a redeploy). A CRITICAL finding 'STRIPE_SECRET_KEY is unset — reconciliation cannot run at all' is filed. The key is restored ten minutes later and every subsequent sweep runs cleanly and reports `discrepancies: 0` — but the finding stays open forever, because the only code path that could close it structurally cannot addre _(audit-2026-09-08/reconcile, 1/1 confirm.)_ | Seed `examined` with `"config"` at the top of the successful path (it is only used by the resolver, which compares text equality, not a uuid `in` filter), so a healthy run closes the config finding the same way it closes a payment one. |
| low | referrals | **A split referral credit permanently loses its clawback link, so a reversed referral is only partly voided — the rest stays spendable and the campaign is never refunded for it** — `supabase/migrations/20260906011000_split_credit_rows_collide_with_the_dedupe_index.sql:63`. Referrer R holds a $10 (1000c) referral bonus with source_booking_id = S (the referred earner's verified gig). R applies to an ordinary $50 flat gig: at 700 bps headroom is 150c, so consume_fee_credit SPLITS — parent stays payable at 850c carrying S, fragment 150c is applied carrying NULL. The poster declines the application (or either party cancels): trg_zz_release_benefits → release_booking_bene _(audit-2026-09-08/referrals-bonus, 2/2 confirm.)_ | Give a fragment durable provenance rather than relying on source_booking_id, which the dedupe index forces exactly one row to hold: add `root_bonus_id uuid references bonus_ledger(id)` (self-link, set on every fragment by both consume_fee_credit and return_unused_fee_credit), and have both void passes in vest_bonuses m |
| low | referrals | **An operator closing a chargeback's dispute row unfreezes the pending referral bonus and the next sweep PAYS it — and ctl_bonus_vesting_stalled's own remedy text tells them to do exactly that, claiming the sweep would void it** — `supabase/functions/stripe-webhook/index.ts:230`. The poster charges back the referred earner's verified gig S. recordReversal files a disputes row on S with status 'open' and leaves payments.refunded_cents at 0. The referrer's pending bonus is correctly held. Two days past vests_at, ctl_bonus_vesting_stalled fires with cause='held_by_dispute' and the remedy above. The operator does what it says — closes the dispute in /disputes with a note — but _(audit-2026-09-08/referrals-bonus, 1/1 confirm.)_ | Make the vest gate independent of dispute bookkeeping: add `and not exists (select 1 from refund_ledger rl join payments p on p.id = rl.payment_id where p.booking_id = b.source_booking_id)` to the pending→payable pass, or key both void passes on refund_ledger (which recordReversal's downstream record_refund writes with |
| low | referrals | **bonus_cash_payout_enabled is read by nothing, and a bonus_delivery='cash' campaign mints, charges the budget, vests to 'payable' and can never be delivered — confirmed still true against the live bodies, and never transcribed into OPEN_WORK.md** — `20260806250000_referral_once_per_person.sql:73`. A bonus campaign is created with bonus_delivery='cash' (the console's createPromotion never sets it, but clonePromotion spreads ...rest, and a service-role/SQL-editor insert is legal). Every referral then mints a delivery='cash' bonus_ledger row, charges promotions.spent_cents and redemptions_used, vests to 'payable' after 7 days, and is skipped by consume_fee_credit forever. admin/app/(console)/p _(audit-2026-09-08/referrals-bonus, 1/1 confirm.)_ | Fail closed at MINT: in accrue_referral_bonus, refuse (and charge nothing) when `promo.bonus_delivery = 'cash' and not coalesce((select enabled from app_flags where key='bonus_cash_payout_enabled'), false)`, or force delivery to 'credit' while the flag is off. Add a control for any bonus_ledger row with delivery <> 'cr |
| low | tax | **Platform income is filed by the date the work was marked done, rendered in UTC — not by the date the money was received, so a New Year's Eve gig and a January payout land in the wrong tax year** — `shared/taxFormat.js:107`. Two concrete misfilings. (a) An earner in Chicago finishes and both parties mark done a $180 New Year's Eve gig at 23:00 on 31 Dec 2026: `completed_at` = 2027-01-01T05:00:00Z, so `startsWith('2026')` is false and the $167.40 net is excluded from 2026 — while a $40 fuel receipt they log the same evening IS dated 2026-12-31 by `localDateISO`, so the deduction and the income it belongs to land in dif _(audit-2026-09-08/tax-center, 2/2 confirm.)_ | File platform income by the date it was RECEIVED (`payments.captured_at`), which is already the date shared/ledger.js:180 uses, and compare it in the reader's local zone rather than by UTC string prefix — reuse `localDateISO(new Date(capturedAt)).startsWith(String(year))` so the year test matches the one `expenses.date |
| low | tax | **A booking carrying a referral fee credit under-reports the earner's income, because the netting ignores the pinned fee_credit_cents** — `shared/taxFormat.js:109`. Ran the real functions: a $100 gig at 700 bps carrying a $5.00 referral fee credit. `earnerNetAfterCreditCents(10000, 700, 500)` = 9655 — the earner is actually paid $96.55 (the credit is spent down to the $3.45 Stripe-cost floor, per settleEscrow.ts's fee call). `earnerNetCents(10000, 700)` = 9300, so the Tax Center reports $93.00. Understated by $3.55 on that booking; the shortfall is bounded pe _(audit-2026-09-08/tax-center, 1/1 confirm.)_ | Subsumed by finding 1 — reading `payments.earner_amount_cents` gets the credit right by construction, because settleEscrow already applied it. If the pin-based path is kept as the pre-payments-row fallback, pass the fourth pin: `earnerNetAfterCreditCents(amountCents, feeBpsQuoted, feeCreditCents)`. Add a credited-booki |
| low | tips | **Reporting a problem silently throws away the tip the poster selected on the same sheet, with no error and no trace** — `src/components/CompletionModal.js:232`. Poster opens Verify & Rate, taps the $10 tip chip (the sheet now reads 'Charged to your saved card and sent to Sam'), then also ticks 'There was a problem — pay a reduced amount' at 75% and writes a reason. stripe-capture-payment returns {adjustment:'proposed'}, the client takes the early return, and the sheet closes on the toast 'Sent to the worker — they have 48 hours to reply.' The $10 tip is n _(audit-2026-09-08/tips, 2/2 confirm.)_ | Either (a) charge the tip on the proposed path too — move the `if (tipCents && tipCents >= 50)` block above the `adjustment === 'proposed'` early return in both src/context/JobsContext.js and web/lib/jobs.tsx, since stripe-tip already accepts a 'completed' booking and a tip is not contingent on the disputed gig amount; |
| low | webhook | **A charged-back TIP emails the operator that the gig booking is "flagged (auto-settlement suppressed)" when nothing was flagged and the escrow is still fully settleable** — `supabase/functions/stripe-webhook/index.ts:108`. A poster tips $20 on booking B, then disputes that tip charge with their bank. `charge.dispute.created` arrives, `recordReversal` reverses the tip in `tip_ledger` and returns B, and the admin is emailed "Chargeback opened: usd 20.00 / Booking: B / The booking is flagged (auto-settlement suppressed); review and respond in Stripe." No `disputes` row exists for B. If B's escrow is still `authorized`  _(audit-2026-09-08/webhook, 2/2 confirm.)_ | Make `recordReversal` return which of the two things it did rather than a bare booking id — e.g. `{ bookingId, filed: boolean, kind: 'tip' / 'escrow' }` — and have both email callers render the suppression sentence only when a `disputes` row was actually written. For the tip branch the correct copy names `tip_ledger.re |

> **2026-09-08 — a working payments audit added 71 rows here and closed 10.**
> 24 read-only finders swept the money surface; every finding was attacked by independent
> refuters (3 lenses for high/critical, 2 for medium, 1 for low) and more than a third
> died there. 49 refuters were themselves killed by a usage limit mid-run, and because the
> harness drops a finding whose verifiers all fail, 22 findings were silently filed as
> "refuted" without ever being read — those were recovered from the run journal and
> re-verified in a second pass (15 confirmed, 2 split, 3 refuted). Every row below carries
> the dimension it came from and its vote, so its evidence is recoverable.
>
> **Nothing in the Closed block is deployed.** Two migrations need `supabase db push
> --linked`, six edge functions need deploying by hand, the console needs
> `cd admin && npx vercel --prod --scope go-hustlr`, web ships on push and mobile is an
> OTA. The gate is green (158 suites / 2600 tests) and every fix was demonstrated failing
> on the pre-fix file and passing after.
>
> **What this audit could NOT exercise, and why:** no end-to-end money actually moved.
> The harness blocked staging bookings and payments in production, so the escrow
> lifecycle, real partial captures, refunds, chargebacks, tips and the concurrent/double-
> submit cases were verified by reading code and live schema plus rolled-back probes, not
> by moving money. The earner's Connect onboarding was left `state: incomplete`. Treat
> every row below as reproducing in code, not as reproduced against a live charge.

## Closed (251)

| Sev | Area | Finding | Closed by |
|---|---|---|---|
| high | disputes | **A booking could carry two LIVE adjustment proposals.** `stripe-capture-payment`'s "idempotent per booking" guard was read-then-decide with no unique index behind it, and its `.maybeSingle()` ERRORED (returning `{data:null, error}`, with only `data` read) the moment a booking legitimately held two dispute rows — a NO_ROOM_TO_HOLD record or a `recordReversal` row. Two taps armed two clocks in `dispute_set_defaults` and fired `dispute_notify_respondent` twice, so the earner got two deadlines and two percentages for one gig, and `settle-disputes` stamped the second row with whatever the first collected. _(audit-2026-09-08/races+dispute-sql.)_ | `disputes_one_live_proposal_per_booking`, a PARTIAL unique index on `pct_paid is null and proposed_pct is not null` — so settled rows and reversal records still coexist with a live proposal (20260909060000). The function's read is scoped the same way and 23505 now means "already proposed", never a 500. `disputeTwoParty.test.js`, proved to discriminate. |
| high | disputes | **A second "report a problem" silently kept the FIRST percentage while both clients printed the new one.** The short-circuit returned a bare success, so the toast narrated the pct the poster had just typed while the row and the earner's notice kept the original. Reopening the sheet is one tap on web — `/hiring` re-offers "Verify & rate" on any `completed` booking with no dispute lookup, and a proposal deliberately leaves the booking `completed`. Propose 50% Tuesday, soften to 90% Wednesday: the poster is told 90%, the earner is paid 50%. _(audit-2026-09-08/races, re-verified 2/2.)_ | Every "proposed" response now carries `proposedPct` — the RECORDED figure — and both clients render it plus the server's real `settleAfter` instead of a hardcoded 48 hours. A DIFFERENT percentage is refused with `ADJUSTMENT_ALREADY_PROPOSED` (409) naming what is on the record. |
| high | disputes | **`stripe-capture-payment` stamped `pct_paid = 100` BEFORE the capture on NO_ROOM_TO_HOLD.** That branch is entered only inside the hold's last 36 hours — exactly when `settleEscrow` refuses with HOLD_EXPIRED or EARNER_PAYOUTS_DISABLED. Zero cents moved and the row permanently claimed the earner was paid in full, and it BLINDED the critical control: `dispute_settlement_pct` opens with `if d.pct_paid is not null then return null`, which `ctl_dispute_settlement_overdue` reads through. _(audit-2026-09-08/money-controls, re-verified 2/2.)_ | The insert now carries the poster's reason and photos and nothing else — not even `proposed_pct`, which would arm a clock and notify the earner about a reduction the branch is abandoning. `pct_paid`/`settled_at`/`resolved_at`/`status`/`resolution_note`/`proposed_pct` are stamped only after `settled.ok`, conditional on `.is('pct_paid', null)`. |
| high | disputes | **An earner who ACCEPTED a reduction was not paid any sooner than one who ignored it.** `respond_to_dispute` writes `settle_after = now()` on accept — "settles at the next sweep rather than waiting out the clock" — and `guard_disputes_write` pinned the column unconditionally four lines ABOVE its `if not responding` block, silently reverting it. Both parties were told "it will be paid shortly". Silent because the guard pins rather than raises: every other column the screen re-reads did land. _(audit-2026-09-08/dispute-sql, 2/2 confirm.)_ | The exemption is now directional — while responding, `settle_after` may move EARLIER only, never later, or a respondent could extend their own deadline until Stripe voided the hold and nobody was paid. `ctl_dispute_accept_not_expedited` (high) watches it against data (20260909070000). |
| high | admin-money | **The console's "Settle & pay earner" captured the FULL hold on a booking with a live adjustment, and "Release hold" voided the authorization the settler needs.** Both were gated on payment status alone — exactly the state a proposal leaves (`authorized`/`completed`) — on exactly the page an operator opens because of a dispute, with Intervene rendered ABOVE the dispute file. On a $200 gig proposed at 60% and ACCEPTED, one click charged $200 and paid $186 into a Connect account, then the sweep told the poster their report "did not stand". Release was the mirror: `HOLD_EXPIRED` for ever. _(audit-2026-09-08/admin-payment-action+stepup-and-console, 3/3.)_ | Both ops now read `disputes` for a live adjustment and refuse with `dispute_open` (409) naming `/disputes/[id]`, failing CLOSED on an unreadable read. The console's two hold buttons are gated on the same fact and say why. `disputeTwoParty.test.js`. |
| high | disputes | **One poisoned dispute starved every other settlement.** Every failure inside `settleEscrow` is a returned `{ok:false}` the loop handles — except the two `stripe.paymentIntents.capture` calls, which THROW. A throw unwound past the loop to the single outer catch, and the listing is oldest-first, so one stuck row (a voided PaymentIntent whose payments row still read `authorized`) aborted the whole batch deterministically, every hour, for ever. This is the only thing that pays a held gig. The outer catch logged with an EMPTY context, so `/errors` named neither the dispute nor the booking. _(audit-2026-09-08/capture+settle-disputes-fn, 3/3.)_ | Per-dispute `try/catch`: `failed.push(d.id)`, `logServerError` with `{dispute_id, booking_id}` and `fatal: true`, `continue`. The outer catch stays for genuinely global failures. |
| high | tax | **Tax Center income was the AUTHORIZED amount, not what was settled.** `platformIncomeForYear` read only `bookings.amount_cents_quoted` and never the `payments` row, so a gig settled at 50% after a dispute and a gig captured then fully refunded both stayed `verified` carrying the full pin and were declared in full. On a $200 disputed gig the earner was told $186 when $93 reached their bank — while `/profile/transactions` showed $93.00 for the same booking on the same day — and Export wrote $186.00 onto the CSV they hand an accountant. _(audit-2026-09-08/tax-center, 3/3.)_ | Valued from the LEDGER ENTRY (`netCents − tipCents` on a settled entry), so the Tax Center and Transactions cannot disagree by construction; falls back to the pin only when no entry exists. Both clients now load `fetchLedger` in the same `try` as the books, so a failed read is an error card rather than a confident wrong total. |
| high | disclosure | **Two false promises about dispute branch 4, on the surfaces that persist.** `respond_to_dispute` wrote the POSTER a durable inbox row reading "Nothing is paid until we do", and Hustlr AI told BOTH clients "disputing it means nobody is paid until a person at GoHustlr has read both sides". Branch 4 captures 100% once the hold nears expiry with nobody having adjudicated — by design. Both dispute SCREENS had been corrected for this; the server notification and the assistant prompt were missed, and the assistant is the surface a worried user actually asks. _(audit-2026-09-08/display-truth+mine, 3/3.)_ | Both rewritten to name the auto-capture. The assistant's hardcoded "48 hours" also became "up to 48 hours — less when the card hold is close to expiring". A new `parity.test.js` block fails if either prompt re-promises an unconditional review; the `MUST_KNOW` regex was moved off the flat number onto the destination. |
| medium | admin-money | **`adminAuth` knew two of the four admin tiers, and tested them with `minRole === 'admin'`** — so every other `minRole` value degraded to "any active membership passes", and a real `finance` operator was refused by anything asking for `'admin'`. Live effect: `admin-payment-action` required `'admin'` while its console half runs `requireFreshAdmin("finance")`, so release-hold, settle, refund and record-reversal were all unreachable for the tier created to perform them — after `run()` had already written the `admin_audit_log` row. Latent only because both `admin_users` rows are `admin`. Supersedes OPEN_WORK's earlier low row on the same subject. _(audit-2026-09-08/idor, confirmed mechanism.)_ | Four-tier `AdminRole` + a `SATISFIES` table byte-mirroring `admin/lib/guard.ts`, `roleSatisfies(role, minRole)` as the gate, and `admin-payment-action` moved to `'finance'`. A third block in `adminTierParity.test.js` pins a console action's tier to the edge function's — a direction the existing two blocks are structurally blind to. |
| medium | disputes | **`settle-disputes` called `recompute_user_rating({ p_user })` against `recompute_user_rating(target uuid)`.** PostgREST resolves an RPC by name AND argument names, so it answered "function does not exist"; supabase-js RESOLVES with that error rather than throwing, and the result was discarded, so the surrounding `try/catch` never saw it and nothing reached `/errors`. Every settled dispute published the poster's held review onto the earner's public profile while `profiles.rating` and `review_count` never moved. `deno check` cannot see it — the argument object is untyped. _(audit-2026-09-08/money-guards-sql, confirmed against live pg_proc.)_ | Corrected to `target`, with the error checked and logged. `__tests__/rpcParamNames.test.js` parses every `create function` in `supabase/migrations/` and the legacy `.sql` files and checks all 74 statically-resolvable `.rpc()` call sites against them; it names the offending file and line. |
| low | admin/access | revokeEmail deleted with `ilike` on the raw address, so `_` and `%` in an email acted as wildcards and one revoke could delete other testers' invites while reporting success | escaped LIKE metacharacters, ilike demoted to a candidate READ, exact case-folded match narrowed in JS, DELETE via .in() on literal addresses, row count in the message; guarded by __tests__/betaRevokeExactMatch.test.js — cc60c36 |
| low | audit-2026-09-05 | **`grantToUsers` resolved a recipient list by username OR by email but never both, and reported the entries it never looked up as 'already holding it'** — `admin/app/(console)/pricing/actions.ts:114`. The box is labelled 'Emails or usernames'; the action matched the whole list against `profiles.username` and reached for auth emails only inside `if (!ids.length)`, so one username entry dropped every email entry. The message `Granted to ${n} of ${emails.length}. Anyone already holding it was skipped.` then blamed the shortfall on `grant_promotion_to_users`'s on-conflict skip. The email fallback was also a single `listUsers({ perPage: 1000 })` page, so it would have stopped resolving anyone past the first thousand accounts. | FIXED — commit 4550d55. Each entry is resolved on its own via `admin/lib/recipients.ts` (`parseRecipients` / `resolveRecipients` / `grantSummary`, pure and unit-tested); the email half goes through the new batch RPC `admin_find_user_ids` (`supabase/migrations/20260906092000_resolve_every_grant_recipient_not_just_the_first_kind.sql`), which returns one row per input with a null id on a miss so unmatched entries can be NAMED — service_role only, asserted by the migration's own DO block. The message keeps granted / already-held / matched-nobody apart, a failed username query now surfaces instead of being swallowed, and the unmatched entries are written into the audit row. Discriminated in `__tests__/grantRecipientResolution.test.js`: old algorithm 1 of 10, new 10 of 10, on the same list and maps. |
| low | admin-console | **/flags rendered ten app_flags rows of three different kinds as one list of kill switches: muting `safety_alert` told the operator users now saw a "temporarily paused" message (nothing changed for users — it stopped paging a human for 24h, the July 10 outage class) and never mentioned the mute self-expires, while `stripe_mode.value` could not be edited at all despite 20260814080000 saying /flags flips it at cutover** — `admin/app/(console)/flags/page.tsx:75`. | FIXED fa2b836. `admin/app/(console)/flags/guide.ts` classifies every seeded key (kill_switch / alert_channel / config) with its enforcement point and true off-effect; the page groups by kind, renders the "where each flag is enforced" list FROM the guide (it named 5 of 12 keys), shows `disabled_until`/`disabled_reason`, and drops `bonus_cash_payout_enabled` — seeded OFF by design — from the paused banner. Muting a pager now requires a written reason (writing the `disabled_reason` column 20260814100000 added and nothing wrote); config rows get no toggle and `setFlag` refuses one; `setStripeMode` writes `value` with typed confirmation + step-up. `__tests__/flagsConsoleDescribesEveryKey.test.js` reads the seeds off disk and fails if a new flag arrives undescribed (7/12 cases fail on the old console code). Console only, no migration. |
| low | admin/access | setOpenBeta (the `*` open-beta row) ran on plain requireAdmin with no step-up, unlike every /flags kill switch — a borrowed AAL2 session inside the 12h cap could make signups public with no second factor | access/actions.ts adminCtx() → requireFreshAdmin("admin") for all three write actions; stale_mfa returned as the sentinel; InviteForm/RevokeButton/OpenBetaControl wired through useStepUp + ReauthPrompt; guarded by stepUpCoverage.test.js — 3a5c52c |
| low | ops / app_flags kill switches | A feature kill switch left OFF (payments_enabled, posting_enabled, signups_enabled, tips_enabled, assistant_enabled, promotions_enabled) cost no line in the digest and no finding, unlike a disabled control or a muted alert flag — a pause taken during an incident was visible only as a red pill on /flags while /controls, the hourly page and the daily digest all read clean | ctl_feature_flag_off registered (medium, lifecycle): reports every off flag with hours_off, never re-enables anything, watches every app_flags row rather than today's six, excusing only the two alert channels (covered by ctl_alert_not_dispatching) and rows carrying value.off_is_normal — set here on bonus_cash_payout_enabled. Migration 20260906085000_a_paused_kill_switch_had_no_watcher.sql with a rolled-back probe; drift guard __tests__/featureFlagOff.test.js; CLAUDE.md recount 70/68. Commit 3087a33 |
| low | controls / payments | **CLOSED 2026-09-06.** No control asserted a captured payment's `fee_cents` against its pinned `fee_bps`. `ctl_payment_ledger_impossible` (20260806040000:73) reads `payments.fee_cents` only as an ADDEND — captured <= authorized, refunded <= captured, signs — and every one of those tests is satisfied by a fee that is simply the wrong number, because the earner's side absorbs the difference and the two halves still sum to what Stripe collected. Across the whole migration tree, `fee_bps` appeared only in the discount, tier and promotion controls, all of which read bookings/fee_tiers/promo grants, never the captured split. Both fee bugs this platform has shipped were therefore invisible: a NULL rate resolving to a free gig (fee_cents 0 on a real capture), and the fee x pct^2 partial-capture retry the capture code's own comments warn about (a $200 gig at 700 bps settled at 50% recording 350c instead of 700c — above the 345c Stripe floor, so arithmetically possible). | `ctl_captured_fee_off_pin` (20260906090000, severity high / money) recomputes the fee from the pinned inputs through `platform_fee_after_credit` / `platform_fee_cents` and fires outside a BAND rather than on an equality, because the four live paths do not agree to the cent: the band spans stripe-capture-payment's derivation (subtracts the poster discount; re-floors a partial at the CAPTURED amount) and earner-claim-payment / admin-payment-action's recompute fallback (does not subtract it). A floor-based lower bound — the shape first proposed — was rejected: it misses the pct^2 scenario (350c clears 345c) and cries wolf on every discounted booking, since the discount is funded out of the fee AFTER the floor. Probed rolled-back on staged rows (correct full + correct partial silent; zero fee and 350c each fire while ledger_impossible sees nothing; doubled fee caught as fee_above_pin; discounted rows silent under both derivations; correction auto-resolves). Guarded by `__tests__/capturedFeeOffPin.test.js`, proved to report NONE on the tree as it stood. Commit c1230a9. |
| low | ops / controls | ctl_bonus_vesting_stalled reported a bonus deliberately held by an open dispute as 'the sweep is not running or is erroring', sending the operator to a healthy pg_cron job for up to five days before ctl_dispute_open_beyond_sla named the real cause | The control now joins the holding dispute (LEFT JOIN LATERAL … LIMIT 1, so one bonus is still one finding) and its detail carries cause=sweep_not_vesting/held_by_dispute plus the dispute's id, status, age and the matching remedy; the registry why describes both causes. Migration 20260906083000_bonus_vesting_control_knows_disputes.sql (rolled-back probe reads the hold off the live vest_bonuses, then proves both causes on the same staged row), guarded by __tests__/bonusVestingControl.test.js. Commit 0574b4c |
| low | ops / suspension | No canary control detected a suspended account still writing — the exact silent weakening CLAUDE.md warns a `migration_fix_lifecycle.sql` re-run causes ('neither failure errors; the policy just gets weaker'). Suspension rests on three one-line write-time clauses (messages_insert, guard_booking_not_suspended, reviews_insert_auth) and nothing asserted the outcome against data; `grep -rn is_suspended supabase/migrations/*.sql \| grep -i ctl_` returned nothing. | FIXED 2026-09-06 — `20260906084000_no_canary_for_a_suspended_account_still_writing.sql` adds `ctl_suspended_user_active` (registry `suspended_user_active`, high/security): any message, booking or review whose `created_at` is after its author's `suspended_at`, 60s grace for console-vs-database clock skew and commit ordering (deliberately NOT the ~1h token, which none of the three checks consults). No jobs arm — `jobs_insert_auth` has no suspension guard to lose, so it would be a standing false positive. Rolled-back probe shows the same three rows silent before the suspension and reported after it, ten seconds absorbed and ninety reported, and the live policy still refusing the write with 42501. `__tests__/suspendedUserActive.test.js` guards the shape. Commit e77c923. |
| low | messages / conversation_state | Unread math compared a DEVICE-clock last_read_at against server-clock messages.created_at; conversation_state had no trigger at all, so a fast phone marked replies read before they were written (no dot, no badge) and a slow one left read threads badged | trg_guard_conversation_state_read_at takes now() for any write that changes the receipt, leaving archive-only upserts and receipt-less rows untouched; future receipts backfilled; guarded by __tests__/conversationReadClamp.test.js (20260906094000, f4b15bc) |
| low | NotificationSettingsScreen / notifications.js | **A failed preferences read renders defaults, and the next toggle upserts all eight defaults over the user's saved notification preferences** — `src/lib/notifications.js:82`. Confirmed: the read discarded the PostgREST error, so a transient failure was indistinguishable from "no row yet", and `saveNotificationPrefs` wrote the whole in-memory row back. | Read fails closed (error surfaced, both clients show a retry card and no switches); whole-object writer replaced by `saveNotificationPref(key, value)` — one-column UPDATE, defaults INSERT only when no row exists, 23505 retry. Web mirror fixed in step. `__tests__/notificationPrefsFailClosed.test.js` (11/13 fail on the old code). Commit 61f158a. |
| low | audit-2026-09-05 | **Security and MFA-challenge screens assumed exactly one TOTP factor: 'Turn it off' removed only factors[0] and reported 2FA off while it stayed on; the challenge picked whichever factor GoTrue listed first** — `src/screens/SecurityScreen.js:133`. | 2026-09-06 (519523c) — `factorLabel`/`factorOrigin`/`preferredFactor` in `src/lib/mfa.js`, mirrored in `web/lib/mfa.ts`. Both Security surfaces list every verified factor by name/origin/enrolment date, let you choose which to remove when there are two, and derive the toast from the RELOADED status ("X removed — two-factor is still ON, N others can sign you in" vs "password-only"); both challenge screens prefer the app's own entry, name it in the copy, and mention a second entry only when one exists. The verifier's correction stands and is fixed here too: `web/app/(app)/settings/security/page.tsx` carried the identical `status.factors[0]` disable despite already mapping over `status.factors` for display, and `web/app/mfa/page.tsx` had the same list-order pick. The naming lookup on both challenge screens is presentational and returns on error, so the authoritative fail-closed `listFactors` check is untouched. 17 new assertions in `__tests__/mfa.test.js`, all 17 failing against the stashed originals. CLAUDE.md's Two-factor section records the mechanism. JS-only — ships over the air (mobile) / on the next push to master (web). |
| low | controls / incentives | money-db-incentives#10 — an earner's fee credit consumed at APPLY sat 'applied' on a pending application with no control watching it; ctl_benefit_held_by_pending covered promo_redemptions only, and ctl_credit_stranded_on_dead_booking requires a declined/cancelled booking, which is the moment the credit is released anyway | Second arm on ctl_benefit_held_by_pending over bonus_ledger (applied + booking pending + 48h), namespaced 'credit:<booking>', grouped per booking, carrying later_applications_without_credit; same registry key so the control count is unchanged — 20260906080000_a_held_fee_credit_is_invisible_while_the_application_is_live.sql, commit 3ceb811 |
| low | admin/promotions | **The `/promotions` cost preview priced every benefit against the FOUNDING rate, overstating fee-override and poster-discount burn ~2x at the live 700 bps.** `estimate_campaign_cost` (20260806170000:186-202) passed the literal `1000` to `platform_fee_cents` and `poster_discount_headroom` in four places — twice in `per_use_cents`, twice again in `max_total_cents` — while its own comment claimed it "uses the real fee functions so the estimate cannot drift from the charge". On the $50 default gig a 0% fee_override previewed at 300c/use against a real 150c, and a $5 poster discount previewed at the 300c of headroom that exists at 10% against the 150c that exists at 7%. The ratio is not fixed: the processing floor is a constant the rate change does not move, so the overstatement grows on smaller gigs and converges toward 10/7 on larger ones. An operator sizing `budget_cents`/`max_redemptions` from it plans a $250 budget as ~83 redemptions when it funds ~166; a rate RISE inverts the sign and exhausts the budget early. Third sighting of the same literal — the charge path was corrected off it twice (20260806220000 "THE BUDGET COUNTERFACTUAL HARDCODED 1000 bps", 20260813140000) and the preview, written between them, was never revisited because it is not on the charge path. | 2026-09-06 (20260906075000, commit 2a4ea1e) — all four literals now read `fee_bps_at(now())`, the standing card, which is the right moment for a campaign that has not run yet. It stays an UPPER BOUND for `fee_override`: since 20260814120000 the charge is measured against the booking's pinned baseline, `least(standing, tier_fee_bps(earner))`, and a per-campaign preview has no earner to resolve a tier for — so the returned `note` the console renders now names the rate it priced at and says so. Probe (rolled back): stages 700 bps, asserts the preview equals `promo_benefit_cents` and that the old 1000-bps arithmetic does NOT, so it cannot pass vacuously; repeats at 1500 bps where the old literal understated the burn; checks `max_total_cents` scales the corrected per-use figure and that the rate-independent bonus arm is untouched. Guard: `__tests__/campaignCostPreviewRate.test.js` over all three benefit-costing functions (`estimate_campaign_cost`, `promo_benefit_cents`, `capped_override_bps`), 3 of 9 cases failing on the pre-fix body and naming the offending call. |
| low | incentives / vest_bonuses | money-db-pinning#5 — both void passes in vest_bonuses returned every voided bonus's budget (`sum(amount_cents)`) but only ever ONE campaign seat (`redemptions_used - 1`), because the literal is applied once per promotion by the UPDATE ... FROM join while accrue_referral_bonus charges one use per minted bonus; N reversals in one hourly sweep left N-1 phantom redemptions counted against max_redemptions for the life of the campaign, so it silently refuses genuine referrals it still has budget for | Fixed: 20260906073000_vest_bonuses_returns_every_redemption.sql aggregates `count(*)` alongside the cents in both CTEs and subtracts it, with a rolled-back probe voiding two bonuses per pass and asserting the seat count falls by two and ctl_redemption_double_charge goes silent; static guard __tests__/vestBonusSeatReturn.test.js (fails 3/7 on the old body). Commit f2e7219 |
| low | audit-2026-09-05 | **settle_booking_benefits charged a poster_discount campaign the full pinned discount on a partial capture** — `supabase/migrations/20260813140000_settle_benefits_at_booking_rate.sql:89-96`. The poster_discount branch read `payments.poster_discount_cents` and never looked at `p_amount_cents`, the scaled gig value stripe-capture-payment computes precisely because a partial capture delivers only `pct` of the discount. $100 gig at 700 bps settled at 50%: the poster received 178c of the 355c discount and the campaign was charged 355c, delta 0. A $50 budget exhausts after ~14 such gigs having delivered ~$25 while /promotions shows the bar full. ctl_promo_spend_understated tests only `delivered > spent` and is blind in this direction. | FIXED 2026-09-06 (`20260906074000_a_partial_capture_charged_the_campaign_the_whole_discount.sql`, commit 1a1c664): the branch now scales the pinned discount by `p_amount_cents / (amount_cents + poster_discount_cents)` — both pinned inputs capture itself derives from, so it stays idempotent under retry — clamped to the pinned discount. A full capture leaves the number unchanged, so earner-claim-payment and admin-payment-action's settle op are unaffected. Rolled-back probe stages the $100 gig, settles at 5001 and asserts benefit_cents = 178 with spent_cents relaxed by 177; it raises on the old body's 355. Adds and registers `ctl_discount_settled_above_delivered`, which re-derives the delivered discount from the ledger and reports the already-over-charged rows (silent on the correct row, fires when it is set back to 355). `__tests__/discountSettledAtDelivered.test.js` fails 3 assertions against the pre-fix branch text. CLAUDE.md control count 69/67 → 70/68. |
| low | stripe-cancel-payment | money-edge-escrow#4 — the only escrow function with no logServerError: bare console.error terminal catch, unchecked payments update after the Stripe cancel, and both clients swallow the 500, so a failed hold release left the row on 'authorized' with no cause anywhere | terminal catch logs fatal with booking/intent/Stripe code; already-void intent reconciled, already-captured probed + refused; ledger write checked and ledger_desync logged; stripe-tip's identical bare catch fixed alongside; guarded by __tests__/edgeErrorSinkCoverage.test.js — 616b8a5 |
| low | earner-claim-payment | Capture error swallowed by `catch (_capErr) {}`; a zero-capture claim returned an unlogged 502 'try again', so a dead or unconfirmed hold sent the earner into a retry loop that never reached /errors | Catch keeps the error; the zero-capture branch logs to `client_errors` with the Stripe code, booking/payment/intent ids and `pi.status` (fatal when canceled/requires_payment_method) and answers HOLD_EXPIRED / HOLD_NOT_AUTHORIZED instead of a generic retry — 7dccb2c |
| low | _shared/payoutCapable | **A failed `stripe_accounts` lookup was reported as a definite "no payout account", so a transient DB error refused a capture with a message blaming the earner** — `supabase/functions/_shared/payoutCapable.ts:57-64` destructured only `data`; on a PostgREST error `acct` was null, `accountId` undefined, and the function returned `{ capable: false }` with no `unverifiable`. Only the Stripe leg set it. Both settle callers then took the hard-refusal branch: `stripe-capture-payment` answers EARNER_PAYOUTS_DISABLED "The earner's payout account is no longer active. They need to re-verify it" and `earner-claim-payment` "Your payout account is not active. Re-verify it, then claim again" — for an earner whose account is fine. Recoverable on retry, but it sends both parties to re-verify nothing, which is the "our failure, their cost" class the helper's own header says it exists to avoid. | 2026-09-06 (commit 4244a67) — the lookup's `error` is destructured and short-circuits to `{ capable: false, unverifiable: true }` ahead of the missing-account branch, so the callers log and proceed to the capture (itself the real check). No error plus no row is untouched: still a definite no. `__tests__/payoutCapability.test.js` gains a case pinning the error read and its position ahead of the refusal; HEAD's helper under it fails 1 of 10. |
| low | realtime / web notifications | notifications was never added to the supabase_realtime publication, so both web postgres_changes subscriptions on it (AppShell alerts badge, /notifications inbox) could never receive an event from the tracked schema | migration 20260906110000 adds the table and asserts membership, RLS and a rolled-back probe; __tests__/realtimePublication.test.js replays every publication statement and fails on any postgres_changes subscriber whose table is unpublished — c552683 |
| low | admin console / team + MFA | Restore (disabled → active) stamped factors_confirmed_at without the confirmation dialog Activate carries, so a factor enrolled while an account sat revoked — /mfa enrols for whoever holds the password, a disabled row grants nothing, and a reset does not demote it — was vouched for by a click that asked nobody, and ctl_admin_unconfirmed_factor stayed silent | setTeamStatus stamps only on pending→active and leaves the column alone on restore; Restore's dialog names the factor count and enrolment time and says it vouches for nothing; trg_admin_restore_never_vouches (20260906070100) pins the stamp across disabled→active at the data layer, with a rolled-back probe proving both arms — 7263993 |
| low | admin console / auth | /denied revealed the caller's own admin_users status (pending / disabled / role-too-low) to a password-only aal1 session — proxy.ts whitelists the route, and situation() never checked the assurance level its own header claimed, so a phished password was a free oracle for "is this credential a live console member" | situation() now requires aal2 via guard.ts's exported aalFromToken, checked before the admin_users lookup, and falls to the generic copy otherwise; four assertions in adminTeamMfaSource.test.js — 91a0459 |
| low | admin/guard | the 12h session cap surfaced as "Not authorized." (or the raw string "stale_mfa") from every plain-requireAdmin action, with no re-verification path | denyResult() in guard.ts is the single mapping and all 13 actions.ts use it; 10 client components wired to useStepUp + ReauthPrompt; ReauthPrompt copy covers both clocks; stepUpCoverage.test.js now enumerates (console) in both directions and caught 4 further dead ends in PricingControls — 97b87e5 |
| low | supabase/functions dependency pinning | **All 32 edge functions imported supabase-js and stripe on major-only specifiers (`npm:@supabase/supabase-js@2`, `npm:stripe@22`) and `deno.lock` is gitignored, so each hand-deploy silently resolved a different library version** — 51 specifiers, zero of them reproducible. Functions ship one at a time on the day each is touched, so the fleet ran whatever minor was newest at each deploy; `deno check --node-modules-dir=none` resolves through Deno's GLOBAL cache, so it type-checked the developer's machine rather than production. Measured 2026-09-06: `@2` resolved to 2.112.3 while the app lockfile held 2.108.1 and the console's 2.110.0. `stripeApiVersion.test.js` does not cover this — it pins the wire API version and asserts only a stripe MAJOR, so the library under it drifted the same way. | 2026-09-06 (commit 5c7683e) — every import site pinned to `npm:@supabase/supabase-js@2.112.3` / `npm:stripe@22.5.0` (the latter matching admin/package.json's existing exact pin), i.e. what a deploy today already resolves, so the next redeploy of each function converges rather than moves. New guard `__tests__/edgeDepsPinned.test.js` fails on any `npm:`/`jsr:` specifier without a full x.y.z, on two sites disagreeing about one package, and on a raw `https://` module import; unpinning one file fails 2 of its 4 tests. CLAUDE.md's edge-function section and the Definition-of-done table record the rule. `deno check` exit 0 on all 32. |
| low | admin deploy runbook | **DEPLOY.md and admin/README.md gave an admin-console deploy command that cannot work as written** — the console has no git integration, so the command in the doc IS the deploy mechanism. Ten places (DEPLOY.md:15, admin/README.md:78, ADMIN_AUDIT_2026-08-04.md, five 2026-08-12 audit runbooks) lacked `--scope go-hustlr`, whose absence answers a flat "Not authorized"/deploy_failed that reads like an expired login. admin/README.md additionally instructed the repo-root run — true of the project's Root Directory, irrelevant to the CLI, which resolves its project from the local `.vercel/` link, and the repo root's is `gohustle`, the WEBSITE: run as documented it deploys the wrong project and prints a success URL. | 2026-09-06 — both halves added everywhere with the reason inline; CLAUDE.md's warning now names the `cd admin` trap it never explained. `__tests__/adminDeployCommand.test.js` scans every tracked .md/.html/.githooks file and holds each console deploy line to the pre-push hook's version; reverting the two files fails 2 of its 4 tests naming both lines. Commit 4476c09. |
| low | docs / agent instructions | **AGENTS.md pointed every session at the Expo SDK 54 docs while the app runs SDK 55** — its third line is the whole file's purpose (`https://docs.expo.dev/versions/v54.0.0/`) and package.json pins expo ~55.0.29 / RN 0.83.10 / React 19.2.0. Code written from the v54 reference type-checks, bundles and misbehaves only on device — the class of regression 54→55 already caused here once (react-native-screens' full-screen back-gesture default, App.js DETAIL_OPTS). CLAUDE.md carried the same staleness in four places ('Expo SDK 54, RN 0.81.5, React 19.1.0', 'auto-picks SDK 54 version', 'Expo Go on SDK 54', and the AGENTS.md row), README's tech-stack table in three, and metro.config.js's blockList comment put the app on React 19.1.0. | 2026-09-06 — pointed AGENTS.md at v55.0.0 and corrected every stale copy, then removed prose from the loop: `__tests__/agentsSdkPointer.test.js` derives the SDK major, RN and React versions from package.json/admin/package.json and asserts the docs URL, CLAUDE.md's version sentence, its present-tense `Expo SDK n` / `Expo Go on SDK n` / `auto-picks SDK n` shapes and metro.config.js's two React numbers against them, so the next SDK move fails the gate instead of misinforming sessions. The sweep is deliberately narrow — historical statements ("SDK 57's CLI fixed the devicectl warning", TESTFLIGHT's 54→55 note) are true and survive. 5 of the 8 new assertions fail against the pre-fix files. Gate: 132 suites, 2081 tests. Commit 3c7a5b0. |
| low | definer-function grants (anon surface) | `resolve_category_slug(text)` (and, found while verifying, the 4-arg `capped_override_bps`) were created after the 2026-07-26 anon sweep with a grant-only line, so Supabase's default EXECUTE to `anon` and Postgres's to PUBLIC were never revoked — both callable with the embedded anon key, and nothing guarded the invariant | `20260906081000_two_definer_rpcs_added_after_the_sweep_kept_anons_execute.sql` revokes both from `public, anon`, re-sweeps the definer surface (view_gig_share excluded, its anon grant is deliberate) and asserts against `pg_proc`; permanent guard `__tests__/definerAnonExecute.test.js` replays the privilege history and fails naming any definer function left anon-executable; `scripts/gen-categories-migration.js` template fixed so a regeneration cannot reopen it — 76b61ea |
| low | definer-function grants | **`tier_fee_bps(uuid)`, `earner_completed_count(uuid)` and `earner_distinct_posters(uuid)` were SECURITY DEFINER RPCs granted to `authenticated` with a caller-controlled uid and zero client callers — any signed-in user could read any other earner's loyalty fee rate, verified-gig count and distinct-counterparty count.** All three run as owner over `bookings`, `jobs` and `fee_tiers` and take their subject from `p_user`, not `auth.uid()` (20260806120000:58-71, 20260817040000:48-83). PostgREST exposes each at `/rest/v1/rpc/<name>`, and an earner's uuid is on every job card and review, so one POST returned that earner's exact pinned platform fee bps — a private commercial term they see only on their own quote screen — plus their verified-gig count (no public column carries it; `review_count` counts reviews, not work) and how many separate clients they have worked for, which is also the anti-collusion threshold in `fee_tiers.min_distinct_posters` and therefore told a farmer how far they were from a rung. Enumerable, unlogged, no consent. The repo had already written the rule down for this shape: 20260710030000 keeps `is_blocked_pair` in the `private` schema because a public one "would be a boolean ORACLE over any two users". | 2026-09-06 (commit f58f6d4) — `20260906082000_loyalty_tier_rpcs_were_an_oracle_over_every_earner.sql` revokes EXECUTE from public/anon/authenticated on all three and grants only `service_role`. Free to do: nothing under `src/`, `web/`, `shared/`, `admin/` or `supabase/functions/` calls them, the only in-tree caller is `pin_booking_amount` (20260806320000:169) which is itself SECURITY DEFINER and so runs with its owner's privileges, and clients quote from `fee_bps_at()`, which takes no uid. The DO block asserts the revoke, then re-grants `tier_fee_bps` to `authenticated` inside a caught subtransaction to show the assertion flips (and rolls it back), then asserts the mechanism the fix rests on — `pin_booking_amount` still `prosecdef`, sharing a `proowner` with `tier_fee_bps`, that owner retaining EXECUTE. New guard `__tests__/definerUidGrants.test.js` replays every grant/revoke across the migrations in apply order and fails on any public SECURITY DEFINER function with a uuid argument left executable by `authenticated` unless excused with a reason (`profile_availability`, `recompute_user_rating`, `create_gig_share`, `raise_gig_emergency` — each resolves the caller itself); it starts every function in the unsafe state because a bare CREATE FUNCTION grants EXECUTE to PUBLIC. Removing the migration fails it naming exactly the three functions. |
| low | contains_prohibited parity test | moderationSync.test.js pinned the blocklist to 20260715060000, an already-applied migration, rather than the newest contains_prohibited definition (20260726050000 since 2026-07-26) — so a drifting new migration was invisible and the only quick green for a legitimate new term was editing an applied migration | resolve the SQL source with newestDefining('contains_prohibited') for the term list and all normalization assertions, and report the resolved filename in each expectation — 1ec553c |
| low | shared/analytics.js + shared/badges.js | `earnedFor()` and the `bigSpender` badge rule valued an hourly booking at its hourly RATE — they read `counterOffer ?? job.pay` and ignored payType, estimatedHours and the pinned `amount_cents_quoted`, so a 6h x $25/hr gig counted as $25 in 'Most profitable day' and in the $1,000 Big Spender tally, disagreeing with Transactions and every other money surface | both now call `bookingGrossDollars()` (shared/taxFormat.js), the same pin-first rule MoneyGoalCard and platformIncomeForYear already use; 8 discriminating cases added to `__tests__/analytics.test.js` + `__tests__/badges.test.js`, built through the real `transformBooking` — c7040c9 |
| low | shared/age.js | computeAge/isAdult read a Date input with LOCAL getters, so a date-only Date (`new Date('YYYY-MM-DD')`, UTC midnight) shifted a day earlier west of UTC and isAdult admitted a 17-year-old on the eve of their birthday — latent, all current callers pass parseDob strings | isoFromDate now reads a Date the way it was written: exactly-UTC-midnight is a date-only value read in UTC, anything with a time-of-day is a local instant read locally (a plain getUTC* swap is the mirror bug for a picker's local-midnight Date, verified in Asia/Tokyo); child-process-per-timezone test in __tests__/age.test.js — 5aa4d2e |
| low | web/app/(app)/u/[id] | web-app#5 — Blocking from the web public profile wrote the blocks row directly (blockUserDb) instead of JobsContext.blockUser, so blockedIds stayed stale and the blocked person's gigs stayed in Browse and their conversation in Messages until a hard reload | doBlock now calls the context blockUser; guarded on both clients by __tests__/blockGoesThroughContext.test.js — 35e8087 |
| high | money-paths | **A hold Stripe said did not exist sat `authorized` forever, and reconcile-stripe only reported it.** Found on the live board: $150, six days old, `authorized` here and `requires_payment_method` at Stripe. expire_stale_pending_bookings reads `authorized` as live escrow and skipped the booking, so the slot never returned to the market, and ctl_escrow_hold_lapsed_uncancelled would have opened a second, misleading CRITICAL at eight days. | 2026-09-06 — reconcile-stripe now corrects the row from Stripe's verdict (authorized→pending/cancelled), re-asserting the status it read so a concurrent capture wins. `__tests__/deadHoldIsRepaired.test.js`. VERIFIED on the real row: it went authorized→pending on the next sweep. Commit 6137ba4. |
| medium | mobile-ux | **A refused booking told the earner to "try again" instead of what to change.** guard_booking_requires_slot raises "This gig is booked through its time slots — pick an available one" and JobDetailScreen replaced it with a generic retry. | 2026-09-06 — bookJob captures the message for SQLSTATE 23514 only (our own guards; every other code keeps the generic line) and the screen prefers it. `__tests__/bookingRefusalReachesTheEarner.test.js`. Commit 6137ba4. |
| medium | admin-console | **`/promotions` selected a column that has never existed, so the Grants section — and the only Revoke lever — always rendered as "No grants issued."** The query asked PostgREST for `promo_grants.uses_used`; the table has `uses_consumed` (20260806070000:120-121) and the only later ALTER adds `revoked_at`/`revoked_reason`. PostgREST fails the WHOLE select with 42703, supabase-js returns `{ data: null, error }`, the page destructured only `data`, and `(grants ?? []).length === 0` rendered the empty state unconditionally. `revokeGrant` (actions.ts:246) is correct but its only entry point lives inside the branch that never rendered — regressing the row closed 2026-08-13 ("promo grants are listed with a working Revoke"). The `bonus_ledger` table below it rendered normally, which made the empty list read as truth. | 2026-09-06 (commit 2ed71ca) — column corrected to `uses_consumed` in both the select and the render, and the query error captured: the error branch prints the message and the heading reads "Grants (unknown)" rather than claiming zero. New guard `__tests__/adminSelectColumns.test.js` parses every `.from(...).select(...)` in `admin/` against the columns the schema creates, reading BOTH `supabase/*.sql` and `supabase/migrations/*.sql` — 128 selects over 33 tables, exactly one mismatch on the old code. Reverting the page fails 3 of its 5 tests. |
| medium | admin/payments | /payments never read refunded_cents, so a fully or partly refunded payment rendered as a live `captured` charge for the full captured amount, and the status chips had no value to filter refunds by | Select refunded_cents/refunded_at/refund_reason; net the Captured figure and add a Refunded column + refunded/part-refunded pill; add a `refunded` pseudo-filter on `.gt("refunded_cents", 0)` since payments.status cannot hold it; arithmetic extracted to admin/lib/money.ts and pinned by __tests__/adminPaymentsRefunds.test.js — eb03d2b |
| medium | web / two-factor | Web recovery-code redemption never refreshed the session, so a reload re-derived nextLevel aal2 from the cached user.factors, bounced back to /mfa and burned a second recovery code (mobile and admin already refreshed; only the web page drifted) | `await supabase.auth.refreshSession()` after a successful redeem in web/app/mfa/page.tsx, and __tests__/adminMfaGate.test.js now asserts the refresh on all three redemption sites instead of only the console — commit 2b6415d |
| medium | controls | **A control that named one entity twice ERRORED instead of filing, and ctl_stripe_id_mode_mismatch would have done it at the live cutover.** run_control upserted every returned row with `on conflict (control_key, entity_id) … do update` and never de-duplicated; Postgres refuses to touch one target row twice in a statement (SQLSTATE 21000), so the whole control aborted — last_error set, `errored` paged, and NOT ONE finding written, including for the entities that were not colliding. Reachable from four registered controls: the id-mode control keyed both its connected-account and customer arms on a bare `user_id` (everyone here can earn AND post, so one person with a Connect account and a saved card pre-cutover is one entity twice), ctl_client_holds_truncate cross joined anon and authenticated against relname, ctl_fee_tier_ladder_inverted joined a rung to every lower rung it beats, ctl_referral_bonus_repeat grouped per (referrer, referred) while naming the referrer. 20260814100000 recorded the hazard in a comment and hand-authored one control around it; three of these four were written before that comment. | 2026-09-06 (20260906045000, commit cdf5757) — run_control now groups by `entity_id` before the upsert and MERGES duplicates into one finding (`kind: multiple_rows_for_one_entity`, originals under `rows_detail`), so no control including future ones can abort this way; `distinct on` was rejected because it would silently drop the second arm, which at the cutover is a stale customer id nobody is told about. The four controls give each row its own id (`acct:`/`cus:` namespaces; one row per table, per rung, per referrer). Probe: the pre-fix statement is issued against a staged table both client roles hold and is ASSERTED to raise 21000 (the migration fails itself if it does not), then run_control files on the same population; one profile with both a Connect account and a customer under a live cutover files two findings where it previously raised. Guard: `__tests__/controlEntityUniqueness.test.js`, 6 of 8 failing on the pre-fix corpus. |
| medium | ops / controls | controls-registry#3 — no dead-man's switch: every alert is dispatched from inside controls_sweep_and_page / controls_digest, both pg_cron jobs, so a dead scheduler pages nobody and /controls stays green. Acknowledged and deferred at 20260814100000:55-57, never registered. | Mutual watch. `20260906050000_nothing_watches_the_watchman.sql` adds `controls_heartbeat()` (service-role RPC: sweep age from the registry, cron.job presence via new `cron_schedule_health()`, controls erroring, open severe findings), `ctl_cron_not_scheduled` and `ctl_heartbeat_absent` (self-arming — the seeded 7-day `grace_until` in `app_flags.controls_heartbeat` is deleted by the first check-in). External half: Vercel cron `35 * * * *` → `admin/app/api/controls-heartbeat` (CRON_SECRET, own Resend transport, never through controls-alert). `/api` excluded from `proxy.ts`'s matcher — it would have 307'd the cookie-less cron to /login and Vercel would have recorded that as a healthy invocation. Guarded by `__tests__/controlsHeartbeat.test.js`. Controls 58→60. ae5e73b |
| medium | ops / controls | controls-registry#5 — no control watched the edge error sink: logServerError lands every money edge function's failures in client_errors, but the only readers were the /errors page and a dashboard tile, so a rotated Stripe key or a migration-broken RPC signature could fail every call all weekend with the controls board green | ctl_edge_errors_burst (high/money) groups the last 90 min of platform='edge', dev=false rows by app_version and reports >= 3 fatal or >= 10 total, one finding per function, rolling window so it auto-resolves; guarded by __tests__/edgeErrorSinkWatched.test.js; CLAUDE.md control count 58->59, in-database 56->57 — commit 433ba27 |
| medium | ops / privacy | controls-registry#6 — no canary asserted that jobs.location is still masked, so the one regression CLAUDE.md warns 'publishes every street address on the platform' would be silent | ctl_job_location_unmasked (critical, security) registered: location = mask_location(location) against data, plus a coordinate-snap arm in the same row; rolled-back probe disables trg_mask_job_location to stage the leak and shows the same insert is silent with it on. __tests__/jobLocationUnmasked.test.js pins the shape. 20260906052000_canary_for_the_address_masking_nobody_watches.sql, commit f1439ef |
| high | db-guards-lifecycle | expire_stale_pending_bookings(14) could never cancel a row from pg_cron — guard_bookings_write raised 'not authorized' in the no-JWT context and controls_sweep_and_page swallowed it as a warning, so the documented backstop for stale applications, held slots and consumed-at-apply benefits had never run since 2026-08-12 | The function now claims service_role transaction-locally around its own UPDATE and restores the caller's claim before run_all_controls; guard_bookings_write is untouched. Migration 20260906042000_stale_pending_expiry_runs_without_a_jwt.sql (rolled-back probe: old body raises on the staged row, new body expires it and frees the slot, a started application is still left alone), drift guard __tests__/sweepHousekeepingRunsWithoutJwt.test.js — bdf24f8 |
| medium | db-guards-lifecycle | jobs.created_at owner-writable on UPDATE — a poster could PATCH it to a far-future date and pin their listing first in the web feed, the assistant's gig search, the mobile browse query and the 'newest' sort forever, bypassing the 24h bump cooldown by never touching bumped_at; the INSERT-only clamp in 20260726100000 skipped UPDATE citing 20260722020000, which pins profiles.created_at, not jobs | Pinned created_at in guard_jobs_write above the no-live-booking early return; corrected the misleading comment in guard_jobs_bump_not_future; backfilled forged rows to least(now(), bumped_at); added __tests__/jobsGuardPinsCreatedAt.test.js — 20260906043000_jobs_created_at_is_pinned_on_update.sql, commit 68261c4 |
| medium | db-guards / lifecycle | `profiles.deleted_at` (the erasure tombstone) was owner-writable — guard_profiles_write is a denylist whose last definition (20260722020000) predates the column, so any user could `PATCH {"deleted_at":…}` on themselves, opening a HIGH ctl_tombstone_leaks_pii finding that pages the on-call for an account nobody erased (and auto-resolves the moment they PATCH it back), and pre-stamping the timestamp a genuine later erasure keeps via `coalesce(deleted_at, now())` | Pinned in the guard's owner branch (service_role + app.recompute paths untouched, so tombstone_profile and the console still write it) — `supabase/migrations/20260906044000_pin_profiles_deleted_at.sql`, with a self-asserting probe that reproduces the write and the HIGH finding on the pre-fix body then shows the fix blocks the identical write; drift guard `__tests__/profilesGuardDrift.test.js` asserts the whole pin list. Commit 7e178d3 |
| medium | erasure | **Deleting your account kept your .edu address and locked you out of re-verifying it.** `student_email_verifications`' FK cascade is its only cleanup, and tombstoning (20260813150000) means it never fires — so the consumed row keeps a real school email forever AND `uniq_consumed_student_email` blocks the same person from verifying that inbox on a new account, at the database. start returns a silent `{ ok: true }` without sending a code and confirm then answers `no_pending`, so the delete-and-start-fresh flow Apple 5.1.1(v) requires costs a student the Verified Student badge permanently, with no self-service or console repair. | 2026-09-06 (20260906025100) — `tombstone_profile()` deletes the rows, rows already stranded under tombstones are cleared, and `ctl_tombstone_leaks_pii` gained `has_school_email`. Probe: the unique index blocks a second account before the fix, and after the tombstone the inbox verifies again. |
| medium | erasure | **Erasing a Google/Apple account killed that sign-in button for good.** `updateUserById` neither deletes nor rewrites `auth.identities`, so the provider `sub` stayed pointed at the banned user: every later social sign-in resolved to it and was refused, with no path minting a new user for that sub, while `identity_data` kept the provider's name/email/avatar on an erased account. Nothing in the repo had ever touched that table. | 2026-09-06 (20260906025200) — `tombstone_profile()` deletes the non-email/phone identities and neutralises the address on the email one (kept, because delete-account's step 5 rewrites it); already-erased accounts are cleared; `ctl_tombstone_leaks_pii` gained `has_social_identity`. Probe stages both identity kinds, tombstones, and asserts the social one gone, the email one scrubbed, and the control discriminating. |
| medium | student-verify | **A 6-digit student code was brute-forceable: the 5-attempt cap was a stale read.** confirm tested `row.attempts >= 5` against the value it had just read and wrote `row.attempts + 1` in a separate, unpredicated statement, so N concurrent guesses all passed and all wrote 1 — twenty guesses cost one attempt against a 900,000 space, with no other limit on the endpoint. A hit grants the Verified Student badge for the VICTIM's school and locks the address's real owner out of verifying it. | 2026-09-06 (20260906025300) — `claim_student_verification()` makes the increment the gate (`… where attempts < 5 returning attempts`), compares the hash after the attempt is spent, and caps 15 attempts per INBOX per 15 min across accounts (per-user does not bind an attacker who registers more accounts); `ctl_student_verify_bruteforce` reports a grind. Probe replays the old shape (2 guesses → 1 attempt) beside the new (2 guesses → 2 attempts) on one staged row. |
| low | payments | **The Transactions CSV dated rows in UTC while the screen dated them locally.** `ledgerCsv` formatted each row with `toISOString().slice(0,10)`, but `rangeBounds`/`filterEntries`/`byMonth` and the screen all work in local time — so a settlement that straddles UTC midnight was exported under a different day, and a 'This year' statement could carry a row dated outside that year. The screen's own comment says the export must match what the user is looking at, because they reconcile against it. | 2026-09-06 (02379c2) — the CSV date is built from local components; three cases in `ledger.test.js` (straddling instant, agreement with the `byMonth` key, and a no-`toISOString` source guard) fail on the old formatter and pass on the new. |
| medium | mobile-booking | **'Book this gig' on a gig whose only slot was taken inserted a booking with `slot_id` NULL, and the database accepted it.** `JobDetailScreen.handleBook` refused only when the job had DATED slots and none was selectable, so the commonest gig on the platform — one undated 'Flexible — Contact to Schedule' slot, already taken — fell through both guards and the footer even read 'Book this gig'. Server-side nothing stopped it: `bookings_insert_own` checks only `earner_id`, `guard_bookings_write` validates the slot only `if new.slot_id is not null`, `bookings_one_active_per_slot` is scoped `where slot_id is not null`, and `guard_booking_slot_not_past` returns early on a null slot. If the poster accepted, a second escrow hold went on their card for a single-slot gig already spoken for, and the row had no `starts_at` — `earner-claim-payment` refuses it forever with NO_SCHEDULE and `ctl_live_booking_without_schedule_anchor` reports it as `no_slot_id`. Detection existed; prevention did not. Reachable from the poster's public profile and from Saved gigs, both of which push JobDetail with no slot check. | 2026-09-06 (3b136b3, migration 20260906033000) — the rule moved into `shared/filters.js` as `bookingBlockReason()` (a gig that HAS slots is booked THROUGH one of them), read by both the handler and the footer label so the button can no longer invite a tap the handler refuses, and into the database as `guard_booking_requires_slot` so the assistant, the website and anything later inherit it. Contention is still decided atomically by `bookings_one_active_per_slot`, not by a read-then-decide check. Probe: the slot-less insert is refused, the same row inserts fine with the trigger dropped and is picked up by the schedule-anchor control, a booking through a free slot still works, and a gig with no slots at all stays bookable. The web client had already been fixed for this in f91ab4f; mobile was the last wrong surface. |
| low | mobile-copy | **The JobDetail rate-the-poster sheet asked 'How was {job?.posterName}?' — a field `transformJob` has never produced** (the name is at `job.poster.name`), so the `|| 'the poster'` fallback fired for every poster ever rated from that screen: nobody was ever named. A repo grep found the identifier only on that line and on an unrelated `JobCard` styles key. | 2026-09-06 (294f74c) — reads through a new `posterDisplayName()` in `shared/transforms.js`, which lives beside the two PLACEHOLDER names the transforms themselves write and refuses them ('Anonymous' for a row with no profile embed, 'Poster' for the booking-fallback shape), so the briefly-resolving fetchJobById path cannot render 'How was Poster?'. `__tests__/posterDisplayName.test.js` asserts the contract from transformJob's output and scans `src/screens` + `src/components` for any `job.posterName` read; restoring the old line makes that scan fail and name the file and line. |
| low | incentives | **A promo code for a campaign nothing can spend said "applied" and bought nothing — permanently.** `promotions.kind` has three values and only two have a consumer: `consume_promo_grant` matches `fee_override`, `consume_poster_discount` matches `poster_discount`, and a `bonus` campaign is paid out of `bonus_ledger` by `accrue_referral_bonus`, which reads the `referrals` table and never looks at a grant. `redeem_promo_code` (20260806070000:252) and `grant_promotion_to_users` (20260806120000:178) were both written without reference to `kind`, so a code on a bonus campaign burned a seat, minted a grant with the NULL `fee_bps` that `promotions_kind_shape` forces, and returned TRUE. `promo_grants_one_per_user_promo` made it permanent — every retry answered TRUE again from the already-claimed branch, and revoking does not free the slot because the index is not partial on `revoked_at`. | 2026-09-06 (20260906031000, commit 617c141) — a POSITIVE kind gate on both functions, ahead of the seat increment AND the already-claimed branch so a pre-existing inert grant also stops reporting "applied"; a fourth kind stays refused until a consumer learns it. `grant_promotion_to_users` raises rather than returning 0. Because the gate is not retroactive, `ctl_inert_promo_artifact` reports the codes and grants already issued, one row per campaign. Console: MintCodes hidden AND refused server-side, `/pricing`'s grant picker filtered, one shared list in `admin/lib/promoKinds.ts`. Probe: bonus refused with zero seats burned and no grant, `fee_override` still redeems and still grants on the same run; control fires 1 / 0 / 0 and auto-resolves. `__tests__/promoKindGate.test.js` fails 7 of 9 on the old code. CLAUDE.md control count 58 → 59 (57 in-database + 2 external). |
| high | controls | **The hourly sweep has never expired a stale application.** `controls_sweep_and_page` (20260814070000:38-43) called `expire_stale_pending_bookings(14)` with no `request.jwt.claims` set, and pg_cron (20260806030000:108) supplies none — so `auth.role()`/`auth.uid()` were NULL, `trg_guard_bookings_write` (20260730140000:49, :182) raised `not authorized to modify this booking` on the first qualifying row, the whole UPDATE rolled back, and the sweep's own `exception when others then raise warning` hid it. Nothing contradicted it: the only callers that ever succeeded were migrations that set the claim first (20260812080000:20), and the console's "Run sweep now" calls `run_all_controls`, which never reaches the expiry. This is the backstop the poster-discount row names for releasing a benefit consumed by an application nobody accepted — it does not fire, the slot stays flagged taken, and the earner waits under Awaiting forever. | 2026-09-06 (20260906034000, commit 67c1e1e) — the sweep claims `service_role` for its own transaction as its first statement (transaction-local; not an escalation — the function is already SECURITY DEFINER and revoked from public/anon/authenticated), body otherwise verbatim from 20260814070000. Probe on one staged 20-day-old pending booking: claims blanked ⇒ raises and cancels nothing; claim set ⇒ same call cancels it; plus a `pg_get_functiondef` assertion that the claim precedes the expiry. New registered control `ctl_expiry_sweep_not_clearing` asserts the outcome instead of the call — the exact rows the expiry matches that have outlived the window by more than a day, i.e. survived 24 sweeps — since `ctl_stranded_pending_booking` deliberately also matches rows the expiry must not touch and can never mean "the sweep is broken". `__tests__/sweepClaudeServiceRole`→`__tests__/sweepClaimsServiceRole.test.js` guards the drift, because the sweep body is copied forward by every migration that adds a call to it. |
| medium | controls / incentives | `ctl_redemption_double_charge` reconciled every campaign kind against `promo_redemptions`, a ledger only fee_override and poster_discount write — so the first referral bonus a campaign minted opened a permanent CRITICAL 'money' finding that paged, could never auto-resolve, and (findings being unique on control_key+entity_id) occupied the row a genuine double charge on that campaign would have used | Each kind now reconciles against its own ledger: `kind <> 'bonus'` against live `promo_redemptions` rows, `kind = 'bonus'` against non-void `bonus_ledger` minting rows (`source_booking_id is not null`, so a `consume_fee_credit` split is not a second charge). `supabase/migrations/20260906035000_redemption_reconciliation_reads_the_right_ledger.sql` + `__tests__/redemptionReconciliation.test.js`; commit 0f6638a |
| medium | payments-audit | **A refund taken back before the credit landed was recorded and then never applied, and the remedy credited the gross.** `record_refund` (20260814130000:97-117) writes the earner's share to `payments.earner_refunded_cents` unconditionally and discards `debit_earnings`' boolean; `debit_earnings` (20260804020000:44-46) refuses outright on a payment whose `earnings_credited` is false. So on a capture whose credit failed — the `ctl_earner_credit_missing` state — a refund recorded a clawback nothing took, and `credit_earnings` then credited `earner_amount_cents` in FULL. $60 captured (5580/420), credit fails, console refunds $30 with `reverse_transfer:true`: Stripe pulls 2790 out of the Connect account, 2790 is recorded, nothing is debited, and the remedy adds $55.80 to a true position of $27.90 — permanently, with `ctl_earnings_total_drift` opening a HIGH nothing can auto-resolve and `src/lib/payments.js` rendering a refund share the balance never lost. | 2026-09-05 — `supabase/migrations/20260906040000_credit_earnings_honours_the_recorded_clawback.sql` (commit 13fcd77). `credit_earnings` now credits `earner_amount_cents − earner_refunded_cents`, floored at 0, read in the SAME claim UPDATE's RETURNING so it comes from under the row lock `record_refund` also takes: whichever commits first, the other sees the settled value, and the claim still fires once per payment so the clawback lands exactly once. A lost chargeback records 0 and still credits in full (20260813160000). Recording 0 instead was rejected — the reversal really happened at Stripe, and the recorded figure is the only durable memory of it. Rolled-back probe proves the net credit (27.90, raising by name on the old 55.80), a silent drift control, an unchanged ordinary path and an unchanged exactly-once claim; `refundAttribution.test.js` pins the net form off disk and fails against the previous definition. `ctl_earner_credit_missing`'s registry text (which said the refund was "silently forgiven … so the platform eats it") and RUNBOOK_MONEY §3 step 2 corrected in the same change. **Needs `supabase db push --linked`.** |
| low | reconcile-stripe | Recording a lost chargeback (the documented remedy) made reconcile-stripe open a permanent CRITICAL refund_mismatch that no action could clear — and the unrecorded case produced no finding at all, because 0 compared equal to 0 | check #4 now adds LOST dispute amounts (via stripe.disputes.list, gated on charge.disputed) to charge.amount_refunded before comparing against refunded_cents; an unreadable dispute reports `dispute_unreadable` instead of a guessed number. Guarded by __tests__/reconcileChargebackParity.test.js, which also pins record_reversal as ledger-only. b3f4bd8 |
| medium | reconcile-stripe | The reconciliation window filtered payments.created_at — the FIRST hold ever placed, on a table with no updated_at — not 'touched', so an external reversal on any payment older than 14 days was never examined; that is the only backstop for an admin refund that succeeds at Stripe and then fails to ledger (the webhook skips it while the in-flight marker is fresh and answers 200) | new public.payments_touched_since() scopes and orders on greatest(created/authorized/captured/refunded/cancelled); reconcile-stripe calls it and adds a Stripe-side pass (refunds.list + disputes.list since the window start) plus a re-examination of payments already carrying an open finding. Probe in 20260906030100 shows the old created_at predicate returns 0 for a row refunded ten minutes ago. bd75b71 |
| low | reconcile-stripe | writeFinding returned null on an RPC error, so the payment stayed in `examined` and out of `seen` and resolve_reconciliation_findings closed a real open discrepancy with 'auto-resolved: reconciles against Stripe' — the class 20260806270000 calls reporting success it did not verify | writeFinding now returns the entity unconditionally and records the failure in the run's warnings, which become last_error on the registry row; checkWebhookConfig captures its three record_external_finding errors and re-stamps last_error after resolve_external_findings clears it. Guarded by __tests__/reconcileFindingWriteFailure.test.js. 0bfc7d3 |
| medium | delete-account / admin deleteUserCascade → Stripe | Account deletion never deleted the Stripe Customer or Connect account, so a departed user's saved card stayed on file at the processor with their name and email attached and off-session charging possible — and since the profile is tombstoned rather than deleted, the stripe_customers/stripe_accounts rows survived too, with no in-app path left to remove any of it | Both paths now call customers.del (detaching every saved card) and attempt accounts.del, best-effort and allowed to fail on a positive Connect balance; the edge function drops the local mapping row once Stripe reports the object gone. New ctl_stripe_object_after_erasure lists the accounts already erased that still hold a mapping — e6f87fc |
| low | parity | **The website alerts inbox could not route a ProfileTab alert.** `web/lib/notifications.ts` TAB_ROUTE knew four of send-push's five tabs, so the 'Two-factor authentication was turned off … change your password now' alert (written by 20260814040000 when a recovery code is redeemed), the Stripe payout alerts and every console admin notice resolved to href null — the tap marked the row read, auto-archived it and navigated nowhere. Mobile had hit and fixed the same omission; the web copy was never updated. | 2026-09-06 — 2da1c47. `ProfileTab: "/profile"` added, matching mobile. parity.test.js now pins BOTH inbox routers to KNOWN_TABS, so the next tab cannot be added in one of three places and forgotten in the other two. |
| medium | admin/users/[id]/export | The GDPR data-access export hard-coded 27 tables, omitted 11 with a direct user FK, and exported bookings by earner_id only — so a poster's subject-access request contained no bookings and no payments, with nothing saying a table was skipped | Added the missing direct-FK tables plus a DERIVED pass (job ids → poster-side bookings → payments → refund_ledger; ticket ids → support_ticket_messages), chunked `.in()` at 200, redacted agent identities; new __tests__/exportCoverage.test.js enumerates every profiles/auth-FK table from the schema and fails unless it is exported, derived or excused with a reason — d7a7a64 |
| medium | parity | **Hustlr AI could not tell the website from the app.** Both clients POST the same body, so one system prompt — written for the phone — was served to gohustlr.com: 'You → Payments & payouts → Transactions' and 'Messages → GoHustlr Support' name screens the website does not have, and 'Settings → Security' is titled 'Two-factor authentication' there. The prompt's own 'never invent a screen' rule was being broken on one of its two surfaces, and parity.test.js could not notice because it only checked the strings existed. | 2026-09-06 — 960df75. Request body carries `client: 'web' \| 'mobile'` (anything but 'web' = the app, so older builds are unaffected); the prompt swaps PLACES_MOBILE for PLACES_WEB. parity.test.js runs every MUST_KNOW check against BOTH prompts, checks each `Settings → …` row against that client's own Settings rows, and fails if the web block hands out an app-only screen. |
| medium | parity | **Hustlr AI deferred to Support for a dozen things the app answers in one tap.** MUST_KNOW had 8 entries and the prompt named 5 destinations; nothing in it mentioned claiming payment on a gig the poster never verified, redeeming a promo/referral code, the on-gig safety tools, report/block, verification, invites, saved gigs/people, the alerts inbox or its settings, availability, Insights, or account deletion. A ghosted earner was answered with the escrow line instead of the button that pays them. | 2026-09-06 — e8d8538. All twelve added to both client blocks (each label verified against both clients; the three app-only ones named as app-only), the escrow lifecycle line now names the claim path, and MUST_KNOW gains a regex per destination that runs against both prompts. |
| medium | parity | **A poster could not reach their own ledger from the Hire tab.** `Payments` (title 'Transactions') is registered in GigsStack and nothing in GigsStack navigated to it, so the only screen showing a poster what they were CHARGED — receipt, pinned fee rate, remainder released on a 75% capture — was reachable only via You → Settings/PayoutSetup. CLAUDE.md had recorded it as 'a gap, not a design' and it was not in the register. | 2026-09-06 — 262a123. A Transactions row at the top of GigsScreen's Past segment. `__tests__/ledgerEntryPoints.test.js` now fails on any route a stack registers that nothing in that stack navigates to, unless excused there with a reason. |
| medium | JobsMap / app.json android config | Android had no `com.google.android.geo.API_KEY`, and the Map toggle / JobsMap / Market Insights heat-map were ungated on Android, so the first Android build would offer a map the Google Maps SDK refuses to initialise. `android.config.googleMaps.apiKey` is dead config here — react-native-maps ships its own plugin that beats the Expo fallback and strips the meta-data when given no props. | `src/lib/mapsConfig.js` is one config-driven gate all three render sites consult; Android falls back to list-only and the toggle is hidden until app.json carries `["react-native-maps", { "androidGoogleMapsApiKey" }]`, at which point the map turns on with no code change. Guarded by `__tests__/androidMapsKey.test.js` (16 tests; 2 fail on the pre-fix sources). TESTFLIGHT.md + CLAUDE.md updated. Commit 8707f64 |
| medium | admin/ + web/ deps | next 16.2.9 carried nine advisories (4 high) incl. a Server-Action DoS reachable pre-auth on the console's 15 "use server" modules | bumped both projects to 16.2.12 + __tests__/nextMinimumVersion.test.js floors the pin in manifest AND lockfile — 400336e (npm install + redeploy still owed) |
| medium | rls/suspension-cascade | Suspending a poster silently hid their confirmed earner's message thread, chat photos, completion photos and dispute, because every party-scoped policy (messages_read, messages_insert, disputes_select_parties, chat_photos_party_read, completion_party_read) established 'party' with an inline `join public.jobs`, which is RLS-filtered by jobs_select_all's suspension clause — so the person who filed the safety report lost their own evidence at the moment staff acted on it | 20260906041000_party_checks_ignore_suspension.sql adds private.is_booking_party + private.booking_parties_blocked (SECURITY DEFINER, `private` schema) and rewrites the five policies to use them; jobs_select_all untouched. Guarded by __tests__/partyPoliciesSuspensionAgnostic.test.js. Commit 4cb0fde |
| medium | safety | **Blocking and suspension stopped the messages and nothing else the counterparty reads.** `reviews_insert_auth` (newest def 20260624220000:186) required only party-to-a-verified-booking, and the only trigger on `reviews` is the keyword filter — so a blocked or suspended party published review text onto the other person's PUBLIC profile, which RUNBOOK_SAFETY §2.3 says cannot be redacted. `guard_bookings_write`'s poster branch (newest def 20260730140000) left `amendment_note` unpinned and let `amendment_status` move to `'pending'`, delivering free text to the earner's My Jobs with a "Change proposed" push; `review_text`, `earner_rating` and `payment_method` were the same shape one row down the same card. `private.is_suspended` / `private.is_blocked_pair` were referenced by three objects in the whole schema, none of them these. | 2026-09-06 (20260906055000, commit e6b2a26) — both clauses added to the policy (blocks bidirectional, matching `messages_insert`, so blocking cannot become a race to silence the other's review; suspension on the actor only, so the harassed party keeps their warning), and a conditional pin of all five poster-authored columns in the poster branch. Pinned rather than raised, because the block is deliberately silent. Lifecycle untouched — decline/cancel/complete/verify still work, since suspension must never strand an escrow hold or withhold earned money. Probe runs as `authenticated` and shows: clean amendment lands, blocked amendment does not, review insert 42501 under a block and again under a suspension, both land again with neither. `__tests__/blockReachCoverage.test.js` asserts on the NEWEST definition of each object so a future recreation cannot drop the checks. |
| medium | safety / SOS | `raise_gig_emergency` accepted any booking status and never deduped, so any applicant could file unlimited rate-limit-exempt 'emergency' reports — each one paging the on-call, filling the moderation queue and blocking the counterparty's account deletion (safety-support-moderation-db#5) | `20260906060000_emergency_requires_an_accepted_booking.sql` requires an accepted booking (confirmed/completed/verified) and returns the caller's existing open emergency on that gig instead of inserting a second, serialised on an advisory lock; repeat notes are appended, not discarded. Guarded by `__tests__/safety-guards.test.js`. Commit f8788e2 |
| medium | tests / money-disclosure | **The fee drift guard was reading a body Postgres had already replaced.** `__tests__/pricing.test.js` — the guard CLAUDE.md leans on to stop the fee the apps QUOTE drifting from the fee the server CHARGES — opened `20260806050000_platform_rate.sql` by hardcoded path, but that file's `platform_fee_cents` body was superseded by `20260806140000_fee_overflow_fix.sql` (the last `create or replace` on disk, and so the live one). Every constant assertion pinned text nothing executes, and `sqlFee()` retyped 5000/0.029/30/25 as JS literals, so the parity matrix compared two JS constants that could never disagree. A third redefinition raising the 25c floor margin or 'simplifying' the rounding back to truncation would have shipped with the whole suite green, while JobDetail/CompletionModal quoted one fee and `stripe-capture-payment`'s RPC took another. | Resolve the LAST defining migration and build the mirror from constants PARSED out of that body, the way `supportGuardDrift.test.js` / `tipCaps.test.js` already do. | FIXED 2026-09-06 (f377d87) — resolver + parsed-constant mirror + a self-check that the resolved file is not the superseded one. Discrimination proven on two staged third redefinitions (floor margin 25->50, and half-up dropped): the new test fails 2 and 3 tests respectively where the old test passed 28/28 green. Gate: 70 suites / 1301 tests. |
| high | admin-console | **Deleting a user from the console hard-deleted the auth row and cascaded away the counterparty's paid work.** `admin/lib/deleteUser.ts:150` ended with `service.auth.admin.deleteUser(userId)` while its header claimed parity with `delete-account` — which stopped doing exactly that on 2026-08-13 (20260813150000) because `profiles_id_fkey` → profiles → jobs → bookings → payments is CASCADE at every link. The step-0 gate blocks only `confirmed`/`completed`, so `verified` (settled) work is precisely what cascaded: every earner's booking and payment row for money they had actually been paid — their Transactions statement, Tax Center income and 1099 evidence — erased because an admin removed a poster, with only a `user.delete` audit row to show for it. | 2026-09-05 (commit 3fb0965) — the console path now runs the edge function's sequence: support scrub → `tombstone_profile` (fail closed, and refuse when no profile row was scrubbed) → empty and permanently ban the auth row → global signOut. Header no longer claims a parity it did not have. `__tests__/accountDeletionParity.test.js` pins the two paths — neither may call `auth.admin.deleteUser`, both must tombstone, fail closed, neutralise the auth row identically and in the same order, and the RPC must exist in the migrations with a service_role grant; 5 of 10 assertions fail on the pre-fix file. BETA_QA_PLAN's deletion row described the old cascade as the expected outcome; corrected to the tombstone with the counterparty's rows named as the thing to check. |
| low | admin-console | **The support queue dropped the people waiting longest and then counted what was left.** /support fetched the 200 most recent open+pending tickets ordered `last_message_at desc` and applied the "needs reply" predicate (`last_author = 'user'`) in JavaScript afterwards; a newest-first limit cuts the OLDEST rows, which on a longest-waiting-first queue are exactly the work it exists to surface, and the "N waiting" badge counted the same truncated array. Past 200 open tickets the tab reads "Nothing waiting on us" while people wait. Same predicate-in-JavaScript false negative /bookings fixed. | 2026-09-06 (9a8bbde) — predicate, ordering and `count: "exact"` in the query, a 50-row `.range()` pager, and a head-only exact count for the badge. Ordering moved server-side onto `priority_rank`, a generated column added by `20260906015300` (PostgREST can only order by a column and `priority` is text that sorts high, low, normal, urgent), with a partial index on the queue's shape. Migration carries a pre-fix probe that refuses to proceed unless the text ordering really is wrong. `__tests__/supportQueueWindow.test.js` fails 8 of 15 on the pre-fix page. |
| high | admin-console | **Force cancel voided the poster's escrow hold and then could not cancel the booking.** `admin/app/(console)/bookings/actions.ts` forceCancel released the hold FIRST (irreversible `stripe.paymentIntents.cancel` via admin-payment-action, which writes only `payments`) and only then wrote `bookings.status='cancelled'` — a write `trg_guard_started_booking_cancel` REFUSES on any booking where the earner tapped "I'm on site", because that guard had no `service_role` early return unlike `guard_bookings_write` (20260730140000:49) and `guard_min_age` (20260710040000:29). Result: authorization gone at Stripe, `payments.status='cancelled'`, booking still confirmed/completed and live for both parties with nothing behind it — capture, settle and earner-claim-payment all impossible, and NO control watches that shape (`ctl_settled_without_captured_payment` only fires when `p.id is null`; this row exists). | 2026-09-05 (44ceb1d, migration 20260905005000) — the console now writes the booking BEFORE it touches Stripe, the order `JobsContext.cancelBooking` already used and explained; both orders can strand something, so this strands the RECOVERABLE half (cancelled booking + live hold is caught by `ctl_money_exposed_on_dead_booking` at 6h, fixed by the Release hold button on the same panel, and expires at Stripe in ~7 days). The migration gives `guard_started_booking_cancel` the `service_role` early return its siblings have — blast radius is one caller, forceCancel being the only service-role writer of `bookings.status='cancelled'` in the tree. Probe stages ONE started booking and runs both roles on it: service_role cancels (raised before), the same row as a party is still refused with "already started". `__tests__/forceCancelOrder.test.js` pins the ordering; reverting the two console files fails 6 of its 11 tests. |
| high | disputes / audit-2026-09-08 | **Eight defects in the two-party dispute model, found by a 16-agent audit of the architecture rather than the code.** The web earner had NO reachable route to the reply screen — the banner shipped inside `confirmed && startedAt && !earnerDone`, a state a dispute can never be in, and the guard passed on a substring match against dead code. Both notification routers tested `job_id` before `data.dispute_id`, so the one server-written notice saying "open it to see why and respond" landed on the public gig listing. `completion_party_read` never covered `disputes.response_photos`, so the accuser could not see the rebuttal photographs. Both new hold-age reads used `payments.created_at` — the column 20260806150000 exists to say is the wrong one. `MIN_RUNWAY_HOURS` (36) was smaller than the flat 48-hour window, so a hold aged into [120h, 132h] got a window closing after Stripe voids the authorization: nobody paid at all. `recordReversal`'s bare row got a clock and a server-authored false accusation, then auto-closed itself. NO_ROOM_TO_HOLD discarded the poster's reason and photos and let both clients tell the earner they were paid a percentage when they were paid in full. And both clients promised "a person reads it before any money moves", which branch 4 does not keep. | 2026-09-08 — `20260909040000` re-anchors branch 4 on `coalesce(authorized_at, created_at)`, derives `settle_after` from the hold (48h or 12h before it dies, whichever is sooner), gates the clock and the notice on `proposed_pct is not null` so a reversal row is inert, and extends `completion_party_read` to `response_photos`; `20260909050000` silences the notice on a pre-settled row. `stripe-capture-payment` files the evidence before the no-runway capture and ages the hold from `authorized_at`. The web banner is rendered from one helper into every state a dispute can be in, `ClaimCta` is hidden while one is open (the server refuses it anyway), both routers put `dispute_id` first, and the copy on both clients now says what branch 4 actually does. New control `ctl_dispute_contested_unadjudicated` (high) fires two days before the auto-capture. `disputeTwoParty.test.js` grew assertions that fail on a link in an unreachable branch and on a hold-age read naming `created_at`. |
| medium | disputes / operations | **A dispute whose escrow is gone has no terminal state, and two console operations can put it there.** `settle-disputes` is the only writer of `pct_paid`. If the authorization lapses or is voided while a dispute is unsettled, the settler retries `HOLD_EXPIRED` every hour forever, writes a fatal `client_errors` row each time, and `ctl_dispute_settlement_overdue` (critical) stays open and unclearable — neither `decideDispute` nor `setDisputeStatus` can close the case in a way any of those read. `admin-payment-action`'s `release_hold` and `settle` can create the state deliberately. _(needs a product decision: what a written-off dispute should record)_ | Guard `release_hold`/`settle` in `admin-payment-action` against a booking carrying a dispute with `pct_paid is null` (mirroring the check `setDisputeStatus` already makes), and add an operator op that stamps `pct_paid = 0` with `settled_at`, a status and a written reason, audited like every other money action. Split `ctl_dispute_settlement_overdue`'s detail into "due and unpaid" vs "unpayable, the hold is gone" so the two go to different humans. |
| medium | disputes / records | **The subject-access export cannot produce the accused party's own dispute, and the coverage guard is structurally blind to it.** `exportCoverage.test.js` enumerates by FK from a uuid column; a dispute reaches the accused through `respondent_id`, which the export's TABLES list does not cover. A GDPR/CCPA request from an earner who was accused returns everything except the accusation. | Add `disputes` to the export keyed on `raised_by` OR `respondent_id`, and teach `exportCoverage` that a table can reach a user by more than one column. |
| medium | disputes / reputation | **The poster's held review publishes even when the platform adjudicates that their complaint did not stand, and the earner has no reply.** `publishHeldReview` runs on every settlement regardless of `resolution_pct`, so an earner who wins a dispute at 100% still receives whatever star rating the poster left while making the claim. _(needs a product decision)_ | Either suppress the held review when the settlement lands at or above 100% of the proposal, or give the earner a public right of reply on a review attached to a dispute they won. |
| low | disputes / provenance | **Photo provenance is destroyed at upload.** `uploadImage.js` compresses through expo-image-manipulator, which strips EXIF, so no dispute photograph carries a capture time — only an upload time. The console asserts to the adjudicator that completion photos were "taken by the earner at completion, before any of this was reported", which the data cannot establish. _(needs a product decision: whether to retain EXIF, which carries GPS)_ | Either soften the console's claim to what the data supports (uploaded at X, before the report at Y), or retain a capture timestamp deliberately — and if so, decide about GPS separately, because retaining location EXIF on a safety-sensitive platform is its own decision. |
| high | disputes / money | **A poster's "there was a problem" tap captured the reduced amount inside their own request, and the `disputes` row was inserted ~80 lines AFTER the money moved.** So the record documented a decision already executed; Stripe had already released the remainder to the poster; and nothing in this repo can pay an earner MORE afterwards (`stripe.transfers.create` appears nowhere). The accused party could not read the accusation on any client — `disputes_select_parties` granted them SELECT and no screen ever issued the query, including after 20260906059000 was written specifically so they could open the photos — and filing it removed their only self-service lever, because `earner-claim-payment` refuses a booking carrying an unresolved dispute. The audit log shows six `disputes.view` reads and zero `dispute.*` writes: nobody has ever resolved one. | 2026-09-08 — the tap is now a PROPOSAL. `20260909010000` adds `proposed_pct`/`respondent_id`/`settle_after`/the four response columns, `public.dispute_settlement_pct()` as the ONE definition of the outcome (operator · accepted · silence past 48h · contested-with-the-hold-expiring → 100, because a partial capture cannot be topped up and a full one can be refunded), a server-written notice in the same transaction, `guard_disputes_write`, and `my_dispute`/`respond_to_dispute`. `20260909020000` dispatches the new `settle-disputes` function from the hourly sweep. Both clients gained a dispute screen with photo evidence and an entry point from My Jobs; the console gained `/disputes/[id]`, which is the first place the two sides have ever sat together. `__tests__/disputeTwoParty.test.js` (37 assertions, proved to discriminate). |
| medium | disputes / rls | **The `disputes` third of the staff-attribution leak below.** `disputes_select_parties` scopes ROWS to the two parties and nothing scoped COLUMNS, so either party could read `assigned_to`/`resolved_by` and resolve them to a real name through `profiles_select_all`. 20260909010000's header claims building `my_dispute` on a whitelisted column set closes it; it does not — a screen is not a permission, and `GET /rest/v1/disputes?select=assigned_to` still answered. | 2026-09-08 — `20260909030000` applies the 20260705060000 pattern: table-wide SELECT revoked from anon and authenticated, 19 columns granted back by name, the two staff ones withheld, asserted column-by-column out of the catalog. `resolution_note` is granted deliberately (both dispute screens render it, and the console now says so where the note is typed). **`support_ticket_messages.admin_id` and `payments.refunded_by` are still open** — see the row below. |
| medium | admin-console | **The trust tier could read /moderation and /disputes and act on nothing, and finance could not intervene on a booking.** Both trust pages passed `isAdmin={ctx.role === "admin"}` to their controls while `resolveReport`, `reopenReport` and `setDisputeStatus` all take `requireAdmin("trust")`; /bookings/:id did the same to `InterventionPanel` while every action behind it is `requireFreshAdmin("finance")`. The UI hid what the guard permits — the documented rule backwards — and since `earner-claim-payment` refuses to settle a booking with an open report or dispute, it made one admin's availability a money-harm control, which is the failure `trust` exists to remove. | 2026-09-06 (7b001be) — authority computed server-side as `roleSatisfies(ctx.role, <the action's own tier>)` and passed as `canResolve` / `canIntervene`; the disputes denial reads "view only". `__tests__/adminTierParity.test.js` pairs every console page with its actions file and fails on any UI gate stricter than the guard — it fails exactly those three pages on the pre-fix files and passes the other six. |
| medium | admin-console | **Re-open confirmed a booking with no escrow behind it.** `reopenBooking` (`admin/app/(console)/bookings/actions.ts`) read only `bookings.status`, refused `verified` and `cancelled`, and wrote `status='confirmed'` for everything else without ever reading `payments` — and `guard_bookings_write` early-returns for `service_role`, so the DB re-checked nothing. `confirmed` means a live Stripe authorization is held (the invariant `accept-booking` exists to attest), but `pending` has no hold yet and `declined`'s was voided, and `InterventionPanel` enabled Re-open for both. A mis-declined application re-opened by support becomes an active gig with a dead PaymentIntent: the earner works, `stripe-capture-payment` has nothing to capture, and `guard_bookings_write` refuses completed→verified without a captured payment. | 2026-09-05 (5efb54f) — `reopenBooking` now reads the `payments` row and refuses unless `status='authorized'`, failing CLOSED on a missing row (a missing row is the pending case and is exactly as unfunded as a voided one); a failed lookup stops the action rather than reading as "no hold", the same fail-closed shape `forceCancel` uses. The panel disables Re-open when there is no live hold and says why; the action re-checks regardless, because a crafted POST reaches it directly. The intended use — undoing a one-sided or forced `completed` — is untouched, since capture only happens at verify. `__tests__/reopenRequiresHold.test.js` isolates `reopenBooking`'s own body so `forceCancel`'s payments lookup cannot satisfy it; reverting the two console files fails 7 of its 8 tests. |
| medium | admin-console | **The `support-photos` drift recurred in the two lists the guard was not reading.** `delete-account` gained the bucket on 2026-08-14 after a departing user's own ticket screenshots were found surviving deletion; `admin/lib/deleteUser.ts` and `admin/app/(console)/users/[id]/export/route.ts` kept the old six-bucket list. So an account deleted from `/users/[id]` retained every photo it had attached to a support ticket — files people attach precisely because something went wrong — and a data-access request answered from the same page returned the ticket TEXT while silently omitting the photographs the platform holds. `__tests__/storagePolicies.test.js` asserted coverage against the edge function's `BUCKETS` only, so the drift it was written to catch recurred one directory over. | 2026-09-05 (commit 749ac9b) — `support-photos` added to both lists; both loops list the `<userId>/` prefix only, so agent attachments under `ticket-<id>/` are untouched. The guard now runs the created-vs-covered assertion against all THREE lists, matches either quote style, and asserts the three agree with each other so a bucket added to one fails before the coverage check applies; 3 of 12 assertions fail on the pre-fix files. CLAUDE.md's delete-account row said the list had drifted twice and that the guard covers it; corrected to three and to all three lists. |
| high | auth-mfa-stepup | **A phished password could irreversibly delete a 2FA-enrolled account without the factor ever being asked for.** The sign-in 2FA gate is CLIENT-SIDE in both apps — a password sign-in on an enrolled account returns a real aal1 session — and `delete-account` validated the JWT with `getUser()` and nothing else (no `requireStepUp`, no AAL read), then tombstoned the profile, replaced the auth email, banned the row for 100 years and revoked every session. It parses no body and needs no confirmation phrase (the typed-username gauntlet is client-side), so a bare `POST /functions/v1/delete-account` with a stolen Bearer token was the whole attack — on the one irreversible account-destroying action in the product, while the two step-up-gated functions protect something reversible by comparison. | 2026-09-05 (commit 0f76ff1) — `requireStepUp(admin, user.id, token)` immediately after `getUser`, before every gate and every destructive step, with the module's existing semantics: no factor proceeds, a lookup error fails closed, and `MFA_REQUIRED` carries a message naming deletion. Both clients' refusal whitelists gained `MFA_REQUIRED` / `mfa_check_unavailable`, since a correct denial otherwise rendered as "Please try again, or email support." `__tests__/stepUpEdgeCoverage.test.js` pins the roster, asserts the gate precedes tombstone/updateUserById/storage-remove, and checks both clients; 4 of 8 assertions fail on the pre-fix tree. CLAUDE.md's step-up bullet named only the two payout actions and did not say the sign-in gate is client-side; corrected. |
| low | auth-mfa-stepup | **A step-up refusal reached the user as a dead end on both clients.** `_shared/stepUp.ts` answers 403 `MFA_REQUIRED` on `stripe-connect-onboard` and `stripe-payout-login-link` with "Enter your authenticator code to change payout details", under a comment claiming the app routes it to the code prompt. Nothing read the string: `grep -rn MFA_REQUIRED src web` found only the line that wrote it, so both payout screens rendered the instruction as a failure toast on a page with no code field. The local AAL check cannot pre-empt it — `getAuthenticatorAssuranceLevel()` reads `session.user.factors` from the STORED session, so a session predating enrolment passes the sign-in gate until its token refreshes. | 2026-09-06 (79255f2) — `requireMfaChallenge()` on both auth contexts opens the challenge gate on the server's refusal; `handledStepUp` in `PayoutSetupScreen.js` and `web/app/(app)/profile/payouts/page.tsx` calls it instead of showing the generic toast. `__tests__/payoutStepUpRecovery.test.js` pins the server literal and both clients to one code; 9 of its 15 assertions fail on the pre-fix files. |
| low | auth-mfa-stepup | **Push-token cleanup on natural session expiry deleted nothing, and a revoked device kept receiving the account's notifications.** AuthContext's SIGNED_OUT branch called `unregisterPushToken(prevUserId)` under a comment saying it stopped the device receiving pushes; auth-js clears the stored session BEFORE emitting SIGNED_OUT and supabase-js falls back to the anon key, so the DELETE ran unauthenticated and owner RLS matched zero rows. `signOut()` works only because it awaits the delete before revoking. A row is otherwise evicted only when another account signs in on the device or the app is uninstalled — so "Sign this user out of all devices" and a suspension both left those phones on the account's push list. | 2026-09-06 (3c638b3) — the dead call and its false claim removed (cacheClear kept, comment records what actually evicts a row); `forceSignOut` and `suspendUser` clear `push_tokens` with the service role, best-effort, count recorded in the outcome audit. `__tests__/pushTokenExpiry.test.js` fails 8 of 10 on the pre-fix files. |
| low | EditJobScreen | **The edit screen answered "can I delete this gig?" with the core-terms lock.** `handleDelete` reused `isLocked` (confirmed/completed/verified) while `GigsScreen` and the web edit page gate on pending/confirmed/completed — and `deleteJob` is a bare `update({status:'cancelled'})` with no server-side check, so the client predicate is the whole rule. A gig with live applications could be soft-cancelled from Edit (applicants stuck on 'Awaiting confirmation' until the 14-day sweep), while a finished, verified gig was refused with 'Someone is actively working this gig' and deleted without complaint from Hire. | 2026-09-06 (commit 8edecf0) — `handleDelete` gets its own `canDelete` predicate on pending/confirmed/completed and GigsScreen's refusal copy; `isLocked` is left alone because editing and deleting are different questions ('verified' locks the terms and permits withdrawal; 'pending' the reverse). `parity.test.js` now pins all three delete gates to the same status set and pins the lock to its own. |
| high | moderation / uploads | Image moderation could be switched off by its own rate limiter: moderate-image answered 429 on quota exhaustion, which supabase-js delivers as a transport error, so both image wrappers' fail-open branch published the image unmoderated to public buckets — the only scanning layer images have | moderate-image now answers 200 `{allowed:false,reason:'rate_limited'}` and both wrappers block via the shared isRateLimited (exported from src/lib/moderation.js + web/lib/moderation.ts); guarded by __tests__/moderationRateLimitFailsClosed.test.js, which drives the real uploadImage() — d974aa6 |
| medium | Hustlr AI / moderation | moderateViaEdge returned allowed for any non-2xx including moderate-text's 429, so exhausting your own per-user moderation quota switched the Claude layer off for create_gig, update_profile and remember | verdict object failing CLOSED on 429 (status or body marker) and OPEN only on 5xx/network/timeout; all three call sites return a distinct rate-limited reply; __tests__/moderationRateLimitFailsClosed.test.js pins both it and the client wrapper — 2d5a8a6 |
| low | assistant | **Confirming a staged booking whose slot was taken meanwhile said "You've already requested this gig."** executeBooking ran from a 10-minute-old payload and read every 23505 as a self-duplicate; the one case where the user has NO request in was reported as though they did, and the sentence was persisted into the thread. | 2026-09-05 (cd11bb4) — executeBooking re-reads job status and the staged slot before inserting and returns listing_closed / slot_taken with the open slots; on a lost race it branches on `bookings_one_active_per_slot` in the error rather than assuming; and the self-duplicate branch will not claim a request exists with no prior row behind it. Guarded by `__tests__/assistantConfirmRace.test.js`, which fails 4/5 on the old body. |
| medium | mobile-core | **Realtime booking toasts fired on the row's CURRENT status, so a user's own non-status writes re-announced old news.** A Supabase UPDATE arrives for any column change on a row the subscriber can see (their own writes included) and `payload.old` carries only the primary key, so `payload.new.status` cannot distinguish "the poster just accepted" from "I stamped started_at on a booking confirmed three days ago". Tapping 'I'm on site' or 'Mark done' re-fired 'Booking Confirmed!'; rating the poster on a verified booking re-fired 'Job Verified! 5★ — paid via card!'; a poster proposing an amendment on a completed booking was told an earner had just marked it complete. `stateRef` already held the previous status and was not read. Web carried the identical logic. | 2026-09-05 (c2f6239) — `enteredStatus(prev, next, target)` in `shared/lifecycle.js`, fed from the status each client already holds (mobile `stateRef`, a new `stateRef` on web), gating every toast on both channels in both clients. An unheld row counts as a transition, because that is a cold start where the event IS the first news. `cancelGigReminder` stays unconditional for finalized bookings (idempotent, and a finalized booking must not keep a pending local notification); `claimEarnerPayment`'s optimistic `verified` now suppresses the duplicate 'Job Verified!' that quoted a rating nobody gave. Guard: `__tests__/realtimeToastTransition.test.js`, 9 assertions, all 9 failing pre-fix. |
| low | mobile-core | **One bookings cache key, `bookings_v1`, served every account on the device.** `loadBookings` read it with no user check and wrote it back AFTER an awaited, unbounded query with no check that the same user was still signed in, while `UserContext` has keyed per user and re-checked `activeUserId` after every await since it was written. Two paths: a query in flight at sign-out completes afterwards (the token stays valid after a `scope:'local'` sign-out) and re-populates the shared key; or `cacheClear()` throws inside AsyncStorage and silently does nothing. Within the 5-minute TTL the next account's My Jobs rendered a stranger's gigs, notes, counter-offers and slot labels, and the Earn badge counted their applications — for the whole session if that account's own fetch errored. | 2026-09-05 (2183b2c) — key is now `bookings_${user.id}` in both clients, which alone closes both paths since a late write lands on its own account's key; an `activeUserId` ref assigned during render (not in an effect, so it does not depend on effect ordering) stops a superseded load dispatching into the next account's state; the legacy shared key is removed on load so an upgrading device stops holding someone else's booking list rather than merely ignoring it. `JOBS_CACHE` deliberately stays global — the browse feed is public data, identical for every account. Guard: `__tests__/bookingsCacheKeyedByUser.test.js`, 5 of 6 assertions failing pre-fix. |
| low | mobile-core | **A push for a gig outside the 200-row browse feed went to nobody, and the same lookup let a blocked poster keep a phantom unread badge.** `bookJob`, `startJob`, `markEarnerDone`, `cancelBooking` and `respondToAmendment` resolved the counterparty via `state.jobs.find(...)?.posterId` alone; every one is an `if (posterId) notify(...)`, so a miss is silent. A three-week-old confirmed booking whose gig has aged out of the feed: the earner marks started and done, both writes land, the poster is told neither and never opens the app to verify — the earner waits until the ghost-claim window. `refreshUnread`'s blocked-party filter used the same lookup, so a conversation on an off-feed gig could never be excluded. The booking's own embed has carried `job.posterId` all along, and two siblings already did a direct lookup for the same reason. | 2026-09-05 (e88cdb2) — `bookingPosterId(booking, jobs)` in `shared/lifecycle.js` is now the single resolver at every site in both clients (feed first, since that row is fuller and survives a gig changing hands; embed second), and `bookJob` falls back to `fetchJobById` so both the self-book guard and the push work off a real job. Two knock-ons fixed with it: `cancelBooking`'s `isPoster` was false for a poster cancelling an off-feed gig, which sent them their OWN 'The earner cancelled' push and skipped the cancellation-fee record — `cancellationFeeFor` moves with it so the confirm dialog cannot quote $0 for a cancel that records a fee. A stale comment claiming the embed 'never carries posterId' is corrected. Guard: `__tests__/notifyTargetFallback.test.js`, 8 of 10 assertions failing pre-fix. |
| high | SupportScreen / uploadImage | Support 'Add a photo' could never attach anything — `if (res?.length)` on pickImages' `{canceled, uris}` object left photos[] empty, killing the upload, the thumbnail strip and the photo-only send, so safety reports reached agents with no evidence | consume the documented shape; new `__tests__/pickImagesContract.test.js` walks every `await pickImage(s)` call site under src/ and fails on `.length`/array-spread consumption (c5b779b) |
| high | Tax Center / income | Platform income valued every hourly gig at ONE hour (computeEffectivePay(b, null) reads hours only from a full job row) and omitted card tips, understating the net-profit headline, the ~27% set-aside and the year-end CSV several-fold | shared/taxFormat.js gains bookingGrossDollars (pinned amount_cents_quoted first) + platformIncomeForYear (netted at each booking's own fee_bps_quoted, tips separate); both Tax Centers and computeEffectivePay's hours fallback fixed; __tests__/taxCenterIncome.test.js — d32ad6a |
| medium | Tax Center / load state | `catch (_) {}` presented a failed expenses/income read as an empty year — 'No expenses yet', Expenses $0.00, a net profit and 27% set-aside ignoring every deduction, and Export still enabled | three states (error / loaded / unknownTotals): error card with Retry, em dash instead of money for unknown totals, empty state gated, Export refused; same fix on web; __tests__/taxCenterLoadState.test.js — 21e0dae |
| low | Tax Center / EarnScreen date default | todayISO() used the UTC date while the year filter uses the local clock, so evening entries in US zones were dated tomorrow and a 31 Dec entry landed in the next tax year | one localDateISO() in shared/taxFormat.js built from local components, used by all four call sites (both Add sheets + both mileage auto-logs); __tests__/localDateDefault.test.js — ed4a9c0 |
| high | CompletionModal / disputes | Verify sheet's dispute 'Add photo' silently discarded every picked photo — `if (res?.length)` on pickImages' `{canceled, uris}` object meant disputePhotos was always [], so the evidence the copy promised support would review was never uploaded | consume the documented `{canceled, denied, uris}` shape + surface the denied case; guarded by `__tests__/disputeEvidencePicker.test.js` (a88abf1) |
| high | mobile-screens-hire-earn | **Conversations on a gig missing from the browse feed vanished from the Messages inbox while the tab badge still counted them.** `MessagesScreen` resolved an earner-side counterparty only via `jobs.find(...)?.poster`; the feed excludes soft-cancelled gigs and caps at 200, so `other` came back null and the row was dropped by the inbox filter, while `refreshUnread` counted the same booking — a badge for a thread the screen would not list, a push deep-linking into 'No messages yet', and no way to mark it read. Reachable both ways: a poster may delete a gig whose only bookings are pending (EditJob's lock covers confirmed/completed/verified only) and then message the applicant from the phantom card on Hire, or delete a finished gig and take the earner's chat history out of their inbox. Web already fell back to `b.job?.posterId`. | 2026-09-05 (d8e960f) — the earner branch falls back to the booking embed through the same `bookingPosterId` resolver `refreshUnread` now uses, so the inbox and the badge cannot disagree about who a conversation is with. A fallback row lists immediately as 'Poster' and is then given its real name and avatar by one batched `profiles` read that fails open — a listed thread beats a hidden one. Blocking now hides such a conversation in both places instead of neither. Guard: `__tests__/messagesCounterpartyFallback.test.js`, its 3 screen assertions failing pre-fix. |
| medium | EditJobScreen / JobsContext.updateJob | Edit gig had no Estimated-hours field and updateJob never wrote estimated_hours, so a flat->hourly edit left the posted multiplier pinning every future booking's escrow at the wrong total | Added the hourly-only field (locked with pay, matching guard_jobs_write's pin) and dbPatch.estimated_hours; guarded by __tests__/editJobEstimatedHours.test.js — 7e475eb |
| medium | GigsScreen.handleVerify / CompletionModal | handleVerify returned undefined on a soft abort (blocked review, booking already finalized, payment not found), which the sheet read as success — closing and discarding the poster's rating, review, tip and dispute selection | Propagate the boolean from both CompletionModal hosts; source contract asserted by __tests__/completionModalResultContract.test.js — f44098b |
| medium | EarnScreen.handleConfirmFinish / GigsScreen.handleMarkDone | Both mark-done callers discarded the boolean, so a rejected write still showed 'Marked Done!', credited challenge c2, fired a success haptic and closed the finish sheet — orphaning the uploaded completion photos | Gate all three call sites on the result, move the success haptic after the await; guarded by __tests__/markDoneResultGate.test.js — 9a21cba |
| low | GigsScreen PastBookingCard | Hire > Past branched only on `declined`, so a cancelled booking printed the literal 'Completed' next to its own Cancelled badge in the poster's history | Key the rating row on `verified` and mute the avatar for cancelled; guarded by __tests__/pastBookingLabel.test.js — ec2e81a |
| high | payments-audit | **Splitting a referral fee credit raised unique_violation on every REAL credit, so the credit was silently never applied — and a partially used one never returned.** `bonus_ledger_dedupe` is unique on (user_id, reason, source_booking_id) where source_booking_id is not null (20260806070000:180) and `accrue_referral_bonus` stamps every referral bonus with the source booking (20260806250000:75), so the accrual key is occupied by the parent row. The original `consume_fee_credit` inserted the split fragment with a literal `null` (20260806080000:199); the 20260814150000 rewrite — reproducing the whole body to add the kill-switch check — changed that one value to `r2.source_booking_id`, and `return_unused_fee_credit` (20260813130000:90-93) was written against the broken shape. Every caller swallows the 23505: `pin_booking_amount`'s `exception when others` is a subtransaction so the whole credit application rolls back to a zero pin, `stripe-capture-payment` logs, `record_refund` warns deliberately. The split path is the ORDINARY path — at 700 bps the headroom is 150c on a $50 gig and 355c on a $100 gig, so the console's $10 preset fits whole only above ~$257. It survived two self-asserting migrations because both probes staged their ledger row WITHOUT a source_booking_id and the partial index does not cover NULLs. | 2026-09-06 (20260906011000, commit 944afc0) — one rule for both functions: the PAYABLE side of a split keeps the accrual key, the APPLIED side does not. `vest_bonuses` reaches a bonus for clawback through `source_booking_id` and only ever touches pending/payable rows, so that lands consume on the insert (fragment gets `null`, the 20260806080000 shape restored) and return on the update (the applied parent gives the key up) — which keeps a returned remainder clawback-reachable, unlike the obvious null-the-new-row fix. Probe stages a credit WITH a source booking, shows the index rejecting the old shape, splits 1000c into 850c payable + 150c applied, returns 410c of an undelivered 765c, asserts which side holds the key each time, rolled back. `__tests__/bonusLedgerSplit.test.js` reads the live definition of each function off disk and fails if a split's two rows can share the key — 3 failures against the pre-fix bodies, naming both. |
| medium | stripe-capture-payment | A partial capture that aborted after the Stripe call lost its disputes row, promo settle and unused fee credit forever — the retry hit an already-'captured' row, skipped the block that assigns capturedGigCents, and returned success | Retry re-derives the settlement from the ledger (earner_amount_cents + fee_cents vs amount_cents), never the caller's pct; return_unused_fee_credit extracted for a second call site; new ctl_partial_capture_without_dispute — efdb990 |
| medium | stripe-create-payment-intent | payments.status was written 'authorized' at PaymentIntent CREATION, before any card was authorized, so an abandoned pay sheet left a permanent phantom hold — booking never expired, slot never freed, and a CRITICAL 'hold lapsed' control firing on a hold never placed | New pre-authorization status 'pending'; promotion only on an observed requires_capture (accept-booking + new payment_intent.amount_capturable_updated handler, subscribed via reconcile-stripe); new ctl_hold_never_confirmed_on_live_booking — c280f0d |
| medium | admin-payment-action | The settle op read the fee from the MUTABLE payments.fee_cents, which a failed partial-capture attempt leaves reduced — so the ledger and credit_earnings overstated the earner's payout and understated platform revenue, invisibly (the two values still sum to amount_received, so reconcile-stripe and ctl_earnings_total_drift both pass) | Fee now taken from latest_charge.application_fee_amount, falling back to platform_fee_after_credit over the pinned inputs; same rule applied to stripe-webhook's succeeded re-derivation — f26387a |
| medium | stripe-webhook/recordReversal | A refunded or charged-back TIP filed a booking-level disputes row with the escrow template, so ctl_external_reversal_not_ledgered attributed it to the ESCROW payment and opened an unresolvable HIGH finding whose printed remedy would have written refunded_cents onto a charge Stripe never touched | recordReversal returns after record_tip_reversal (tip_ledger IS the record); ctl_external_reversal_not_ledgered redefined to exclude a reversal whose exact amount a reversed tip on that booking accounts for — d3f9047 |
| medium | stripe-tip | **The tip idempotency key is released in our database and kept 24h at Stripe.** `reserve_tip_slot`'s slot key `resv_<booking>_<cents>` was also the Stripe idempotency key. Releasing it at confirm frees our unique index but not Stripe, which replays the original response for 24h — so a second legitimate tip of the same amount (the count cap allows three) returned the FIRST PaymentIntent, `confirm_tip_charge` found the already-credited first row by PI, and stripe-tip answered `{success:true}`: no charge, no credit, the earner notified anyway, and a stranded reservation that `ctl_earner_credit_missing` could never clear. Also replayed a decline for 24h and made a retry with a replaced card an `idempotency_error`. | 2026-09-06 (20260906014000, commit 0c070f3) — the Stripe key is now the reservation ROW (`resv_<row id>`, returned as `stripe_key`) while `reservation_key` stays deterministic per (booking, cents), because the unique index on it is the double-tap collapse. Probe: confirm a tip, reserve the same amount again, slot keys identical (the failure) and Stripe keys not; release-then-reserve gets a third key; the control prints the row's key and carries the legacy form for older rows. |
| low | stripe-webhook/payout.* | **A failed payout write was answered 200 and the earner told the money arrived.** The handler throws on a failed `stripe_accounts` lookup deliberately, but `if (poErr) console.error(...)` swallowed a failed `stripe_payouts` upsert and fell through to the 'Money landed in your bank — $X has arrived' notification and a 200, so Stripe never redelivered. The Bank deposits sheet kept showing in_transit with the estimated date, and `ctl_payout_overdue` then reported a deposit that had landed as overdue. | 2026-09-06 (commit 017d99e) — throws on `poErr`, mirroring the account-lookup branch, so Stripe redelivers (safe: `guard_stripe_payout_ordering` drops a stale write on `last_event_at`) and the notification cannot outlive its row. The file's remaining 'needs a human' `console.error` lines, including the terminal 500 catch, now go to `logServerError` → `/errors`. Guarded by five assertions in `webhookEventCoverage.test.js`. |
| high | parity | **The safety pager and the admin console never surfaced the exact address.** `raise_gig_emergency` exists so a worker at a stranger's home can page a human with one tap, but safety-alert emailed only names, ids, details and a `/moderation` link; `ctl_safety_checkin_overdue`'s detail carried `j.location`, which `trg_mask_job_location` masks to city level at write; and the console read `job_locations` nowhere, so RUNBOOK_SAFETY §1 walked the on-call through three pages and never reached a street address. All three surfaces already held the service-role/DEFINER access to `job_locations` and none used it, while `view_gig_share` hands the same address to whoever the earner sends a token to. | 2026-09-05 (d4448b2, migration 20260905003100) — safety-alert emails "Where:", `started_at` + an in-progress marker and links `/bookings/<id>`; the control LEFT JOINs `job_locations` and emits `exact_location` + `exact_location_known`; `/bookings/[id]` grows a Safety section (address, work-started, check-in timer) gated `roleSatisfies(ctx.role, "trust")` and recorded in `admin_audit_log` with `exact_address_shown`; RUNBOOK_SAFETY §0/§1 updated. Blast radius handled in the same change: `controls-alert` now strips `exact_location` from the Claude triage payload, so widening the detail does not begin mailing home addresses to an LLM daily. Migration probe stages a masked gig and proves detail.exact_location ≠ detail.location, that a city-level gig still reports flagged unknown, and that the earner_done false-page exclusion survived; `__tests__/safetyAddressReachable.test.js` fails 15/16 on the pre-fix source. **Needs `db push`, `functions deploy safety-alert` + `controls-alert`, and `cd admin && npx vercel --prod --scope go-hustlr`.** |
| medium | parity | **The web app had no Transactions ledger at all** — no receipts, refunds, pinned fee, CSV or bank deposits — and Hustlr AI sent web users to it anyway. `web/lib/payments.ts` did not exist, there was no `/transactions` route, and web read neither `payments` nor `stripe_payouts`; the web Tax Center said so itself in a comment. The assistant builds ONE prompt with no platform argument and its widget is in the web app layout, so a web user was told to open a screen that did not exist on their client — from a prompt whose own rule is "Never invent a screen". | 2026-09-05 (30928ae) — the PURE ledger logic moved to `shared/ledger.js` (twelve functions), leaving `src/lib/payments.js` and the new `web/lib/payments.ts` as the two Supabase reads plus a re-export; a second hand-ported copy of money maths whose fee is pinned per booking and whose refund share differs by side was the thing to avoid. `web/app/(app)/profile/transactions/page.tsx` carries both sides, filters, the six-month trend, per-transaction receipts at the booking's pinned rate, CSV export and Bank deposits; reached from Settings → Money, `/profile/payouts`, `/my-jobs` and `/hiring` (the last deliberately closing the mobile GigsStack gap rather than inheriting it). `__tests__/webLedgerParity.test.js` fails 5/25 on the pre-fix tree and pins the arithmetic to exactly one definition; `ledger.test.js`'s on-disk guard repointed and hardened. CLAUDE.md's Transactions section rewritten. **Ships OTA + web auto-deploys on push.** |
| medium | parity | **Signed-in web support was a `mailto:` — the two-way ticket thread was mobile-only, and an emailed reply could not re-enter the ticket.** Settings and Profile both opened a personal inbox, `/contact` was linked only from the marketing footer, and `grep -rn support_ticket web/` returned nothing, so a web request never became a triageable ticket and a web ticket could never be read back. `support-reply` mails with `reply_to = mainmail@` and there is no inbound-mail ingestion anywhere, so a user's reply leaves `last_author` unmoved, the reopen rule unfired and `ctl_support_ticket_unanswered` blind — and an agent's attachment is a signed URL into the private `support-photos` bucket inside an app the user never installed. RUNBOOK_SAFETY claimed tickets already landed at `/support` from "mobile + web". | 2026-09-05 (0b2a6e4) — `shared/support.js` now holds `SUPPORT_CATEGORIES` + the thread-selection rules, re-exported by both clients so one person cannot get two different active conversations on two devices; `web/lib/support.ts` + `web/app/(app)/support/page.tsx` are the web conversation (support-submit for the first message, reads of both tables, reply/archive/resolve, signed `support-photos` attachments, reply-reopens-a-resolved-thread stated on screen); Settings and Profile point at it and `/messages` pins a GoHustlr Support row above the gig conversations. `/contact` untouched for signed-out visitors. RUNBOOK_SAFETY §0 rewritten to say **reply in the console, never by email**, with the reason. `__tests__/webSupportParity.test.js` fails 19/20 on the pre-fix tree. **Ships on the next web push; no app or DB change.** |
| low | ops / controls | ctl_external_reversal_not_ledgered assumed the newest disputes row carried the cumulative refund, but recordReversal wrote one row per charge id and never updated it, so a second external refund on the same charge was invisible once the first was ledgered | recordReversal now moves the row's figure forward when Stripe's cumulative total does — refund-only, forward-only, over its own template only (ed1bbac) |
| medium | tips / reversal-control | **A refunded or charged-back TIP filed a `disputes` row on the GIG, and the control's remedy told the operator to corrupt the gig ledger.** `recordReversal`'s tip fallback (f9b2550) set `bookingId` and fell through; the skip logic below reads the `payments` row, which does not exist for a tip, so the insert was unguarded and the row carried the tip's charge id in the gig machine template. `ctl_external_reversal_not_ledgered` anchors on that template and joins `disputes` to `payments` on `booking_id`, so the gig's captured, never-refunded payment was reported as carrying an unledgered reversal, with a printed remedy ("Record chargeback") that writes `refunded_cents` onto a charge nobody refunded — misstating the poster's receipt and making `vest_bonuses` void any referral bonus sourced from that booking. The open row also blocked `earner-claim-payment` and held bonus vesting. With no console path to refund a tip, the Stripe Dashboard is the only way one gets reversed, so this fired on every tip reversal. | 2026-09-06 — the tip branch now returns its own `booking_id` before the insert is reachable (the admin email still fires); `20260906022000` leaves the control's predicate untouched — a suppressed reversal is the worse failure — and changes only the `remedy` when the figure exactly matches a reversed tip on the same booking, naming `tip_ledger`, saying which Stripe id to check, and withholding the Record chargeback instruction. Rolled-back probe proves both halves; `tipCaps` and `refundAttribution` guard the writer and the two-branch remedy. Commit `1156d4f`. |
| medium | money / incentives | record_refund valued the earner's fee credit from the full authorization and the discount-net fee, so a partial refund on a partially-captured or poster-discounted booking returned too little credit — usually zero | public.fee_credit_delivered_cents() is now the one definition, called by record_refund and stripe-capture-payment alike; probe stages a 50% capture and a 305c-discounted capture (942ead1) |
| low | tests / controls | reversalReasonParity.test.js guarded only the ILIKE prefixes, not the anchored regexes that are the authenticity half of ctl_external_reversal_not_ledgered, so a webhook template change could blind the control with the suite green | test now extracts and asserts the `~` patterns, requires one per arm, and proves a typed note passes the prefix and fails the anchor (0713fbf) |
| medium | storage/RLS | **An avatar or gig photo could be any URL on the internet, rendered to everyone.** `profiles.avatar_url` and `jobs.photos` were owner-writable free text (no guard column, no CHECK), and image moderation only ever sees uploaded bucket objects — so a direct `PATCH /rest/v1/profiles {"avatar_url":"https://attacker.tld/x.jpg"}` put an unmoderated external image beside every gig that user posted, in every chat row, and on their public profile, and handed the attacker's host the IP and UA of every viewer. The same class was already closed for `certifications.image_url` (`safeCertUrl`) and never applied to the two visible columns. | 2026-09-06 (20260906014100, commit 7a586b1) — `guard_profile_avatar_url` / `guard_job_photo_urls` require an object under the writer's own folder at this project's storage origin (`app_flags.storage_public_origin`, pinned to the project, not `*.supabase.co`); `ctl_foreign_image_url` reports pre-existing and service-role-written rows; `safeStorageUrl` (shared/transforms.js) drops the rest at render in `Avatar` on both platforms and in `transformJob`. Probe drops the triggers, lands the external URL live, restores them and shows the same PATCH refused, then proves genuine `getPublicUrl` writes still pass. Controls 58 → 59. |
| low | rls-policies | **The accused earner had no read path to the dispute evidence against them.** 20260806360000 put the poster's dispute photographs in the private completion-photos bucket and justified reusing it with "completion_party_read already lets EITHER booking party read it — so the earner can see what they are accused of, which they must be able to do." The policy never looked at `disputes.photos`: its non-owner branch only unnests `bookings.completion_photos \|\| before_photos`, and stripe-capture-payment writes the photos under the POSTER's own `<posterId>/` prefix onto the dispute row. So the earner failed the owner branch (wrong folder) and the party branch (not in either bookings array), and the console renders `disputes.photos` nowhere — the only person who could open the evidence was the poster who wrote it. | 2026-09-05 (20260905004000, commit 323a4ae) — a third branch on `completion_party_read`, joined disputes → bookings → jobs and filtered on the caller being the earner or the poster, mirroring the existing branch including the legacy URL form. Probe stages a real booking + dispute and runs the OLD test and the NEW one against the same object as the same earner: old false, new true, non-party false. `__tests__/storagePolicies.test.js` asserts the replayed final policy references `disputes`; it fails with the migration moved aside. |
| low | docs-tiers | **The safety runbook told the on-call to escalate a report they were allowed to resolve.** `RUNBOOK_SAFETY.md` §1.6 said "Only `admin` role can resolve" while `moderation/actions.ts:19,43` have gated resolve AND reopen at `requireAdmin("trust")`; `ROLE_PERMISSION_MATRIX.md` carried the same claim. The `trust` tier exists precisely so an open report does not withhold an earner's payout waiting for an admin, so a trust-tier teammate believing the runbook re-creates the money-harm delay the tier removed. | 2026-09-06 — both docs corrected to `trust` or `admin` with the two call sites cited; the ROLE_PERMISSION_MATRIX row marked SUPERSEDED with a correction block rather than rewriting a dated two-tier snapshot. A new section in `__tests__/adminSurface.test.js` reads the tier out of the action file and fails if the runbook stops naming it. Commit `df4f3bc`. |
| high | safety-ops | **An SOS or overdue check-in paged a human who could not be told where the worker was.** jobs.location is masked and the exact address sits in job_locations; ctl_safety_checkin_overdue emitted the masked label, the safety-alert email carried no location at all, and admin /bookings/[id] — where RUNBOOK_SAFETY §1 sends the on-call — rendered no location, masked or otherwise. All three run as service_role / SECURITY DEFINER. | 2026-09-05 (39a97b6, migration 20260905002100) — the control LEFT joins job_locations and emits exact_location beside the masked one (left, so a remote gig is still reported); the alert email carries gig title, exact address and booking state, leads the subject with EMERGENCY for source='emergency' and links /bookings/<id>; the console page shows Where + Started and audits the disclosure as booking.exact_location; /moderation shows an emergency pill. Probe stages a street-address gig and proves the finding names the place, and that a gig with no stored address does not fall out of the control. |
| medium | safety-ops | **The check-in "nudge" was never built — the first thing that happened was a critical page.** safety_checkins.nudged_at / escalated_at were written by nothing; the earner was never asked, and the share page told a worried friend GoHustlr checks in automatically, which was false. | 2026-09-05 (26bddc6, migration 20260905002200) — run_safety_checkin_stages() is the first step of the hourly sweep: an Alerts-inbox notification to the earner + nudged_at, then escalated_at 30 minutes later if still open. ctl_safety_checkin_overdue fires on escalated_at OR on any check-in >3h past due (the backstop, so a broken nudge cannot turn the board green) and carries a `stage` field. Share-page copy corrected. Delivery is IN-APP ONLY — see the open row below. |
| medium | safety-legal | **The Terms and Privacy Policy promised the earner could revoke a gig share link "at any time"; no client offered revocation.** The schema supported it (gig_shares_revoke_own) and nothing ever wrote revoked_at, so a link carrying the poster's exact street address, both first names and live status could only end by expiring. | 2026-09-05 (f86943d) — SafetyBar shows "Stop sharing my location" whenever a live link exists and revokes every live row for the booking, with a confirm that says what stops; the share button reads "Share again" once a link is out. A parity assertion fails if any client that calls create_gig_share has no path to revoked_at, pinned to the legal text that makes the promise. Hustlr AI's prompt now names the control (dea1ebd). |
| medium | safety-alerting | **The safety pager answered 200 when it could not send.** `safety-alert` returned `{ok:true, emailed:false}` / HTTP 200 whenever `RESEND_API_KEY` was unset, so a config state in which no harassment or assault report is ever emailed was invisible: `ctl_alert_dispatch_failing` reads `net._http_response` for a NON-2xx and `ctl_alert_not_dispatching` reads `app_flags`, neither of which can see a clean 200. The stated reason ("never wedges the trigger") did not hold — `notify_safety_report` posts through asynchronous pg_net inside `exception when others then raise warning`, so the status code never reaches the insert. `controls-alert` had the same branch, so the daily digest would go quiet the same way. This is the 2026-07-10 shape: four weeks of a dead safety channel above a green board. | 2026-09-06 — both no-transport branches now answer 503 `email_not_configured`, matching the same function's own missing-secret path, so the failure lands in `net._http_response` and `ctl_alert_dispatch_failing` opens a high finding within the hour; `controls-alert`'s `nothing_new` 200 deliberately kept. Guarded by a new suite in `__tests__/safetyAlert.test.js` that reads the branch out of both files. Commit `0bc8a55`. |
| medium | web-safety-parity | **The website could start a gig but not raise the alarm on one.** `web/lib/jobs.tsx` `startJob` writes `bookings.started_at` and `my-jobs` offers "Start job · I'm on site", but no file under `web/` called `create_gig_share` or `raise_gig_emergency` — an earner on a phone browser with no TestFlight build stood at a stranger's address with no way to tell anyone where they were and no SOS, while the public `/s/[token]` page had existed on web the whole time. The server half was never one-sided: `open_safety_checkin` fires on any `started_at` write, so the check-in timer, nudge and escalation always covered web. | 2026-09-06 — `web/components/SafetyBar.tsx` ports both controls onto the started-gig card, keeping mobile's deliberate weights (share unconfirmed, SOS confirmed) and server-side token minting. Hustlr AI's prompt now names both. Guarded by a both-clients section in `__tests__/parity.test.js` plus a `MUST_KNOW` row, so the next one-sided safety control fails the gate. Commit `f576ab6`. |
| low | tests | parity.test.js's 'names the "You" tab' assertion could never fail — the prompt slice begins with 'You are **Hustlr AI**' | labels now checked word-bounded against the prompt's tab sentence, plus assertions that the line exists and carries no retired tab name (3f75a1f) |
| low | parity | **The fourth copy of maskLocation still failed open on "remote".** 20260726030000 deleted the `includes('remote') -> return label` shortcut from src/lib/address.js, web/lib/address.ts and public.mask_location because it matched inside real street addresses and published them in full. The assistant edge function kept it, under a comment claiming to be a mirror of address.js, and no test read it. Dormant (its inputs are pre-masked; the exact-address path skips maskLocation) but it is the documented leak sitting in the one consumer that already selects job_locations.exact_location. | 2026-09-05 (d7dc1c1) — branch removed and the NOTE copied across. __tests__/address.test.js now extracts the assistant's body off disk, strips the TS annotations and runs it against the same cases as the mobile copy; re-adding the shortcut fails 3 of the new tests. **Needs `supabase functions deploy assistant`.** |
| medium | web-parity | **The web Messages badge counted unread messages from people you had blocked, permanently.** `refreshUnread` (web/lib/jobs.tsx) had no blocked-party filter while the hub hides those conversations, so a message unread at the moment of the block could never be marked read — and `unblockUserDb` has zero callers on the web, so nothing cleared it. Mobile had fixed exactly this; the port never received it. | 2026-09-06 (c148051) — a `counterparties` memo resolves each booking's other party the way the Messages hub does and refreshUnread filters those ids, with `blockedIds` in the deps so a fresh block re-counts at once. `__tests__/webUnreadBlockedParity.test.js` asserts the filter, the dep and the two-sided resolution on BOTH clients; it fails 4 of 10 against the pre-fix file. |
| medium | data-integrity | **The Tax Center valued every hourly gig at ONE hour and ignored the pinned amount and tips — in the figure exported to an accountant.** `computeEffectivePay` read hours only from its optional `fullJob`, which the Tax Center never passes, so a 4-hour $25/hr gig counted as $25 (~4x understatement); `amount_cents_quoted` and `tip_amount` were never consulted. Both files claimed in a comment that the embed had no estimatedHours — false, JOB_MINI selects it. | 2026-09-06 (ac1ad0b) — fixed on BOTH clients: computeEffectivePay falls back to `booking.job.estimatedHours`, and each Tax Center takes gross from the pinned amount (list pay as the pre-pin fallback) and adds the tip whole. `__tests__/taxIncomeHourly.test.js` evaluates the real function body out of each client; it fails 7 of 15 against the pre-fix tree. |
| medium | money | **The Verify and Accept sheets quoted dollar figures that ignored the booking's pinned poster discount and fee credit.** The server authorizes `amount − poster_discount_cents` and pays `amount − platform_fee_after_credit(amount, bps, fee_credit_cents)`, but `transformBooking` exposed only 2 of the 4 pins, so the web Verify sheet overstated the hold and understated the payout ($186.00 shown where $193.65 is paid) while calling it "the amount you already authorized" — and BOTH clients' accept paths rendered the edge function's pre-discount `amountCents` as the hold. | 2026-09-06 (0d93c59) — all four pins surfaced through `transformBooking`; `platformFeeAfterCreditCents` / `earnerNetAfterCreditCents` / `posterChargeCents` added to `shared/pricing.js` mirroring `platform_fee_after_credit` and the edge function's `authorizedCents`, which is now returned on both response paths; the "we keep N%" label is dropped whenever a benefit is pinned. `__tests__/benefitDisplay.test.js` matrix-compares the mirror against the parsed SQL and fails 9 of 11 pre-fix. |
| medium | web-parity | **gohustlr.com had no Transactions ledger while Hustlr AI's shared prompt told every user to open "Payments & payouts → Transactions".** The web never read the `payments` table and had no ledger route, so a web-only earner had no receipt, no pinned fee rate, no refund record and no deposit date, and a web-only poster no record of being charged. `parity.test.js` checked the prompt named the destination, not that it existed. | 2026-09-06 (f6aa299) — the pure ledger (wording, `toEntry`, filters, stats, CSV, `receiptLines`) extracted to `shared/ledger.js`, which both clients import; `src/lib/payments.js` re-exports it and keeps its queries, `web/lib/payments.ts` adds the web queries, and `/profile/payments` is the screen, linked from `/profile/payouts` and the settings hub. `parity.test.js` now requires the SURFACE on both clients (6 assertions fail with the web half removed), and the 71-test ledger suite passes unchanged against the extracted module. |
| low | payments-audit | **The loyalty ladder was farmable by two accounts.** `earner_completed_count` is a bare `count(*)` of verified bookings and `tier_fee_bps` picked the highest rung it cleared — nothing looked at WHO the counterparty was. One person with two accounts could book, accept and verify fifty times and hold the Veteran rate permanently. Unlike the referral system, which vests on outcome and charges a real fee every loop, a fee TIER is a permanent rate cut: the farming cost is paid once and the discount never stops. | 2026-08-17 (20260817040000) — `fee_tiers.min_distinct_posters` plus `earner_distinct_posters()`, both required by `tier_fee_bps`. Rungs set to 3 / 6 / 10 strangers against 10 / 25 / 50 gigs. The standard anti-collusion control: the attacker's cost stops being "repeat the loop" and becomes "recruit N unrelated people", which is indistinguishable from building a real reputation. Done NOW because all three rungs still ship `enabled = false` — rates pin per booking, so tightening after a rung goes live would never reach the bookings already struck at the loose threshold. Probe: 50 verified gigs from ONE poster now buys no rung, the old count-only rule WOULD have granted it on the same rows, and a genuine earner across 11 counterparties still gets 700 bps. |
| low | payments-audit | **The platform pays Stripe's 2.9%+30¢ on every tip and takes nothing — decision needed.** `stripe-tip` sets `transfer_data.destination` with no `application_fee_amount`, so 100% reaches the earner and processing comes out of the platform's pocket. | 2026-08-17 — **DECIDED: keep it, deliberately**, recorded as KNOWN_RISKS T-1 and in CLAUDE.md's tips bullet with a do-not-"fix" note. Passing tips through whole is the industry norm — DoorDash, Instacart, Uber and Lyft all route 100% to the worker, and every attempt to do otherwise became a public scandal rather than a revenue line. On a platform selling "keep what you earn", a skimmed tip is the most expensive dollar available. The cost is bounded and an order of magnitude under the fee revenue on the gigs the tips attach to. Revisit only if tip volume grows large relative to gig volume, which would itself mean the rail is being used for something other than tipping. |
| medium | tooling | **This row asserted a mechanism that is FALSE, and I wrote the false version.** It said "a local iOS build MUTATES node_modules/react-native-maps", stated as confirmed after builds 29/30 and 32. Measured 2026-08-17: `find node_modules/react-native-maps -newermt '-90 minutes'` returned ZERO touched files while the hash sat drifted, and `npm pack` + `diff -rq` showed a clean install byte-identical to the published tarball. Nothing is written into the package. Two confident guesses — the 08-14 note's and mine repeating it — were both wrong. | 2026-08-17 — superseded by `scripts/preflight-eas-build.mjs`, which guards the observation without needing the cause. The evidenced theory now recorded in CLAUDE.md: the hash IS content-sensitive, the clean tree matches the tarball, and no file was modified — reconciling only if the drifted tree was MISSING content, likely npm pruning. Stated as a theory, not a finding, with the capture procedure for turning it into proof next time. |
| high | tooling | **A fingerprint drift has failed three EAS builds across two days while the CLI exited 0, and the same drift silently mis-targets `eas update` so an OTA reaches nobody and nothing errors.** Always one source, react-native-maps, always the same two hashes. | 2026-08-17 — `scripts/preflight-eas-build.mjs`, run before every build and OTA, wired into CLAUDE.md's command block. Fails loudly with the remedy, fails loudly when it cannot measure (a preflight that shrugs turns "I don't know" into "go ahead"), and refuses rather than blessing a mismatch when the package version moves. Verified both directions: silent on a clean tree, fires on a perturbed one, and `--expect-runtime` exits 1 on a wrong hash. **Two defects found in my own first version and fixed:** it used `@expo/fingerprint .`, which is a generic hash and NOT the runtime — EAS resolves per platform, and `expo-updates fingerprint:generate --platform ios` reproduces build 34's runtime byte for byte while the other tool said something else entirely. On a preflight that is the worst kind of wrong: authoritative-looking and off. **The diagnosis also moved from guess to evidenced theory:** the hash is content-sensitive (adding one file moves it), a clean install is byte-identical to the published tarball (`npm pack` + `diff -rq` silent), and zero files were modified in 90 minutes — which reconciles only if the drifted tree was MISSING content, likely npm pruning, not a build writing anything. Two earlier confident claims about the mechanism were wrong and are corrected in CLAUDE.md. |
| medium | payments-audit | **A campaign benefit held by an application nobody accepted was invisible to every control.** `ctl_benefit_never_settled` (20260806220000:295) filters to bookings in `('verified','declined','cancelled')` — and a benefit consumed at APPLY sits on a booking that is `pending`, which matches none of them. So the one state where a poster's single discount is held hostage by a stranger's application was precisely the state nothing watched. It self-heals only when `expire_stale_pending_bookings(14)` runs, by which point a campaign can be over. | 2026-08-17 (20260817030000) — `ctl_benefit_held_by_pending`, registered, fires at **48 hours** rather than the sibling's 2 because a pending booking is a live state a poster is legitimately still considering and 2h would fire on every normal application (the permanent-noise failure fixed three times here already). The probe proves the gap is real by running BOTH controls on the same staged row: the new one returns 1, `ctl_benefit_never_settled` returns 0. It also proves the finding is resolvable in both directions — released closes it, settled never opens it. Detection only; the underlying consume-at-apply defect stays open with its reason recorded. |
| high | release | **OTA delivery was blocked by a runtime-fingerprint drift, and my diagnosis of it was wrong.** I recorded that the gitignored `ios/` directory had drifted after a local `xcodebuild`, and prescribed `expo prebuild --clean`. The actual cause, from build 32's phase log, was one package: `node_modules/react-native-maps`, local `2f21a192…` vs EAS `bd7bcd0a…` — the same package and the same two hashes as the 2026-08-14 failure. The `ios/` theory was plausible, untested, and would have cost a prebuild and a pod install for nothing. | 2026-08-17 — `rm -rf node_modules/react-native-maps && npm install --legacy-peer-deps` restored the hash to EAS's exactly, and build 33 then went through with **no mismatch warnings**. Local fingerprint and build 33's resolved runtime are both `4b96f3e924c3fd27ba6b8ab82091569a2bd722f6`, so OTAs land again. The lesson is the one the register already carried and I did not follow: **read the phase log, do not guess** — it printed the exact diff both times. |
| medium | admin-console | **A console session never expired.** AAL2 proves the factor was satisfied at SOME point; Supabase rotates the refresh token indefinitely, so a session holding the service-role key stayed live for as long as the laptop it was opened on. Step-up already time-boxed money and privilege actions to 5 minutes, but nothing bounded the session itself — an unattended machine was still an open console the next morning. Raised by Chris 2026-08-17, who asked whether it should auto-expire or stay as is. | 2026-08-17 — a **12-hour re-verification cap**, chosen by Chris from four options. `SESSION_MAX_AGE_SECONDS` in `lib/guard.ts`, enforced in `requireAdmin` so it applies to every request rather than only to actions that touch money, and failing closed on an unreadable `amr` exactly as `requireFreshAdmin` does. Re-verification, NOT sign-out: signing someone out mid-task loses their place and teaches them to resent the control, whereas six digits costs seconds and keeps them where they were. Server-side by design — a browser timer looks like security and is defeated by a client that ignores it. Reuses `mfaAgeSeconds` so there is ONE definition of "when did this person last prove who they are", and leaves the 5-minute step-up window alone: the two answer different questions ("is this still the same day?" vs "is the person at the keyboard right now the one who passed the factor?"). Two traps handled: `stale_mfa` previously fell through `requireAdminPage` to `/denied`, so a correct current admin would have been told they are not authorized — it now redirects to `/mfa?reauth=1`; and the gate honours that flag, because `/mfa` short-circuits an aal2 session straight back to `/` and the two would otherwise bounce forever. The flag is read off `window`, not `useSearchParams`, since the route is statically rendered and that hook demands a Suspense boundary at build time. 7 new assertions, 5 failing pre-fix. Deployed. |
| medium | mobile | **Four text fields sat under the keyboard, including the one you type a review into.** Reported 2026-08-17: "when rating a poster I can't see what I'm typing." The app has TWO keyboard mechanisms and they are not interchangeable — `KeyboardDoneBar` DISMISSES the keyboard (for multiline and numeric inputs with no return key), `KeyboardAvoidingView` stops it COVERING the input. JobDetailScreen's rate sheet had the first and not the second, so the keyboard could be dismissed but never got out of the way, and "it already handles the keyboard" was true and useless. EarnScreen's rate sheet — the SAME flow, rendered twice — has had the avoider since it was written. A sweep found three more: SettingsScreen's promo-code sheet (bottom-anchored, field is the last thing in it), ProfileSettingsScreen's delete-confirm dialog (centred, but a centred card is laid out against the FULL screen height so its lower half — the "type DELETE" field and the button — goes under the keyboard on a short phone), and SecurityScreen's verify/disable cards, which render below the status card with no `automaticallyAdjustKeyboardInsets` on the scroll body. | 2026-08-17 — all four fixed with the pattern the rest of the app already uses: `KeyboardAvoidingView behavior={Platform.OS === 'ios' ? 'padding' : undefined}` on sheets, `automaticallyAdjustKeyboardInsets` on scrolling bodies. New `__tests__/keyboardAvoidance.test.js` walks every file under `src/`, extracts each `<Modal>…</Modal>` block, and fails naming any that contains a `<TextInput>` without a `<KeyboardAvoidingView>` — KeyboardDoneBar explicitly does not count. It asserts it found at least 5 modals so it cannot pass vacuously, and it found the ProfileSettings one I had missed by hand. 3 assertions fail against the pre-fix tree. **Verified on the simulator with the software keyboard actually raised** (the sim defaults to hardware-keyboard mode, where the bug is invisible — `defaults write com.apple.iphonesimulator ConnectHardwareKeyboard -bool false` and restart the Simulator app): the sheet lifts clear and both the field and the button stay visible. |
| medium | tooling | **`expo run:ios` cannot target a simulator on this Mac — but the register's claim that raw `xcodebuild` also fails was WRONG, and it cost a build.** The row said xcodebuild "fails (missing React C++ vtable symbols) because it lacks the prebuilt-framework env expo sets up" and concluded "native rebuilds are currently blocked on this Mac". Acting on that today, I treated a stale SDK 54 simulator binary as unfixable and routed around it. CLAUDE.md had the correct command all along; the two documents disagreed and I believed the wrong one. | 2026-08-17 — **verified by doing it**: `LANG=en_US.UTF-8 LC_ALL=en_US.UTF-8 xcodebuild -workspace ios/GoHustlr.xcworkspace -scheme GoHustlr -configuration Debug -destination "platform=iOS Simulator,id=<udid>" build` → **BUILD SUCCEEDED**, installed with `simctl install`, launched, JS bundle loaded with no version mismatch, and Settings → Security renders correctly on SDK 55. Preconditions that made it work and are worth checking first: `Podfile.lock` already at React-Core 0.83.10 (pods current for SDK 55), `ios/build/generated/ios/ReactCodegen` intact, and no `.DerivedData` anywhere under `node_modules`. The vtable failure was most likely the polluted-node_modules state recorded in the sibling row, which has since been cleaned — not a missing env. `expo run:ios` remains broken for simulators; use xcodebuild, as CLAUDE.md says. |
| medium | web | **Passing the MFA code dropped you on the marketing page, which looks signed out.** `/` is `web/app/page.tsx` — the signed-out marketing hero, with no session check — and `web/app/mfa/page.tsx` navigated there in ALL FOUR of its `router.replace` calls, while login, onboarding, consent and the `(app)` layout all land on `/browse`. It was the only one of the five that did not. Reported live 2026-08-17: *"typed in MFA, it took me back to home screen, but when I clicked sign in it took me right back logged in"* — the second half being `/login:49` noticing the session the user already had. A gate that lands you somewhere indistinguishable from signed-out is a gate people conclude did not work, and the natural next move is to try signing in again. | 2026-08-17 — one `HOME = "/browse"` constant, all four navigations. Guarded by a new `parity.test.js` assertion that sweeps every web auth gate for a bare `router.replace("/")` and fails naming the offender; it flags the pre-fix file and passes clean now. Deployed. |
| **high** | web | **The web could FORCE you through two-factor and could not help you manage it.** `web/app/mfa/page.tsx` has challenged every web sign-in since it was written, and there was no page anywhere on gohustlr.com to turn 2FA on, mint recovery codes, or turn it off — the only surface that could was a CURRENT build of the mobile app, which is exactly the device someone locked out of their authenticator may not have. Found 2026-08-17 the hard way: an admin needed recovery codes, the simulator's dev client was a stale SDK 54 binary (native 0.81.5 vs Metro's 0.83.10 — the known `expo run:ios` wall), and the honest answer from a laptop was "you cannot, from anywhere". | 2026-08-17 — `web/lib/mfa.ts` + `web/app/(app)/settings/security/page.tsx`, mirroring `src/lib/mfa.js` and `SecurityScreen`: enrol, recovery codes minted AT enrolment, regenerate, and disable behind a current code. The QR is PRIMARY here, the reverse of mobile — a phone cannot photograph itself so the app leads with the otpauth:// deep link, but on a laptop the second screen is the whole point. Enrols under the SAME friendly name (`GoHustlr`) the app uses, so /team's surface column keeps meaning what it says. Carries mobile's hard-won details: `listFactors` fails closed, "factor on but codes failed" is its own message rather than a generic error, and the codes are SHOWN before `confirm_mfa_recovery_codes` retires the previous set. Row added to the settings hub with live on/off state. `parity.test.js` gained a block asserting BOTH clients can manage 2FA and not merely enforce it — 11 new assertions; the suite fails outright without the page. Also fixed `PageHeader`'s JSDoc, which promised a boolean `back` the type forbids and the body would have rendered as a link to undefined. Gate: 69 suites, 1286 tests. |
| medium | admin-console | **The human confirmation that guards the console lived entirely in prose, and nothing noticed when it was owed.** 20260804030000 built pending→active around one instruction — activate only after you have "confirmed the enrolment time with them directly, [because] the sign-in page will enrol a factor for whoever knows the password, so that confirmation is the actual control." Two holes fell out of it on 2026-08-17: (a) **nobody is told the confirmation is owed** — an invited admin enrolled and then sat at a denial screen until he texted the founder, because a pending row is visible only to whoever happens to open /team; that is how a real person concludes the tool is broken and starts making second accounts, which he offered to do in writing. (b) **The confirmation left no trace**, so a factor enrolled a week after activation is indistinguishable from the one that was vouched for — which is exactly what a stolen password buys. | 2026-08-17 (20260817020000) — `admin_users.factors_confirmed_at`, stamped by Activate (which IS the confirmation) and by a new `confirmAuthenticators` action that audits WHAT was vouched for, not merely that someone clicked; cleared by Reset and on the way out of active. Two registered controls: `admin_pending_enrolled` (pending + enrolled + waiting 12h, so a same-day activation never pages) and `admin_unconfirmed_factor` (active + any verified factor newer than the stamp; a NULL stamp counts as unconfirmed, since failing open would silence it on the accounts it knows least about). **Deliberately NOT a "more than one factor" alert** — the app enrols as `GoHustlr` and the console as `GoHustlr Admin`, so two is a legitimate permanent steady state and the finding could never be closed; that is the permanent-false-positive shape already fixed three times here. The signal is "unreviewed", which is closable, and the probe proves it closes and re-opens. /team renders the same state the control sees, with a Confirm authenticators button. `claudeMdInventory` caught the stale registry count (54→56); updated. 9 new assertions. Applied and deployed. |
| medium | admin-console | **Four different situations rendered one sentence, and it nearly produced a duplicate admin account.** `requireAdmin` throws `forbidden` for (a) no membership row, (b) a PENDING row, (c) a DISABLED row, and (d) an active row whose role is too low for the page. `/denied` answered all four with "This account doesn't have access to this tool" — true of exactly one. On 2026-08-17 a newly invited admin enrolled his authenticator, passed the code prompt, landed here and read it as "wrong account". He had done everything right and was one click from being switched on; his next message was *"I have other emails if I need to set it up through another one"* — a second account, a second factor, and a second thing for someone to confirm. He also said "But is signed in" three times, because he was, and the screen contradicted what he could see. | 2026-08-17 — `/denied` reads the caller's OWN `admin_users` row and branches four ways: pending ("Waiting on approval — your authenticator is set up, an admin still has to switch you on, signing in again won't change it and a different email would just create a second account with the same problem"), disabled, role-too-low ("your account can reach the console, but not this page"), and the original generic string kept for the genuinely-no-row case so someone who lands here by guessing still learns nothing. Disclosure is bounded: it tells the signed-in person about their own membership, after they have already presented a password AND a TOTP code for that account. A lookup failure degrades to the generic copy rather than a 500. The GUARD is untouched and asserted to be — this is copy, not access, and a test fails if softening the message ever softens the check. 5 new assertions, 4 failing pre-fix. Deployed. |
| medium | admin-console | **The Reset authenticator button I shipped hours earlier re-opened the trust-on-first-use window on an ACTIVE member.** An account with no factor is one where `/mfa` enrols a fresh authenticator for whoever presents the password — the exact hole `pending` was created to cover for new members (20260804030000). Resetting an active admin put them straight back into it with no pending gate in front, so a compromised password bought a full admin session to whoever reached the login page first. The reset itself is not an escalation (admin tier, step-up, audited, and it removes a factor rather than granting one — the target still needs their password), but the window it opens was unguarded. | 2026-08-17 — a reset on SOMEBODY ELSE who is currently active now sets them back to `pending`, so the fresh enrolment grants nothing until an admin confirms the new time and activates. Deliberately never on yourself: a self-reset is someone at the keyboard about to re-enrol, demoting them would strand the console with nobody able to activate anyone, and it is not a new window anyway — an attacker holding your password and finding you factorless is already in. The console cannot be stranded either way, since the caller is an active admin by construction and never the target. A failed demotion is reported rather than swallowed (the factors are already gone by then, so failing the action would leave the worst of both). `MemberControls` also calls `router.refresh()` on success — `revalidatePath` marks the route stale but the client router still served the old row, so the screen showed an authenticator that no longer existed directly beside the message saying it was removed. 4 new assertions, all failing without the change. Deployed. |
| **high** | admin-console | **Sign-in wedged on every SUCCESSFUL login, and the fix I shipped yesterday caused it.** `recordLoginAttempt` is a server action, so it POSTs back to `/login` — and it runs immediately after `signInWithPassword`, by which point the session cookie exists. `proxy.ts` answered `user && path === "/login"` with a redirect unconditionally, for every method; `NextResponse.redirect` is a 307, so the POST was re-sent to `/`, whose bundle does not carry that action. The call rejected out in the page, the function unwound past `setBusy(false)`, and the button sat on "Signing in…" forever while the session was in fact established — refreshing landed straight on /mfa, exactly as reported. Only successful sign-ins broke: a failure leaves no session, so `user` is null and the POST passes through, which is why it survived a deploy (the failure path is the one the throttle was tested on). | 2026-08-17 — the `/login` bounce is now `method === "GET"` only; it is a UX affordance for a NAVIGATION and has no business touching an action POST. The signed-out bounce deliberately still covers every method. Belt to that braces in the page: `try/finally` so nothing can leave the button stuck, `recordLoginAttempt(...).catch(() => {})` because recording is detection and never access, and `window.location.assign("/mfa")` instead of `router.replace` + `router.refresh` — the cookie changed one line earlier, and the refresh was re-fetching the route being LEFT. 5 new assertions in `adminLoginThrottle.test.js`, all failing pre-fix. Deployed and verified live. |
| medium | admin-console | **One enrolment date could not answer the question the Activate dialog asks.** 20260817000000 put the real `auth.mfa_factors` timestamp on /team, and within the hour it proved too thin to act on: a pending admin's row read "authenticator enrolled Aug 17, 6:02 PM" three days after he was added, and nothing on screen could say whether that was a SECOND factor beside an older one or the only one there had ever been. Those render identically under a single `max()` and mean opposite things — forgot-where vs somebody-enrolled-beside-them. Separately, the only route out of a lost authenticator was direct Supabase credentials, which 20260804030000 calls "exactly the wrong dependency", and the console's own enrol flow minted no recovery codes until today — so a lost phone was a lost console. | 2026-08-17 (20260817010000) — `admin_team_list` returns `mfa_factor_count` and a `mfa_factors` jsonb array (oldest-first, name + created_at), empty array not null so the page maps unguarded. The friendly name reads the surface: `GoHustlr` = app/web, `GoHustlr Admin` = console. /team itemises them and warns on >1; Activate has three copies now, not two. Added `resetAuthenticator` (admin tier, step-up, audited BEFORE the mutation via `run`) which deletes EVERY factor including unverified ones — a leftover unverified row holds the friendly-name uniqueness and the next enrol fails on a collision instead of showing a QR — and clears the `admin_users` cache it invalidates. Probe proves two factors read as two, oldest-first, headline still newest, unverified excluded. Deployed. |
| **high** | admin-console | **The console's TOTP gate held none of the four properties the app and web gates hold. Found 2026-08-17 when an invited admin was stuck on it.** (a) **2FA BYPASS.** `const { data: factors } = await supabase.auth.mfa.listFactors()` dropped the error. listFactors is a NETWORK call returning `{data: null, error}` rather than throwing, so a request that merely FAILED gave `factors = null`, `verified = undefined`, and fell through to `enroll()` — handing whoever holds the password a brand-new authenticator and aal2 the moment they verify it. Blocking one XHR is a devtools checkbox. Nothing stopped it: the app enrols as `friendlyName: 'GoHustlr'` (src/lib/mfa.js:96) and the console as `'GoHustlr Admin'`, so the uniqueness constraint that would have collided does not, and gotrue holds both. `web/app/mfa/page.tsx` carries this exact guard with a comment recording that the same bug made **airplane mode a 2FA bypass on mobile** — the console never got it. (b) **NO EXIT.** No sign-out, no "not you?"; every redirect is `router.replace()`, which writes no history entry, so Back reaches /login with the session still live and signing in returns to the gate. A closed loop. (c) **NO RECOVERY CODES AT ENROLMENT**, against CLAUDE.md's own rule ("2FA without a way back in turns a lost phone into a lost account") — so every admin who enrolled via the console has zero codes and no recovery path, which is what made this lockout unrecoverable without database access. (d) The challenge never named or dated the factor it was challenging, so nobody could tell a forgotten setup from one enrolled by somebody else with their password. | 2026-08-17 — `admin/app/mfa/page.tsx` rewritten. Fails closed on `listErr` before reaching `enroll()`; sign-out via the `signOutAction` server action (client-only signOut leaves the cookie the server guard reads — same reason /denied uses it), rendered in every mode except the one showing unsaved codes; `generate_mfa_recovery_codes` after verify (the one moment the session is aal2, which 20260813070000 requires) then shown and `confirm_mfa_recovery_codes`; `redeem_mfa_recovery_code` + `refreshSession()` behind "I've lost my authenticator", one message for wrong/used/rate-limited so it is not an existence oracle; the challenge prints the factor's `created_at` and says what an unrecognised date means. All 9 shape assertions in `__tests__/adminMfaGate.test.js` fail against the previous file. Deployed and verified live in the shipped chunk on admin.gohustlr.com. |
| medium | admin-console | **The Team page asked a cache whether an admin has an authenticator, and only one of the two enrolment routes writes it.** Reported live 2026-08-17: an invited admin signed in at admin.gohustlr.com and was taken straight to the 6-digit prompt having never been shown a QR, while /team displayed "no authenticator enrolled yet" for the same account. `app/mfa/page.tsx` renders `challenge` iff `listFactors()` returns a verified TOTP factor, so that screen is proof one exists; `admin_users.mfa_enrolled_at` is written only by `admin_record_mfa_enrollment`, called from that page's `verify()`. The console and the app share one Supabase auth project, so a factor enrolled in the APP (`SecurityScreen`) is the same `auth.mfa_factors` row the console challenges against, and nothing writes the cache for it. Not cosmetic: pending→active is the control that closes the trust-on-first-use window, and `TeamControls.tsx` picks its Activate copy off this field — on a lagging cache the dialog asserts "They have NOT enrolled an authenticator yet" and **drops the confirm-the-time-with-them instruction entirely**, in the one dialog whose whole job is to make a human perform it. It fails the other way too: redeeming a recovery code removes the factor and nothing clears the cache, so a dead authenticator reports a reassuring date forever. `ctl_admin_without_mfa` was moved onto the authoritative source by 20260806100000 — whose own header calls the column "only a cache … [that] can lag" — and the console display was left behind. | 2026-08-17 (20260817000000) — `admin_team_list` reads `auth.mfa_factors` via a left lateral, `max(created_at)` over verified factors only. MAX not MIN: the dialog asks "did a factor appear that wasn't yours?", and min() surfaces the older vouched-for factor while hiding the one nobody has confirmed. Probe proves both directions on the same staged row (cache NULL + real factor ⇒ now reported; stale cache + factor deleted ⇒ now silent), that unverified factors don't count, and that a member with no factor is still listed. All 5 shape assertions in `__tests__/adminTeamMfaSource.test.js` fail against the old definition. Applied; no console redeploy needed — the page reads the RPC. |
| low | payments-audit | Both claim time-gates read payments.created_at, which a recovery re-hold never updates — the row is UPSERTed on booking_id, so created_at is the FIRST hold ever placed. A fresh authorization therefore read as days old, deciding both whether a claim is allowed and whether the near-expiry escape hatch opens. That hatch exists because Stripe voids an uncaptured hold at ~7 days, so anchoring it to a hold Stripe already cancelled inverts its purpose. | 2026-08-14 — both gates use `authorized_at ?? created_at`, which is what 20260806150000 added the column for and what the controls already do. 3 of the new assertions fail pre-fix. Deployed. |
| low | payments-audit | A worker on a 'Flexible — Contact to Schedule' gig whose poster ghosted had NO settlement path. No starts_at means shared/lifecycle returns false, EarnScreen renders no Claim button, and earner-claim-payment refuses with NO_SCHEDULE telling them to contact support — while the console offered release (nobody pays), refund (poster pays then unpaid) and record-chargeback, but no capture. The hold voided at ~7 days: nobody charged, nobody paid, for work that was done. | 2026-08-14 — added a `settle` op (admin tier, 300s step-up, written reason, audited before the Stripe call) and a "Settle & pay earner" control on the booking page. FULL capture only, reconciled to amount_received, then credit_earnings and settle_booking_benefits — a reduced settlement is a dispute outcome and belongs to the poster's Verify sheet, which records who asked for it. 4 assertions fail pre-fix. Deployed. |
| low | payments-audit | A referral bonus was never clawed back once it VESTED — vest_bonuses voided only 'pending' rows, and nothing anywhere else writes state='void'. A reversal landing after the 7-day window left a payable bonus standing on undone work, and Stripe's chargeback window is far longer than seven days. | 2026-08-14 (20260814150000) — a second pass voids 'payable' rows on a reversed source booking. Deliberately NOT applied to 'applied' rows: that credit is already spent and return_unused_fee_credit owns unwinding those. Two functions moving one row between the same states is how a ledger starts disagreeing with itself. |
| low | payments-audit | Voiding a bonus never returned its campaign budget. The campaign is charged when the bonus is MINTED; the void pass touched bonus_ledger only, and release_booking_benefits — the one budget-returning function — iterates promo_redemptions and knows nothing about bonus_ledger. Campaigns exhausted early against money never given away. | 2026-08-14 — void and budget-return are now ONE statement, so they cannot be applied separately: a void without its refund holds budget forever, a refund without its void hands it back twice. Probe: spent 500c → 0c. |
| low | payments-audit | The promotions_enabled incident kill switch reached only consume_promo_grant. consume_fee_credit (spending earned credits) and accrue_referral_bonus (minting new ones) both ignored it, so flipping the documented incident lever left two of the three incentive paths running — worse than no flag, because whoever flips it believes the bleeding stopped. | 2026-08-14 (20260814150000 + 20260814160000) — both paths now read it. Scoped to the FLAG, not campaign status: a credit already earned survives its campaign merely ending, because ending a promotion never re-prices agreed work. The switch suspends; it does not confiscate. |
| medium | payments-audit | /promotions was the console's only budget-spending surface with no MFA step-up — createPromotion, setPromotionStatus, mintCodes, editPromotion, clonePromotion and revokeGrant all on plain requireAdmin — while the SAME grant capability routed through /pricing required requireFreshAdmin. A borrowed session could mint codes, clone a campaign, raise a budget or revoke a grant. | 2026-08-14 — all six mutating actions now require a factor satisfied within 300s; previewCost stays a read, because forcing a re-auth to look at a number teaches people to re-auth reflexively. The UI half matters as much: each component holds a useStepUp instance and renders ReauthPrompt, so stale_mfa is recoverable rather than a dead end — a step-up with no way forward gets asked for removal. stepUpCoverage.test.js asserts BOTH directions and names any component that could dead-end; 6 assertions fail pre-fix. Console deployed. |
| medium | payments-audit | ctl_stripe_id_mode_mismatch judged an id's SHAPE, and the shape is identical in both modes. A Stripe account id is `acct_1` + 14 chars = 21 always, so `length < 22` matched EVERY account ever created; a customer id is `cus_` + 14 = 18 exactly, so `length < 18` matched none. One arm flagged everything, the other nothing. | 2026-08-14 (20260814140000) — judges PROVENANCE instead: `created_at < live_since`, where live_since is stamped by trigger the first time the flag goes live. An explicit stamp wins (the control's own remedy tells an operator to set one), an existing stamp is preserved (so the console toggle cannot re-date every account and silently empty the control), otherwise now(). Probe proves five states: 11 stale before a cutover vs 0 after, where the old predicate matched all 7 accounts. **Corrects my own 20260814080000 note and PRE_LAUNCH_DATA_RESET.md — the "8 test-shaped ids" I reported as real signal was the broken predicate matching everything.** |
| medium | payments-audit | A fee credit spent on a booking later refunded or charged back was destroyed. release_booking_benefits is the only path back to 'payable' and early-returns the moment any payment is captured — a refunded booking is captured by definition — while its trigger fires only on declined/cancelled. record_refund never read bonus_ledger, so the earner paid for the refund twice: the clawback, and a referral credit they earned, spent here, and could not spend anywhere else. | 2026-08-14 (20260814130000) — record_refund now calls the EXISTING return_unused_fee_credit rather than growing a second restore path, scaled by the share of the capture that survives the refund. Idempotent on the delivered figure, so incremental refunds each hand back their share. Non-fatal: the money has already moved at Stripe and bookkeeping must not roll back a refund the cardholder was given. Probe: 382c returned on a half refund, the remaining 383c on the rest — 765c, the whole applied credit, where the old path restored 0c. |
| medium | payments-audit | Promo cost was measured against the STANDING rate, blind to the loyalty tier. For an earner already on a tier at or below the promo's rate the campaign was charged the full standing→promo difference and a use was burned off a finite grant, while the pinned rate did not move at all because least() had already taken the tier. The budget drained against deliveries that never happened. | 2026-08-14 (20260814120000) — consume_promo_grant takes the ALREADY-PINNED baseline and the 3-arg form is DROPPED in the same transaction (leaving it would both re-create the bug silently and risk the "function is not unique" trap 20260813140000 records). No delivery ⇒ no charge, no use burned, so the grant stays available for a booking where it would actually move the rate. Probe: the old form would have charged 400c and burned a use for zero delivery; a promo that genuinely beats the baseline still applies. |
| medium | payments-audit | ctl_discount_without_grant fired a permanent CRITICAL on every declined booking that carried a poster discount. Its NOT EXISTS required `released_at is null` — and a decline is exactly what sets released_at, while deliberately leaving poster_discount_cents in place (20260806260000 explains at length that clearing it re-entered guard_bookings_write from an AFTER trigger and rolled back the decline itself). So the control described a state the system intends, could never auto-resolve, and trained people to scroll past criticals. | 2026-08-14 (20260814110000) — dropped `released_at is null` (a RELEASED redemption is still evidence a campaign granted it, which is the actual question) and excluded declined/cancelled bookings. Probe proves both sides on staged rows: the old predicate fires on the declined booking and the new one does not, while a live discount with no campaign behind it still fires. |
| medium | payments-audit | Both settle paths gated on the CACHED stripe_accounts.onboarded flag and never re-verified live. The flag going false is always correct; going STALE at false is silent — the account is fixed at Stripe, nothing tells us, and the hold voids at ~7 days leaving the worker unpaid AND the poster uncharged. Authorization time already re-verified for the milder case of merely blocking a booking. | 2026-08-14 — `_shared/payoutCapable.ts` lifts that live check and both settle paths use it. Deliberately asymmetric: a cached YES is believed (a stale true is caught by the capture itself failing, which is loud and immediate), a cached NO is checked against Stripe. It also distinguishes "no" from "could not ask" — refusing on an unreachable Stripe would start the clock on a voided hold over a transient API error, so that path logs and proceeds and lets the capture be the check. 6 of 9 assertions fail pre-fix. Both functions deployed. |
| medium | payments-audit | admin-payment-action could reach Stripe with NO admin_audit_log row, and its own header asserted the opposite — "the console writes its row BEFORE calling this, so an action that reaches Stripe is always already on the record". Nothing enforced it: the function authenticates the OPERATOR's own JWT, so the same token in a console browser can be POSTed straight at the endpoint with no console involved, and three money ops ran unlogged. | 2026-08-14 — it writes its OWN row before touching Stripe, tagged `.edge` so a direct call shows on /audit as the distinct event it is, and FAILS CLOSED if that write errors: an untraceable money action is worse than a refused one, and the operator can retry. Header corrected. |
| medium | payments-audit | Pricing, promotions, controls and moderation wrote the admin_audit_log row AFTER the mutation. audit() throws on failure and every action wraps its body in a catch, so a crash between the two left the change made and unattributed — and the log ends up missing exactly the rows from the runs that went wrong while looking complete. | 2026-08-14 — moved BEFORE the mutation for the four that matter most: setPlatformRate (the rate card is append-only precisely so its history is readable), grantToUsers (hands a campaign benefit to named people), setControlEnabled (a disabled control is a blind spot) and resolveFinding. Guarded by auditBeforeMutation.test.js; 7 of 8 assertions fail pre-fix. The remaining sites in promotions/moderation follow the same one-line pattern and are lower leverage. |
| medium | payments-audit | addTeamMember could strand the console with no admin: it was an UPSERT that always wrote status:'pending', and requireAdmin refuses any row that is not active — so re-adding an already-active member, to fix a role or a typo in a note, demoted them on the spot. With one admin in production that is a total lockout recoverable only with direct Supabase credentials, which the migration itself calls "exactly the wrong dependency". | 2026-08-14 — an existing member is now UPDATEd without touching status (active stays active), a new one is still created pending because that is what closes the enrolment window, and self-targeting is refused as it already was in both sibling actions. ALSO widened setTeamRole's last-admin check from `role === 'support'` to `role !== 'admin'`: finance and trust are peers of each other, NOT of admin, so demoting the last admin to either stranded the console just as completely while passing the guard. 4 of 6 assertions fail pre-fix. Deployed. |
| medium | payments-audit | The dispute-recording branch was gated on the REQUESTED pct, not on whether a capture happened, so a poster could POST pct:0.5 at their OWN already-settled booking and fabricate a "50% paid" dispute. No money moved — capture is skipped and credit_earnings is idempotent — which is why it went unnoticed. | 2026-08-14 — gated on `capturedGigCents !== null`, the same did-a-capture-run flag the benefit settle already used, so both consequences of a capture hang off ONE fact instead of drifting apart. The harm was never the money: it froze a THIRD PARTY's referral bonus (vest_bonuses blocks on any open dispute), wrote a false row into the only server-authored dispute log rendered as fact on three console pages, and would open a HIGH control finding after 14 days. Guarded by captureClaim.test.js, proved to discriminate. |
| medium | payments-audit | Refund idempotency key is built from the RUNNING TOTAL, so a console retry after the 20s timeout refunds the poster twice (audit F4 / payments-core #16) | 2026-08-14 — **ALREADY FIXED, verified against current code.** The key no longer derives from a moving value, and the ledger has its own independent guard.  1. Stripe key is now operator-intent-scoped: supabase/functions/admin-payment-action/index.ts:298-301 — `idempotencyKey: requestId ? `refund_${pay.payment_intent_id}_${requestId}` : <old shape>`; requestId parsed and charset-c |
| medium | payments-audit | payments.refund_source='admin' is sticky and permanently disarms charge.dispute.created for that booking — including the only control that detects an unledgered chargeback (payments-core #11) | 2026-08-14 — **ALREADY FIXED, verified against current code.** Fixed in three independent layers; the chargeback half on 2026-08-13, the refund half today.  1. The exemption can no longer reach a chargeback at all: supabase/functions/stripe-webhook/index.ts:148 — the whole skip block is inside `if (kind === 'refund') {`, and recordReversal's `kind` parameter is documented at :56-5 |
| medium | payments-audit | payments.refund_source='admin' is sticky forever, so every LATER genuine chargeback or Dashboard refund on that payment is silently swallowed by the webhook (payments-core #17) | 2026-08-14 — **ALREADY FIXED, verified against current code.** Same defect stated twice, as the task suspected. #11 is written from the webhook side (stripe-webhook/index.ts:68) and #17 from the console side (admin-payment-action/index.ts:178); both describe one column, one early-return, and one control going dark. The audit's own dedup note says as much: docs/audits/2026-08-12-pa |
| medium | payments-audit | The only automated detector of a charged-but-not-credited tip is blind to the cause its own registry entry names, because the trigger rolls back the row it queries (payments-core #15) | 2026-08-14 — **ALREADY FIXED, verified against current code.** The premise — that the tip_ledger row is only ever written AFTER the charge, inside the transaction the cap trigger aborts — is no longer true. supabase/migrations/20260814090000_tip_reservation_and_reversal.sql turned the pre-charge gate into a write.  1. The row exists before any money moves: supabase/functions/strip |
| medium | payments-audit | A transient Stripe `paymentIntents.retrieve` failure silently orphans the live escrow hold and re-points the ledger at a brand-new, unconfirmed PaymentIntent | 2026-08-14 — **ALREADY FIXED, verified against current code.** The bare `catch (_)` the finding cites is gone. `/Users/chrismalson/Documents/gohustle/supabase/functions/stripe-create-payment-intent/index.ts:342-397` now classifies the retrieve error: `:364-366` `const missing = err?.statusCode === 404 \|\| err?.code === 'resource_missing' \|\| err?.raw?.code === 'resource_missing' |
| medium | payments-audit | payment_intent.succeeded credits credit_earnings from the pre-computed split and never reconciles to pi.amount_received | 2026-08-14 — **ALREADY FIXED, verified against current code.** Fixed 2026-08-13, before the audit was recovered. `/Users/chrismalson/Documents/gohustle/supabase/functions/stripe-webhook/index.ts:289` now reads `const received = typeof pi.amount_received === 'number' ? pi.amount_received : 0;` and `:290` computes `storedSplit`. `:294-308`: when `received > 0 && received !== storedS |
| medium | payments-audit | CANCELLABLE_STATUSES does not close the accept-then-void hole: `pending` is not a hold-free state, and accept-booking's unconditional `payments.status='authorized'` write can resurrect a row the poster just cancelled | 2026-08-14 — **ALREADY FIXED, verified against current code.** The unpredicated write is gone. `/Users/chrismalson/Documents/gohustle/supabase/functions/accept-booking/index.ts:96-101` is now a checked claim — `.update({ status: 'authorized' }).eq('id', payment.id).in('status', ['authorized', 'failed']).select('id')` — and `:103-118` refuses to confirm on zero rows, re-reads the c |
| medium | payments-audit | A fee credit is burned in full at booking but delivered pro-rata at partial capture, and the fee can fall below Stripe's cost (incentives F10) | 2026-08-14 — **ALREADY FIXED, verified against current code.** (a) THE CREDIT. Fixed by /Users/chrismalson/Documents/gohustle/supabase/migrations/20260813130000_return_unused_fee_credit.sql:44-100 plus the call at /Users/chrismalson/Documents/gohustle/supabase/functions/stripe-capture-payment/index.ts:375-402, ordered after the status flip at :331-336. Delivered is computed at :37 |
| medium | payments-audit | The unpaid-worker control was silently gutted (ctl_escrow_hold_expiring_work_done lost `earner_done` and 'confirmed') | 2026-08-14 — **ALREADY FIXED, verified against current code.** The earlier triage was RIGHT, and I re-derived it independently rather than taking its word. `ctl_escrow_hold_expiring_work_done` has exactly three definitions in `supabase/migrations/` (grep across all of supabase/): 20260806040000_control_library.sql:148 (original), 20260806150000_authorized_at.sql:66 (the gutted one |
| medium | payments-audit | The daily controls digest still dispatches at pg_net's 5s default | 2026-08-14 — **ALREADY FIXED, verified against current code.** The stated mechanism is gone. `controls_digest` has two definitions (20260806030000_schedule_controls.sql:74 and /Users/chrismalson/Documents/gohustle/supabase/migrations/20260812070000_dispatch_timeouts.sql:38); the later one is live and passes `timeout_milliseconds := 20000` at :59, with the comment 'Daily, with no r |
| medium | payments-audit | All three tip caps are bypassable by concurrency, and the over-cap money erases its own ledger row (payments-core JSON #13) | 2026-08-14 — **ALREADY FIXED, verified against current code.** Fixed today by /Users/chrismalson/Documents/gohustle/supabase/migrations/20260814090000_tip_reservation_and_reversal.sql plus the stripe-tip rewrite. The gate is no longer a lock-free read.  - The pre-charge call is now a WRITE: supabase/functions/stripe-tip/index.ts:90-93 calls `reserve_tip_slot`, and :94-99 fails CLO |
| medium | payments-audit | **UNVERIFIED (2026-08-12 audit, recovered 2026-08-14).** accept-booking resurrects a cancelled hold: it writes `payments.status='authorized'` with no status predicate and confirms the booking off a stale PaymentIntent read `supabase/functions/accept- | 2026-08-14 — the payments re-authorize is now a checked claim that excludes the statuses which must never be resurrected, aborting BEFORE the booking confirm. Deliberately still admits 'failed', because that rewrite is load-bearing: stripe-capture-payment hard-refuses a 'failed' row, so excluding it would strand every poster who retries a declined card. Narrowed, not eliminated — claim and confirm are two adjacent writes; the complete fix is one RPC doing both in a transaction. |
| medium | payments-audit | **UNVERIFIED (2026-08-12 audit, recovered 2026-08-14).** A stale or retried payment_intent.payment_failed kills a LIVE escrow hold: 'authorized' is in the allow-list, so the row is stamped 'failed' and the confirmed booking is demoted to 'pending' `s | 2026-08-14 — the handler now retrieves the intent's CURRENT status before demoting and skips it when Stripe says the hold is live, failing safe if Stripe cannot be read. bb428f9 had fixed the duplicate-authorization half and touched no line of the webhook. |
| high | payments-audit | **UNVERIFIED (2026-08-12 audit, recovered 2026-08-14).** All three tip caps are bypassable by concurrency, and the over-cap money erases its own ledger row `supabase/functions/stripe-tip/index.ts:86` | 2026-08-14 (20260814090000) — the pre-charge gate is now a WRITE: reserve_tip_slot inserts the row so trg_guard_tip_caps evaluates all three caps under the advisory locks it already takes, so the second concurrent caller blocks and is refused rather than both passing a lock-free read. No second copy of the cap arithmetic. Probe proved the old gate returned the same headroom twice while the new one gives ok + tip_cap_booking on the same two calls. |
| high | payments-audit | **UNVERIFIED (2026-08-12 audit, recovered 2026-08-14).** A refunded or charged-back TIP is never reversed anywhere — and ctl_earnings_total_drift certifies the inflated balance as correct `supabase/functions/stripe-webhook/index.ts:59` | 2026-08-14 (20260814090000 + stripe-webhook) — tip_ledger gained reversed_cents/reversed_at, record_tip_reversal uses cumulative-target semantics so a Stripe redelivery is a no-op by arithmetic, ctl_earnings_total_drift subtracts reversed tips, and recordReversal now falls back to a tip_ledger lookup so the RPC actually has a caller. Probe: the old body expected 70.00 against a stored 70.00 and reported healthy; the new one reports 20.00 drift. |
| high | payments-audit | **UNVERIFIED (2026-08-12 audit, recovered 2026-08-14).** Turning off the alert-dispatch flags kills all alerting permanently and no control can see it — the 2026-07-10 silent death, recurring with a different key `supabase/migrations/20260806160000_d | 2026-08-14 (20260814100000) — ctl_alert_not_dispatching reads app_flags directly for both channels and names every broken shape (row missing, switched off, blank url, blank secret), resolving url/secret exactly as each dispatcher does INCLUDING the GUC fallback only notify_safety_report has, so it cannot report a live channel dark. Mutes now expire. Probe: on the same data the incumbent control returned 0 rows and the new one names the channel; run_control wrote 2 findings. |
| high | payments-audit | **UNVERIFIED (2026-08-12 audit, recovered 2026-08-14).** Capture's promo settle-back refunds a poster discount in full, so budget_cents stops bounding poster_discount campaigns `supabase/migrations/20260806220000_benefit_lifecycle.sql:177` | 2026-08-14 — **ALREADY FIXED, verified against current code, not assumed.** The audit read supabase/migrations/20260806220000_benefit_lifecycle.sql:177, which is three revisions stale. The LAST migration that defines settle_booking_benefits is supabase/migrations/20260813140000_settle_benefits_at_booking_rate.sql:69-121, and it branches on kind: :86-87 `select kind, max_ben |
| high | payments-audit | **UNVERIFIED (2026-08-12 audit, recovered 2026-08-14).** settle_booking_benefits refunds a poster-discount campaign's entire reserve at capture, so budget_cents never binds and burn-down reads $0 `supabase/migrations/20260806220000_benefit_ | 2026-08-14 — **ALREADY FIXED, verified against current code, not assumed.** DUPLICATE of finding 1 — same function, same defect, same fix; the two came from different report files (docs/audits/2026-08-12-payments/reports/findings-payments-core.json and findings-incentives-and-abuse.json) and cite the same line, 20260806220000_benefit_lifecycle.sql:177. Same current-code evi |
| high | payments-audit | **UNVERIFIED (2026-08-12 audit, recovered 2026-08-14).** settle_booking_benefits has no promotion-kind filter: every poster-discount capture refunds the campaign's entire budget charge, so budget_cents stops bounding loss `supabase/migratio | 2026-08-14 — **ALREADY FIXED, verified against current code, not assumed.** DUPLICATE of findings 1 and 2, stated as the mechanism (missing kind filter) rather than the effect (budget refund). The filter exists in the current definition: supabase/migrations/20260813140000_settle_benefits_at_booking_rate.sql:86-100 selects `kind` into `k` and branches `if k = 'poster_discoun |
| high | payments-audit | **UNVERIFIED (2026-08-12 audit, recovered 2026-08-14).** settle_booking_benefits is kind-blind: every poster-discount charge is refunded to the campaign at capture, so budget_cents stops bounding the loss `supabase/migrations/20260806220000 | 2026-08-14 — **ALREADY FIXED, verified against current code, not assumed.** DUPLICATE of findings 1, 2 and 3 — four separate report rows for one defect. Same evidence: the kind branch at supabase/migrations/20260813140000_settle_benefits_at_booking_rate.sql:89-100. Findings 1-4 should be recorded as ONE closed item, not four. |
| high | payments-audit | **UNVERIFIED (2026-08-12 audit, recovered 2026-08-14).** max_benefit_cents caps the CHARGE but never the DISCOUNT — budget_cents does not bound the platform's real loss `supabase/migrations/20260806220000_benefit_lifecycle.sql:269` | 2026-08-14 — **ALREADY FIXED, verified against current code, not assumed.** This one is the fee_override rail, not the poster_discount rail (the cited line 20260806220000_benefit_lifecycle.sql:269 is consume_promo_grant's `hit := least(p.max_benefit_cents, promo_benefit_cents(...))`, while the function RETURNED the uncapped g.fee_bps). Fixed by supabase/migrations/202608130 |
| high | payments-audit | `ctl_stripe_id_mode_mismatch` was gated on an `app_flags` key no migration ever created, so every arm required `mode = 'live'`, the coalesce always returned 'test', and the control was structurally incapable of returning a row while registered, enabled and running hourly. | 2026-08-14 — **CONFIRMED, then fixed** (20260814080000). The flag now exists as `{"mode":"test"}` (today's truth), so behaviour is unchanged but the control is ARMED and /flags can flip it at cutover. Absence is now loud: a deleted flag makes the control report its own disarming instead of returning silence. Probe proved all three states — and the live arm flagged **8 existing test-shaped ids**, so this WILL fire the moment the mode flips, which is the point. |
| high | payments-audit | The admin login throttle had ZERO callers — no lockout existed and `ctl_admin_login_bruteforce` was permanently blind, counting a table nothing wrote to. CLAUDE.md documented "5 failures per account / 15 min, or 20 per IP" for two months and it enforced nothing. | 2026-08-14 — **CONFIRMED by direct grep, then fixed.** `admin/app/login/actions.ts` is a server action (the RPC and table are service_role-only, so a browser client cannot reach either): it checks `admin_login_blocked` BEFORE the sign-in and records EVERY attempt including failures, which is the half that makes the control work. Fails OPEN on a throttle error — this is the door people come through during an incident. Blocked and wrong-password show the same string, asserted by comparing every setError argument. Honest bound now in CLAUDE.md: the sign-in is client-side, so this gates the CONSOLE path; someone POSTing straight at Supabase is bounded by Supabase's limits. 3 of 5 assertions fail pre-fix. Console deployed. |
| medium | mobile-ux | Connect onboarding opens with `WebBrowser.openBrowserAsync` (PayoutSetupScreen.js:91), so when Stripe redirects to the return_url the in-app browser does NOT auto-dismiss — the user taps Done by hand and only then does `refresh()` run. AuthContext.js:320 already uses `openAuthSessionAsync` for Google, so the pattern exists. | 2026-08-14, commit a22cc71 — the reason it was never a one-line swap is a hard Stripe constraint, verified against the live API: an account link with return_url `gohustlr://…` fails with `url_invalid` / "Not a valid URL". So the return stays https and relays: mobile client sends `native: true` → the edge function appends `?native=1` → /stripe/connect-return bounces to the app scheme → openAuthSessionAsync dismisses. Web never sends the flag, so absence keeps the browser on https. Scheme hardcoded both ends (a client-named destination would be an open redirect on our own domain). payoutReturnContract.test.js pins all four hops; 5 of 8 assertions fail pre-fix. ⚠️ **NEEDS A DEVICE to confirm the browser actually dismisses — a simulator cannot prove that hop.** |
| low | web-parity | The assistant-memory viewer shipped on MOBILE only. gohustlr.com has no way to see or delete what Hustlr AI has stored about you, so the same account gets a privacy control on one client and not the other — and the assistant now tells every user the screen exists. | 2026-08-14 — ported to web/app/(app)/settings/memory, reading via the my_profile() RPC (a direct select cannot see the column) and writing without a chained .select() (which would report failure on success). parity.test.js pins both clients. |
| low | assistant-gate | `remember` — the SECURITY half is closed (2026-08-14, commit ed9f787): the fact now runs findProhibited + moderateViaEdge, the same two layers create_gig and update_profile run. **The DISCLOSURE half is not.** Telling the user what was stored is an instruction in the tool result and the system prompt, i.e. model-generated text an uncooperative or injected model can simply omit, and no test asserts it. There is still no viewer and no per-item delete anywhere in the app, so "no way for the user to see or delete it" remains literally true — a stored fact is unremovable except by overflowing the 25-item window. | 2026-08-14 — MOBILE half shipped: Settings → Account → "What Hustlr AI remembers" lists every stored note with per-item delete and a Forget-everything action, verified end to end on the simulator (a fact saved via the assistant appeared, deleted, and stayed deleted across a re-read). The assistant prompt no longer claims users have no way to see stored memory, and parity.test.js MUST_KNOW now pins that destination. **The WEB half is still missing — tracked as its own row below.** |
| medium | assistant-gate | The per-turn caps on bookings and gigs are dead code after the gate, so one request can stage unbounded actions and the client renders only the first | 2026-08-14 — both cap on STAGED confirm_actions now (one per request), which is what the clients render: AssistantButton and AssistantWidget both use actions.find, so every staged row past the first was a live consumable action the user never saw. Commit ed9f787. |
| low | assistant-gate | purge_assistant_pending_actions is never scheduled, so staged action payloads are retained indefinitely | 2026-08-14 — runs inside controls_sweep_and_page beside vest_bonuses, in its own exception block. The probe asserts via pg_get_functiondef that the SWEEP CALLS IT, because the function was always correct and simply had no caller. Commit 75019b1. |
| low | money-paths | A failed stripe_accounts lookup is silently treated as 'no such account' and nulls the payout's owner, hiding the deposit permanently | 2026-08-14 — the lookup error is destructured and throws so Stripe retries; a genuine no-such-mapping still persists and still renders as "unmapped account". Commit ed9f787 (change itself landed in 9296632). |
| low | support-abuse | The 'cold contact does not email' rule is bypassed by the second call: an agent-opened thread reused before the user has ever replied does send brande | 2026-08-14 — gated on the FACT rather than thread novelty: coldContact = !reuse || reuse.last_author !== "user". Note it lives in admin/app/(console)/support/actions.ts, NOT an edge function. Commit ed9f787 (change landed in 9296632). |
| low | regressions | Redeeming a recovery code leaves the local session's factor list stale, so a relaunch inside the hour re-prompts and burns a second code | 2026-08-14 — refreshSession() before the gate clears, so the stored user no longer lists the deleted factor. Commit ed9f787, shipped OTA 3525772e. |
| low | regressions | The assistant's confirm turn is never written to the thread, so History shows a booking that reads as never made | 2026-08-14 — the confirm path appends to assistant_messages after the same owner-scoped thread check the model path uses. Residual: the expired/already-used early return still writes nothing, which is defensible since no action occurred. Commit ed9f787. |
| medium | stripe-config | The Connect webhook subscribes to `account.updated` ONLY, so no `payout.*` event is ever delivered — `stripe_payouts` can never receive a row. Bank deposits in Transactions is therefore permanently empty and the "$X has arrived" / "could not be deposited" push at [stripe-webhook/index.ts:332](supabase/functions/stripe-webhook/index.ts:332) never fires. The handler at [index.ts:290](supabase/functions/stripe-webhook/index.ts:290) is correct and complete; nothing calls it. CLAUDE.md already states this dependency, so the gap is config, not code. Verified 2026-08-13 on test-mode endpoint `we_1TxVcPE0UZFlVCOpL7VI3ZPu`. | 2026-08-14 — STALE, verified directly against Stripe: `stripe webhook_endpoints list` shows we_1TxVcPE0UZFlVCOpL7VI3ZPu (the same endpoint the finding named) carrying account.updated PLUS payout.created/updated/paid/failed/canceled. The `stripe_webhook_config` control agrees — it asserts the subscription against Stripe on every sweep and reports 0 violations. Bank deposits is therefore wired end to end in test mode; the empty state seen on the simulator is a genuine no-payouts-yet, not a dead pipeline. |
| low | support-abuse | last_message_at is unpinned on INSERT — a self-created ticket can be made permanently invisible to the SLA control and can evict every real ticket fro | 2026-08-14 — guard_support_ticket_write now pins created_at and last_message_at on INSERT (20260814030000:96-97). Note the branch is also unreachable: 20260813090000 revoked INSERT from authenticated, so support-submit is the only writer. Defence in depth. |
| low | support-abuse | A user can staple a stranger's booking to their own support ticket, defeating the verification support-submit performs | 2026-08-14 — the INSERT branch forces booking_id/job_id to null for non-service_role callers (20260814030000:98-99); support-submit sets them server-side after verifying them. |
| low | support-abuse | support_tickets.ip is not pinned on UPDATE, so the ticket's author can rewrite the only forensic IP record on it | 2026-08-14 — new.ip := old.ip on UPDATE, and user_read_at is clamped to now() so a device clock cannot backdate the read receipt (20260814030000:135,139-140). Probed on production. |
| medium | money-paths | ATTACK_FINDINGS.md's 11 confirmed findings, reconciled 2026-08-14. **NINE are now closed** (partial capture below Stripe cost; fee credit destroyed on partial capture; payment_intent.succeeded crediting off the row; promo settlement at the live rate; runSweepNow skipping externals; record_reversal debiting the earner; the capture race; the recovery re-hold; the refund replay). **ONE was verified already fixed and the write-up is stale** — the receipt double-count was closed by 9f404d1 and ledger.test.js:437-450 covers the finding's own cases, including "(0%) − $3.45". **ONE remains open**, below. A twelfth defect was found while verifying them and is also fixed (the reversal control's dead parser, b0cfd0f). | 2026-08-14 — ALL ELEVEN closed. The last (pinned fee inputs invisible in the console) shipped as b88fce0. ATTACK_FINDINGS.md carries a status header so its stale claims are not re-derived. |
| low | mfa-lockout | Generating recovery codes destroys the previous set before the new one is delivered — a dropped response silently leaves the user with no valid codes | 2026-08-14 — delivery is now the point of no return: generation keeps the previous batch, confirm_mfa_recovery_codes() retires it. Batches ordered by a sequence, not created_at. Commit 07cfe88. |
| low | mfa-lockout | Enrollment can finish with 2FA switched ON, zero recovery codes, and a screen still reading 'Off' | 2026-08-14 — load() moved into a finally, and a post-verify failure now says "Two-factor is now ON, but we could not create your recovery codes". Commit 07cfe88. |
| low | mfa-lockout | The recovery-code rate limiter is self-extending, so a user who mistypes can hold themselves out of their own account indefinitely | 2026-08-14 — counts before recording, so the window drains on a fixed schedule. Proved on production: an over-limit attempt held attempts at 12. Commit 07cfe88. |
| medium | admin-surface | The pinned fee inputs are never rendered anywhere in the console — `fee_bps_quoted`, `fee_credit_cents` and `poster_discount_cents` return 0 hits across admin/app and admin/lib, including on the booking page an operator opens BECAUSE of a dispute. The two escape hatches both fail: dividing fee_cents ÷ amount_cents is wrong on exactly a partial capture (the ratio renders bps × pct — a 50% capture on a 700bps booking displays 3.5%), and PaymentIntent metadata carries only {booking_id, job_id, earner_id, poster_id}. Live today: production holds 2 distinct fee_bps and two payments at the IDENTICAL amount_cents=2000 carrying different pinned rates. | 2026-08-14 — booking detail now renders the pinned rate (plus credit/discount when non-zero) and /payments gained a Rate column. Console deployed to production. Commit b88fce0. |
| medium | money-paths | A partial refund overstates the earner's loss by the platform-fee share, so Transactions disagrees with the earnings dashboard | 2026-08-14 — earner net now uses their proportional share (earnerRefundShareCents); ledger.test.js parses record_refund off disk so SQL and JS cannot drift. Commit 288135c. |
| medium | money-paths | The payout upsert has no ordering guard, so a redelivered or out-of-order payout event reverts 'paid' to 'pending' | 2026-08-13 — 20260813100000_payout_event_ordering.sql added last_event_at + guard_stripe_payout_ordering. Row was stale. |
| medium | money-paths | Load failures render as authoritative empty states — the exact failure mode the screen's own comments forbid | 2026-08-14 — fetchLedger throws on either booking query error; payouts gained a third state (null/[]/error) with a Retry row. Commit e01d6eb. |
| medium | support-abuse | A photo-only support reply can never be sent — support.js writes body: null into a NOT NULL column | 2026-08-14 — body: text, not text || null. Commit b1da3bb. |
| medium | support-abuse | Photos attached to a FIRST support message are uploaded and then silently discarded | 2026-08-14 — images threaded SupportScreen → submitSupportRequest → support-submit, owner-scoped paths only. Commit b1da3bb. |
| medium | support-abuse | Attachments an agent sends are unreadable by the user they were sent to — the object path can never satisfy the storage read policy | 2026-08-14 — support_photos_read_ticket authorizes on the ticket OWNER, not the path (20260814020000). Applied to production. Commit b1da3bb. |
| high | mobile-ux | react-native-screens 4.16 → 4.23 flipped `fullScreenSwipeEnabled` to default TRUE on iOS 26, so every one of ~40 pushed screens gained swipe-to-go-back from ANYWHERE, not just the edge. 4.16.0 had explicitly disabled UIKit's iOS 26 content-pop gesture (`interactiveContentPopGestureRecognizer.enabled = NO`); 4.23.0 removed that line. Nothing in the app opts out and nothing guards removal (no `usePreventRemove` anywhere), so a horizontal drag mid-form on PostJob/EditJob, mid-message on Chat, or across ProfileSettings would pop the screen and discard the work. | 2026-08-13 — fullScreenGestureEnabled: false in DETAIL_OPTS, guarded by parity.test.js. |
| low | mobile-ux | RN 0.82 enabled `enableFontScaleChangesUpdatingLayout` by default (false in 0.81.5, true in installed 0.83.10), so native re-measures text live on an OS text-size change. FloatingTabBar's label window was a one-shot `PixelRatio.getFontScale()` snapshot, so the labels grew inside a container that did not and `overflow: 'hidden'` clipped them until the next navigation re-rendered the bar. | 2026-08-13 — useWindowDimensions() subscribes to fontScale. |
| medium | money-paths | `refund_source` is still never cleared, so the exemption remains permanent for REFUNDS: after an admin refund, a SECOND externally-issued refund on the same charge files no `disputes` row either. Narrower than the chargeback case (now fixed) because refunds do have compensating detectors — reconcile-stripe compares `amount_refunded`, and ctl_external_reversal_not_ledgered has a refunded_cents arm — whereas a chargeback's only signal was the disputes row. | 2026-08-14 — record_refund clears refund_source once it ledgers the refund, and recordReversal gained a second self-clearing test on the cumulative refunded total. Commit c8b23fd. |
| critical | mfa-lockout | Any AAL1 session can mint its own recovery codes and redeem one — deleting every MFA factor on the account. Password alone defeats 2FA, the payout ste | 2026-08-13 |
| high | mfa-lockout | The 2FA challenge screen lets you into the app with no code at all when the factor lookup fails — put the phone in airplane mode and press Continue | 2026-08-13 |
| high | stepup-bypass | An AAL1 (non-stepped-up) session can self-neutralize MFA: generate_mfa_recovery_codes and redeem_mfa_recovery_code are granted to `authenticated` with | 2026-08-13 |
| high | assistant-gate | The confirmation card shows no details | 2026-08-13 |
| critical | stepup-bypass | requireStepUp fails closed on EVERY call | 2026-08-13 |
| critical | regressions | Payout setup and "Manage payout details" now fail for EVERY user | 2026-08-13 |
| high | regressions | Turning on two-factor never shows the recovery codes | 2026-08-13 |
| medium | regressions | Every hourly token refresh unmounts and remounts the whole app | 2026-08-13 |
| medium | regressions | The assistant confirmation card carries no server-derived detail | 2026-08-13 |
| high | assistant-gate | executeCreateGig references an undeclared… | 2026-08-13 |
| medium | money-paths | Export CSV ignores every on-screen filter… | 2026-08-13 |
| high | money-paths | Tips are stored in dollars but the ledger reads them as cents — every tip shows as 1/100th of its value | Convert at the boundary: `const tip = Math.round((Number(b.tip_amount) \|\| 0) * 100)` in toEntry. Number() also hardens against PostgREST ever emitting numeric as a string, which today's `typeof v === 'number' |
| high | money-paths | A partial capture (dispute payout) bills the poster's receipt for the full authorized hold, not what Stripe actually charged | Derive the settled total the way the console does: for a captured row, capturedTotal = earner_amount_cents + fee_cents, and use that (minus refunded_cents) as the poster's charge and as the earner's 'Gig total' |
| high | regressions | Hustlr AI "Post it" throws a ReferenceError after the gig has already been inserted — the user sees a failure for a gig that is live | Derive requirements from the staged payload inside executeCreateGig, the same way title/category/pay are re-derived at lines 927-934 — the payload already carries them (staged at index.ts:910). Add `deno check  |
| high | support-abuse | Any authenticated user can shut down the entire support intake channel for an hour with 60 direct PostgREST inserts | Support intake should have one writer. Revoke INSERT on public.support_tickets from `authenticated` and drop support_tickets_open_own so every new ticket goes through support-submit, which already rate-limits,  |
| medium | assistant-gate | The web app cannot confirm a staged action at all — Hustlr AI can no longer post or book on gohustlr.com | Port the mobile confirm flow to web: add `confirm_action_id` to web/lib/assistant.ts, capture the confirm_action in runActions, render a confirmation card, and post the id back. Until that ships, the alternativ |
| medium | stripe-config | THREE Stripe API versions are live across the money paths: 16 functions construct `new Stripe(key)` with no pin (SDK default), 3 pin `2024-06-20`, and `reconcile-stripe` alone imports `npm:stripe@14` whose types only accept `2023-10-16` while being told `2024-06-20`. Webhook payloads are rendered at a fourth (`2026-05-27.dahlia`). Reconciliation compares Stripe against our DB, which is exactly where a response-shape difference misreads. This is why `reconcile-stripe` is the one `deno check` exemption in `.githooks/pre-push`. | Pick one API version, pin it explicitly in every function, and align reconcile-stripe to `npm:stripe@15`. Needs a tested pass over each money path, not a blind bump — then delete the EXEMPT entry in the pre-push hook. |
| high | web-parity | gohustlr.com has NO assurance-level check anywhere: a user who enrolled TOTP on mobile signs into the website with the password alone and gets a full aal1 session (browse, book, message, poster payments, PII). `grep -riE 'mfa|aal[12]|totp|listFactors' web/app web/lib web/components` returns zero hits; `web/app/(app)/layout.tsx:13-15` enumerates three gates (session, onboarding, consent) and omits the MFA challenge mobile holds FIRST. Web also offers no enrolment and no recovery-code redemption. Payout redirection IS still blocked server-side by `_shared/stepUp.ts`, so this is account access, not money movement. | Port the mobile gate: check `getAuthenticatorAssuranceLevel()` in `web/lib/auth.tsx` after `signInWithPassword` and hold a challenge route ahead of the app shell, mirroring `src/context/AuthContext.js:191-211` + `MfaChallengeScreen`. Add a `parity.test.js` assertion that both clients gate on AAL, the same way it now asserts both speak the assistant confirm protocol. KNOWN_RISKS.md:71-73 is stale — it still claims there is no user 2FA at all. |
| low | money-paths | A poster discount is deducted a second time on a receipt whose total is already net of it | Show the pre-discount figure as 'Gig total' (amount_cents + poster_discount_cents) so the discount line has something to subtract from, leaving amount_cents as the total charged. Same correction in the stat gri |
| high | money-paths | `payment_intent.succeeded` marked the payment captured with NO status predicate and credited the earner straight off `earner_amount_cents`, never reading `pi.amount_received`. The full split is written at AUTHORIZATION, so any capture taken outside our own capture function — the Stripe Dashboard being the obvious one — credited the earner the FULL figure regardless of what Stripe collected, then latched `earnings_credited` so it could not be re-run. Both sibling handlers were already status-guarded; this was the only unguarded one, and the only one that moves money. RUNBOOK_MONEY.md §3 had been instructing operators around it. | FIXED 2026-08-13: re-derives the split from `amount_received` before crediting, refuses to rewrite a split already paid out on, will not resurrect a cancelled/failed row, and logs the out-of-band capture. Guarded by __tests__/webhookEventCoverage.test.js. Runbook §3 rewritten. |
| high | tooling | **Expo SDK 57 does not build on Xcode 26.3 / Swift 6.2.4.** `expo-modules-jsi@57.0.4` (latest patch, Expo's own module) fails: `JavaScriptCodable+Date.swift:53:50: type of expression is ambiguous without a type annotation`. Reproduced across four clean builds. The expression compiles fine in isolation under both Swift 6 and default mode, and the `@usableFromInline` annotation the library-evolution theory needs is already present — so the cause is inside how Expo builds that module (it produces its own 168MB .DerivedData and a .swiftinterface, i.e. library-evolution mode), not something fixable from this repo. Branch `expo-57-upgrade` holds the work: 771 tests pass, expo-doctor 19/20, prebuild + pod install clean. Only the native compile blocks. | **Target SDK 55 instead** — `expo-modules-jsi` does not exist before 56.0.0, so 55.0.28 avoids the failing package outright while still moving RN, React and stripe-react-native forward. Or wait for an expo-modules-jsi patch and retry 57. Do NOT patch node_modules: three separate theories were tried and all three were wrong. | RESOLVED by targeting SDK 55 instead 2026-08-13 |
| high | money-paths | A partial capture settled BELOW Stripe's own cost, so the platform paid to resolve a dispute. `feeCents = round(fullFee * pct)` scaled an ALREADY-FLOORED fee, shrinking its fixed 30c+25c while Stripe's fixed 30c on the captured amount did not shrink. Verified live: $200 gig with a 765c credit at 50% → fee 318 vs Stripe cost 320 (−2c); $10 gig with NO credit at 50% → 42 vs 45 (−3c). The second needs no promotion at all, and 0.5 is a one-tap chip in the poster's Verify sheet. | FIXED 2026-08-13: the scaled fee is floored at `platform_fee_cents(captureCents, 0)` — the floor from the ONE fee definition, via RPC rather than a local formula — and fails closed if it cannot be computed. Guarded by pricing.test.js, proved to discriminate. 2026-08-13 |
| high | money-policy | **Needs your decision.** On a partial capture the earner's fee credit is consumed IN FULL but delivers only `pct` of its value — a $7.65 credit on a gig settled at 50% delivers $3.82 and destroys $3.83. `release_booking_benefits` early-returns when captured, and its only trigger fires solely on declined/cancelled, so the applied grant is never returned. `settle_booking_benefits` relaxes the PROMO budget on a partial capture but never touches `bonus_ledger`, so the referral credit alone is destroyed. | Decide: (a) return the unused portion to `bonus_ledger` on a partial capture (symmetric with how promo budget is already relaxed), or (b) state plainly that a credit is spent on use. (a) is the consistent answer given settle_booking_benefits already does it for promos. Either way the current behaviour — silent, asymmetric destruction — is not a choice anyone made. | RESOLVED 2026-08-13 |
| medium | money-paths | On a partial capture the poster's FULL discount is still funded out of a fee computed on the reduced amount, which can drive the platform's share to the floor. Related to the settle-below-cost bug (now fixed by flooring) but distinct: it is about whether a 50%-settled gig should still carry 100% of the discount. | Decide whether the discount scales with the capture percentage. Fixing the floor stopped the platform PAYING to settle; this is about how much it collects. | RESOLVED 2026-08-13 |
| high | money-paths | One admin refund permanently silenced every subsequent CHARGEBACK on that payment. `refund_source` is written once and never cleared, `recordReversal` early-returned on it, and that function is shared by `charge.refunded` AND `charge.dispute.created`. The /disputes page, `ctl_external_reversal_not_ledgered` (inner join), `dispute_open_beyond_sla`, and `earner-claim-payment`'s DISPUTE_OPEN gate all read that table — so a live chargeback was invisible to every one of them and the booking would settle. | FIXED 2026-08-13: the exemption is scoped to `kind === 'refund'`. A chargeback is a cardholder going to their bank — an admin cannot initiate one, so the exemption is incoherent there whatever the flag says. Guarded by webhookEventCoverage.test.js, proved to discriminate. 2026-08-13 |
| high | admin-surface | Bank deposits (`stripe_payouts`) had ZERO surface anywhere in admin/ — `grep -rn stripe_payouts admin/` returned nothing. Payout data has been arriving since the payout.* events were enabled, and when an earner asked "where is my money" support could see that we released it and nothing about whether it reached their bank. | FIXED 2026-08-13: a Bank deposits section on /payments showing earner, amount, status, arrival date, method, failure code, and a link to the Stripe payout. Resolves user_id to a profile; an unmapped connected account renders as "unmapped account" rather than being hidden, since those are exactly the rows needing reconciliation. Admin deployed. 2026-08-13 |
| medium | admin-surface | `revokeGrant` (promotions/actions.ts:234) is a DEAD server action — defined, exported, zero callers anywhere in admin/. An admin can mint promo codes and grant a promotion directly to users, but cannot revoke a grant once issued. `bonus_ledger` is read on the promotions page as aggregate tiles only: no rows, no owner, no action. | Wire revokeGrant to a control on the promotions page (it already writes an audit row), and give bonus_ledger a row-level view with the earner, state and applied booking — it is now the mechanism behind the fee-credit return, so it needs to be inspectable. | FIXED 2026-08-13 — RevokeGrant control wired on /promotions; bonus_ledger now renders rows with earner, state, delivery and applied booking. Admin deployed. |
| high | money-paths | Promo settlement priced the counterfactual at `fee_bps_at(now())` — the standing rate AT CAPTURE — so a rate change re-priced a campaign's historical spend. The rate moved 1000 → 700 bps on 2026-08-12: a 0-bps override on a $100 gig is worth 655c at the booking's rate and 355c at today's, a 300c gap per redemption. Because the rate fell, campaigns were UNDER-charged and budgets outlasted the money actually given away. | FIXED 2026-08-13: `promo_benefit_cents` gained an optional timestamp and `settle_booking_benefits` passes the BOOKING's created_at. `fee_bps_at()` already took a timestamp — it was simply being handed now(). Note: the old 2-arg form had to be DROPPED first or `promo_benefit_cents(int,int)` becomes ambiguous and every caller fails with "function is not unique". 2026-08-13 |
| high | money-paths | `earner-claim-payment` never called `settle_booking_benefits` at all (grep: zero occurrences), so every gig an earner CLAIMED after a poster ghosted them left its campaign permanently charged the worst-case reserve. Budgets exhausted against money never given away and `promo_redemptions` stayed unsettled forever — exactly what `ctl_benefit_never_settled` exists to report. | FIXED 2026-08-13: settles after the money moves, non-fatal like the capture path, and passes the GIG VALUE (captured + discount) rather than the captured amount, matching how stripe-capture-payment calls it. No scaling needed — this path always captures the full hold. 2026-08-13 |
| medium | admin-surface | Remaining console gaps after 2026-08-13: the `/controls` page's "Run sweep now" silently SKIPS both external controls (stripe_reconciliation, stripe_webhook_config), so the only lever the UI offers never runs them and still reports "Sweep complete". CLOSED today: bank deposits (`stripe_payouts`) now have a section on /payments; promo grants are listed with a working Revoke; `bonus_ledger` renders rows with earner/state/applied booking. | Make runSweepNow dispatch the external controls too, or relabel the button so it does not claim a completeness it does not deliver. | FIXED 2026-08-13 — runSweepNow now dispatches reconcile-stripe (which carries both external controls) and reports honestly when it could not. Admin deployed. |
| medium | promotions | Promo CODES are unredeemable: `redeem_promo_code(text)` is defined and granted to `authenticated` (20260806070000_promotions.sql:252,334) but has ZERO callers in src/, web/ or supabase/functions/, and there is no "have a code?" field on any screen. Codes minted by the console's promotions page are dead strings. NOTE the narrower truth: incentives are NOT blocked — `grantToUsers` (admin/app/(console)/pricing/actions.ts:95 → `grant_promotion_to_users`) is fully wired and delivers a campaign directly to a pasted list of users today. | Either add a redemption entry point (Profile → "Have a code?" → `redeem_promo_code`, plus the web equivalent) or stop minting codes in the console until there is one. Minting a code no user can type is worse than not offering codes. | FIXED 2026-08-13 (mobile) — Settings → Money → "Have a code?" calls redeem_promo_code via src/lib/promoCodes.js. WEB STILL HAS NO ENTRY POINT. |
| medium | web-parity | The promo-code redemption entry point landed on MOBILE only. gohustlr.com still has no way to type a code, so a campaign distributed by code reaches phone users and not web users. | Port the Settings → "Have a code?" sheet to the web app, calling the same `redeem_promo_code` RPC, and add it to the parity assertion that already covers 2FA and the assistant gate. | FIXED 2026-08-13 — ported to web/app/(app)/settings with the same one-message rule; parity.test.js now asserts both clients. |
| high | docs | The entire in-person SAFETY subsystem is undocumented: `gig_shares` and `safety_checkins` tables, and `jobs.location` being MASKED server-side with the exact address in `job_locations`. A session that does not know the address is masked can trivially expose it. Also undocumented: `tip_ledger` (the tip idempotency ledger), `stripe_accounts`, `stripe_customers`. | Add a Safety section to CLAUDE.md and document the location-masking contract beside the schema notes. Cross-check against RUNBOOK_SAFETY.md, which may already describe it. | FIXED 2026-08-13 — CLAUDE.md now carries an In-person safety section covering the masking contract, job_locations RLS, gig_shares and safety_checkins; parity.test.js asserts the masking contract stays documented. |
| medium | docs | ~~adminSurface.test.js does not enforce what CLAUDE.md claims~~ — **REFUTED 2026-08-13 by direct test, not argument.** Injected an unregistered `ctl_orphan_probe_xyz` function: the test failed and NAMED it. Injected an `admin/app/(console)/orphanprobe` page: the test failed and named that too. CLAUDE.md's claim is TRUE on both halves. Two independent audit agents asserted it was false and both were wrong — I recorded their agreement without testing it, which is the same over-trust I have been criticising elsewhere. Two agents agreeing is not evidence. | Nothing to fix. Kept in the register as the correction, so the false claim does not get re-derived from the old row. |
| critical | data-loss | **A poster deleting their account destroys every earner's record of paid work for them.** PROVEN on a staged row (rolled back): `profiles → jobs → bookings → payments` are ALL `ON DELETE CASCADE`, and deleting the job took the booking and its payment from 1 to 0. `delete-account` blocks only UNSETTLED bookings, so *completed, verified, paid* work is exactly what cascades. Also destroyed: disputes, messages, tip_ledger, gig_shares, safety_checkins. For the earner this deletes their Jobs Done count, the row from their Transactions statement, and their Tax Center income record — for a 1099 contractor, their tax evidence — by another party's action, with no consent and no notification. | **Needs Chris's decision — it is an erasure-vs-retention call, not a code fix.** Recommended: stop hard-deleting the profile row. Anonymize it instead (scrub name/username/avatar/bio/email, mark `deleted_at`), so the FKs hold and the counterparty's financial records survive. Retaining transaction records is a standard and defensible exception to a right-to-erasure request, and the CURRENT behaviour is the riskier one — it destroys a third party's financial records to satisfy the first party. Alternative (worse): block deletion for anyone who has ever transacted, which makes accounts permanently undeletable. Do NOT simply flip the FKs to SET NULL: that orphans payments from any booking and breaks the ledger's two-query side attribution. | DECIDED + FIXED 2026-08-13 |
| high | money-policy | **Needs your decision, not a code fix.** `record_reversal` ("Record chargeback") calls `record_refund`, which unconditionally `debit_earnings(earner_share)` — but unlike the `refund` op it makes NO Stripe call at all. The `refund` op EARNS its debit by passing `reverse_transfer: true`, actually pulling money back from the connected account. A chargeback on a destination charge debits the PLATFORM balance and does not reverse the transfer (RUNBOOK_MONEY:78 already says "the platform eats it"), so the earner keeps the cash while their displayed earnings drop by their share. `debit_earnings` writes no debt row and recovers nothing — it just makes the number smaller. | Decide the policy: (a) the platform absorbs chargebacks — then stop debiting the earner on this path; or (b) the earner shares the loss — then it needs a real clawback (a debt row, or reverse_transfer where the balance allows) instead of a cosmetic decrement. Today it is the worst of both: the earner's ledger says they lost money nobody took back. | DECIDED + FIXED 2026-08-13 |

## Other registers that feed this one

- **Payments/incentives audit (2026-08-12)** — ~72 findings in 6 root causes, produced by a
  read-only session and never written to disk. Three of the worst are now closed
  (`ctl_escrow_hold_expiring_work_done` blind for a week; poster-discount campaigns
  refunding themselves at capture; fee overrides delivering 2.7× their cap). The rest are
  recoverable from that session's transcript by searching other sessions for
  `LIVE / DORMANT`. **Anything confirmed there belongs in the table above, not in a
  transcript.**
- `KNOWN_RISKS.md` — accepted risks and beta-readiness. Different purpose: that file is
  what we have DECIDED to live with; this one is what has not been done yet.
