// Re-exported from the shared cross-platform package so mobile + web stay in sync.
// Edit the logic in shared/analytics.js — not here. (Named "insights" to avoid
// colliding with src/lib/analytics.js, which is the monitoring/error-capture module.)
export * from '../../shared/analytics.js';
